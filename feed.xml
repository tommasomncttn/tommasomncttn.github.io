<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" xml:lang="en"><generator uri="https://jekyllrb.com/" version="4.4.1">Jekyll</generator><link href="https://tommasomncttn.github.io/feed.xml" rel="self" type="application/atom+xml"/><link href="https://tommasomncttn.github.io/" rel="alternate" type="text/html" hreflang="en"/><updated>2026-08-17T05:38:23+00:00</updated><id>https://tommasomncttn.github.io/feed.xml</id><title type="html">blank</title><subtitle>A simple, whitespace theme for academics. Based on [*folio](https://github.com/bogoli/-folio) design. </subtitle><entry><title type="html">First Dialogue on RLHF (with a bit of VAE): REINFORCE</title><link href="https://tommasomncttn.github.io/blog/2026/first-dialogue-on-rlhf-with-a-bit-of-vae-reinforce/" rel="alternate" type="text/html" title="First Dialogue on RLHF (with a bit of VAE): REINFORCE"/><published>2026-08-15T00:00:00+00:00</published><updated>2026-08-15T00:00:00+00:00</updated><id>https://tommasomncttn.github.io/blog/2026/first-dialogue-on-rlhf-with-a-bit-of-vae-reinforce</id><content type="html" xml:base="https://tommasomncttn.github.io/blog/2026/first-dialogue-on-rlhf-with-a-bit-of-vae-reinforce/"><![CDATA[<p><em>P: In which stage can you decompose the most common post-training pipelines?</em></p> <p>T: We begin by <strong>SFT/IFT</strong> (instruction or supervised fine-tuning). The aim is to teach the model to follow QA; in this way, user can ask questions and obtain answer, that is, interact. It turns a next-token predictor (a world model if you wish), into a “persona”. We rely on the same training objective and we focus on a small dataset of high quality examples.</p> <div class="notion-columns"> <div class="notion-column" style="flex: 50 1 0%"> <p><img src="/assets/img/posts/first-dialogue-on-rlhf-with-a-bit-of-vae-reinforce/notion-3bd815d2904b8091a5e8ff974cf66350.png" alt="first-dialogue-on-rlhf-with-a-bit-of-vae-reinforce"/></p> <p><em>Fig 1.,</em> a reward model is a machine that takes text and tells you how good it was.</p> </div> <div class="notion-column" style="flex: 50 1 0%"> <p>The second step is <strong>RLHF</strong>, which aims to improve the style of the answers. We need to teach the model to follow some vague metric of quality, which we cannot compute via a hand-designed function, e.g., how could you define a function over a <a href="https://en.wikipedia.org/wiki/Kleene_star">Kleene Star</a> \(\mathcal V^*\) that returns a scalar proportional to how funny is the message?. Then, we simply use a reward model that is trained to learn our preference and map each model output to its answer</p> </div> </div> <p>The third step is RLVR, where we boost the actual performance of the model on verifiable domains via more RL training.</p> <p><em>P: For example, in which way could RLHF improve the style of the answer of a simple SFT model?</em></p> <p>T: Well, for example if I ask to the base model “Who is Franco Battiato?” it would answer directly (“Franco Battiato is the Italian greatest songwriter of 20th century” ) and then start to speak about random topics related to its music until it hits the <code class="language-plaintext highlighter-rouge">token max</code> threshold. Instead, the RLHF model would simply give me a synthetic answer.</p> <p><em>P: What is the difference between the Superficial Alignment Hypothesis [2] and the Elicitation theory?</em></p> <div class="notion-columns"> <div class="notion-column" style="flex: 50 1 0%"> <p>T: Both believes that most of the skills are acquired during pertaining. Yet, SAH asserts that you just need few examples to elicit them, and therefore post-training is a tiny and shallow layer on pretraining. Nathan instead believes that skills are grown out from the base model with a lot of effort. He has a more pragmatic view: here is where a lot of money are spent, and even training hours. Here is where we see the core differences between a good and an excellent frontier model. Like in F1, you spend a bit of time at the beginning of the year building the car chassis to be a super cool skeleton, but then you spend a shit ton of time improve aerodynamics and all that boring stuff to beat the other apes competing to go faster than you.</p> </div> <div class="notion-column" style="flex: 50 1 0%"> <p><img src="/assets/img/posts/first-dialogue-on-rlhf-with-a-bit-of-vae-reinforce/notion-3bd815d2904b80cb80b3f8561b26af7d.png" alt="first-dialogue-on-rlhf-with-a-bit-of-vae-reinforce"/></p> <p><em>Fig. 2,</em> an image of a car chassis as I had no clue about what it was. I would like to press a button to destroy F1 from the collective memory.</p> </div> </div> <p><em>P: why do we need to rely on RL for training during RLHF?</em></p> <p>T: Because the reward model has as input a sampled answer from the model:</p> \[\operatorname{reward} = \operatorname{RM(generated-text)}\] <p>Sampling is not a differentiable function. Yet, there is a known trick in RL to solve this problem: <strong>policy gradient methods (REINFORCE).</strong></p> <p><em>P: Can you elaborate a bit on the core methodology to differentiate through sampling via</em></p> <p>T: Let’s says i have a prompt \(\mathbf c\) and my model, parametrized via \(\theta\), defines a probability distribution on an answer \(\mathbf y \vert \mathbf c\) of other \(t\) token autoregressively:</p> \[Y | C= \mathbf c \sim\pi_\theta(\mathbf y \mid \mathbf c) = \prod_{t=1}^{T} \pi_\theta(y_t \mid \mathbf c, \mathbf y_{&lt;t})\] <p>Then we have a reward model \(R: \mathcal V^* \to \R\). Ideally, we would like to shakes \(\theta\) via a gradient that maximizes the expected reward (which we approximate via <em>Monte Carlo approximation</em>, that is \(E_{p} [X] \approx \frac 1 n \sum^n_i x_i\) with \(x_i \overset{iid} \sim X\)):</p> \[J(\theta) = \mathbb{E}_{y \sim \pi_\theta(\cdot \mid \mathbf c)}\big[R(\mathbf y)\big] = \sum_{\mathbf y \in \mathcal{V}^T} \pi_\theta(\mathbf y \mid \mathbf c)\, R(\mathbf y)\] <p>Here you can see the problem clearly:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="bp">...</span>
<span class="n">logits</span> <span class="o">=</span> <span class="nf">policy</span><span class="p">(</span><span class="n">prompt</span><span class="p">)</span>          <span class="c1"># all good we can backprop.
</span><span class="n">y</span> <span class="o">=</span> <span class="nf">sample</span><span class="p">(</span><span class="n">logits</span><span class="p">)</span>               <span class="c1"># fook we cannot backprop. (+ discrete!)
</span><span class="n">reward</span> <span class="o">=</span> <span class="nc">RM</span><span class="p">(</span><span class="n">y</span><span class="p">)</span>                   <span class="c1"># all good... but gradient arrives dead.
</span><span class="bp">...</span>
</code></pre></div></div> <details class="notion-toggle"> <summary><strong>Connection with VAE</strong></summary> <p>This is similar to the problem we have with VAE. There we need the <strong>reparametrization trick</strong> as we cannot back-propagate via a gaussian:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="bp">...</span>
<span class="n">mu</span><span class="p">,</span> <span class="n">sigma</span> <span class="o">=</span> <span class="nf">encoder</span><span class="p">(</span><span class="n">x</span><span class="p">)</span> <span class="c1"># all good we can backprop.
</span><span class="n">z</span> <span class="o">=</span> <span class="n">random</span><span class="p">.</span><span class="nf">normal</span><span class="p">(</span><span class="n">mu</span><span class="p">,</span><span class="n">sigma</span><span class="p">)</span> <span class="c1"># fook we cannot backprop.
</span><span class="n">x_recon</span> <span class="o">=</span> <span class="nf">decoder</span><span class="p">(</span><span class="n">z</span><span class="p">)</span> <span class="c1"># all good we can backgrop.
</span></code></pre></div> </div> <p>This was solved via the reparametrization which isolate the sampling in a branch where our gradient does not flow backwards via backpropagation</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="bp">...</span>
<span class="n">mu</span><span class="p">,</span> <span class="n">sigma</span> <span class="o">=</span> <span class="nf">encoder</span><span class="p">(</span><span class="n">x</span><span class="p">)</span> <span class="c1"># all good we can backprop.
</span><span class="n">e</span> <span class="o">=</span> <span class="n">random</span><span class="p">.</span><span class="nf">normal</span><span class="p">(</span><span class="mi">0</span><span class="p">,</span><span class="n">I</span><span class="p">)</span> <span class="c1"># we do not care
</span><span class="n">z</span> <span class="o">=</span> <span class="n">mu</span> <span class="o">+</span> <span class="n">e</span><span class="o">*</span><span class="n">sigma</span> <span class="c1"># all good now
</span><span class="n">x_recon</span> <span class="o">=</span> <span class="nf">decoder</span><span class="p">(</span><span class="n">z</span><span class="p">)</span> <span class="c1"># all good we can backgrop.
</span></code></pre></div> </div> </details> <p>Then what is the solution? We do not have a clean trick as the <em>reparametrization trick</em>, where we can keep the same flow and just change its computation instantiation. With <strong>REINFORCE</strong> (policy gradient), we change what we differentiate for, yet it works! Check this out:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="bp">...</span>
<span class="n">y</span> <span class="o">=</span> <span class="nf">sample</span><span class="p">(</span><span class="n">logits</span><span class="p">)</span>               <span class="c1"># we do not care (no grad through this branch)
</span><span class="n">reward</span> <span class="o">=</span> <span class="nc">RM</span><span class="p">(</span><span class="n">y</span><span class="p">).</span><span class="nf">detach</span><span class="p">()</span>          <span class="c1"># we do not care (just a constant)
</span><span class="n">logp</span> <span class="o">=</span> <span class="nf">log_prob</span><span class="p">(</span><span class="nf">policy</span><span class="p">(</span><span class="n">prompt</span><span class="p">),</span> <span class="n">y</span><span class="p">)</span>   <span class="c1"># all good we can backprop.
</span><span class="n">loss</span> <span class="o">=</span> <span class="o">-</span><span class="n">reward</span> <span class="o">*</span> <span class="n">logp</span>            <span class="c1"># all good — Adam on this.
</span><span class="bp">...</span>
</code></pre></div></div> <p>The core idea is that we stop optimizing \(\theta\) to improve the expected reward of its answer directly. Instead, we <strong>try to optimize</strong> \(\theta\) <strong>to increase the probability of answer that have good reward</strong>. In practice, we say:</p> <ol> <li>stop doing this: “sample y, compute R(y), call <code class="language-plaintext highlighter-rouge">.backward()</code> on R.”</li> <li>start doing this: “sample y, compute R(y), call <code class="language-plaintext highlighter-rouge">.backward()</code> on <code class="language-plaintext highlighter-rouge">-R·log πθ(y)</code>.”</li> </ol> <p>On the one hand, this is now possible as the gradient is inside the sampling, so we can simply kill the sampling part via a monte carlo approximation. On the other hand, this core identity \(\nabla_\theta \pi_\theta = \pi_\theta \, \nabla_\theta \log \pi_\theta\) shows us that is the same (do not be scare it is the classic \(\frac{d \log f(x)} {dx} = \frac 1 x \frac{d f(x)} {d x}\to \frac{d f(x)} {d x} = x\frac{d \log f(x)} {dx}\)):</p> \[\underbrace{\nabla_\theta\, \mathbb{E}_{\mathbf y \sim \pi_\theta}\big[R(\mathbf y)\big]}_{\text{the gradient of the true objective}} = \mathbb{E}_{\mathbf y \sim \pi_\theta}\Big[\underbrace{R(\mathbf y)\,\nabla_\theta \log \pi_\theta(\mathbf y)}_{\text{what recipe B computes per sample}}\Big] \approx \sum_{\mathbf y \sim \pi_\theta}\Big[\underbrace{R(\mathbf y)\,\nabla_\theta \log \pi_\theta(\mathbf y)}_{\text{what recipe B computes per sample}}\Big]\] <p>A closing remark: the reason why this new objective is equivalent to optimize the weights with steps that increase chance of good samples, is that is is a weighted sum where the weights are given via \(R(\mathbf y)\). If I am trying to optimize \(\mathcal L = \mathbf c^{\top} f_{\theta}(\mathbf y)\) by linearity of differentiation my gradient will be:</p> \[\nabla_\theta \mathcal{L} = \mathbf{c}^\top \nabla_\theta f_\theta(\mathbf{y}) = \sum_{i} c_i \, \nabla_\theta \big[f_\theta(\mathbf{y})\big]_i\] <p>Therefore I will move a lot in the directions \(\big[f_\theta(\mathbf{y})\big]_i\) where \(c_i \approx 1\) (is very high), and a little when \(c_i \approx 0\) (is very little)</p> <hr/> <p><a href="https://rlhfbook.com/c/01-introduction">[1] RLHF Book</a></p> <p><a href="https://arxiv.org/pdf/2305.11206">[2] LIMA: Less Is More for Alignment</a></p>]]></content><author><name></name></author><category term="blog"/><category term="tutorial"/><category term="ml"/><summary type="html"><![CDATA[Random walks in RLHF exposed via a dialogue format.]]></summary></entry><entry><title type="html">Notes on Diffusion Models</title><link href="https://tommasomncttn.github.io/blog/2026/notes-on-diffusion-models/" rel="alternate" type="text/html" title="Notes on Diffusion Models"/><published>2026-08-14T00:00:00+00:00</published><updated>2026-08-14T00:00:00+00:00</updated><id>https://tommasomncttn.github.io/blog/2026/notes-on-diffusion-models</id><content type="html" xml:base="https://tommasomncttn.github.io/blog/2026/notes-on-diffusion-models/"><![CDATA[<h2 id="a-denoising-diffusion-probabilistic-models-ddpms">A. Denoising Diffusion Probabilistic Models (DDPMs)</h2> <p>Diffusion models consist of two process. A fixed forward process where you add <em><strong>gaussian</strong></em> noise to input image, a learned reverse process where you remove noise.</p> <p><img src="/assets/img/posts/notes-on-diffusion-models/notion-3bc815d2904b80b79bc4ceeb57395ad4.png" alt="notes-on-diffusion-models"/></p> <h3 id="1-the-forward-process-mathematical-definition">1. The Forward Process: Mathematical Definition</h3> <p>The forward process is a fixed Markov chain that adds Gaussian noise according to a pre-defined variance schedule \(β_t\)</p> <p><img src="/assets/img/posts/notes-on-diffusion-models/notion-3bc815d2904b8073abd7c6d54e672d5e.png" alt="notes-on-diffusion-models"/></p> <p><strong>SINGLE STEP TRANSITION</strong> The transition from one timestep to the next is defined as a Gaussian distribution where the mean is a scaled version of the previous state \(x_{t-1}\) and the variance is fixed by the schedule \(\beta_t\) s.t. \(t \to T\) then \(\beta_t : 0 \to 1\)</p> \[q(x_t|x_{t-1}) = \mathcal{N}(x_t; \sqrt{1-\beta_t} x_{t-1}, \beta_t \mathbf{I})\] <p><strong>JOINT DISTRIBUTION</strong> Given that we have a markovian structure we can simply define the joint distribution:</p> \[q(x_0,...,x_t) = q(x_0)\prod_tq(x_t | x_{t-1})\] <p><strong>CLOSED FORM t-STEP MARGINAL</strong> A key result is that \(x_t \vert x_0\) has a closed gaussian form. This means we can sample \(x_1,...,x_T\) in parallel and is super-useful to speed up training</p> <p><img src="/assets/img/posts/notes-on-diffusion-models/notion-3bc815d2904b8001bbede2a9b8f7951c.png" alt="notes-on-diffusion-models"/></p> <p>In practice we sample from it via the <strong>reparametrization trick</strong> that makes us write:</p> \[x_t = \sqrt{\bar{\alpha}_t }x_0+ \sqrt{(1-\bar{\alpha}_t )}\epsilon \;\;\;\; \epsilon \sim N(0,I)\] <p>Which means we just need to compute alpha, take the original image and sample \(\epsilon\).</p> <div class="notion-callout notion-callout--red"> <div class="notion-callout-icon">🚨</div> <div class="notion-callout-body"> <p><strong>NB</strong> here the coefficient \(\bar{\alpha}_t\) is opposite to \(\beta_t\). While \(\beta_t\) is increasing and \(\beta_0 = 0\) instead \(\bar{\alpha}_t\) is decreasing and \(\bar{\alpha}_T=0\)</p> </div> </div> <p><strong>DERIVING CLOSED FORM GAUSSIAN</strong> To derive the close form gaussian set \(\alpha_t = (1-\beta_t)\) as a useful short hand and start by using the reparametrization trick to write \(q(x_t \vert x_{t-1})\):</p> \[x_t = \sqrt{{\alpha} }x_{t-1}+ \sqrt{\beta_t }\epsilon \;\;\;\; \epsilon \sim N(0,I)\] <p>Now by linearity of gaussian we know that the all the \(x_t \vert x_0\) will be gaussian, so we just unroll the mean and the variance to learn them</p> <details class="notion-toggle"> <summary><strong>solution</strong></summary> <p><img src="/assets/img/posts/notes-on-diffusion-models/notion-3bc815d2904b803fb89ae37f36618914.png" alt="notes-on-diffusion-models"/></p> </details> <p><strong>IMPORTANCE OF GAUSSIANITY</strong> The choice of Gaussian noise is not arbitrary; gaussian maps been closed under sum, linear maps…allow for the simple, closed-form expressions for:</p> <ul> <li>the forward marginal \(q(x_t \vert x_0)\)</li> <li>the <em><strong>forward posterior</strong></em> \(q(x_{t-1} \vert x_t, x_0)\) → <img src="/assets/img/posts/notes-on-diffusion-models/notion-3bc815d2904b8039b814dacd2fa8babf.png" alt="notes-on-diffusion-models"/></li> </ul> <p>If another noise distribution like <strong>Laplace</strong> or uniform were used, these elegant closed forms would not exist, <em><strong>making the training objective (the ELBO terms) and the sampling updates intractable.</strong></em></p> <h3 id="2-the-reverse-process-and-training-objective">2. The Reverse Process and Training Objective</h3> <p>Now that we saw how to generate noisier image in the forward pass we want to learn the true reverse pass to generate denoised image. That is we want to learn \(\theta\) such that our parametrized distribution \(p_{\theta}\) is similar to the true reverse process \(p\):</p> \[p_θ(x_{0:T})=p(x_T)∏^T_tp_θ(x_{t−1}∣x_t) \approx p(x_T)∏^T_tp(x_{t−1}∣x_t) = p(x_{0:T})\] <p><strong>WHY LEARN ? INTRACTABILITY OF REVERSE</strong> The main problem of denoising is that \(x_{t-1} \sim q(x_{t-1} \vert x_t)\) is intractable. So we cannot compute it simply as in the forward process and we need to learn</p> <p><img src="/assets/img/posts/notes-on-diffusion-models/notion-3bc815d2904b8002a2f8ddf2412ecd9f.png" alt="notes-on-diffusion-models"/></p> <p><strong>LEARNING PROBLEM</strong> The goal of training in the reverse process is to learn a neural network \(p_θ\) that can approximate the true reverse denoising distribution. So we could try to maximize the likelihood of real data \(x_0\) under our parametrization \(p_{\theta}\) but we cannot compute such distribution! but we can do ELBO!</p> <p><strong>ELBO</strong> Training aims to maximize the log-likelihood of the data, <code class="language-plaintext highlighter-rouge">log pθ(x₀)</code>, which is computationally intractable. Instead, we maximize a tractable lower bound, the Evidence Lower Bound (ELBO). For diffusion models, the negative log-likelihood can be bounded by a sum of Kullback-Leibler (KL) divergence terms:</p> \[\mathbb{E}{q(x_0)}[-\log p\theta(x_0)] \le \mathbb{E}[ D_{KL}(q(x_T|x_0) || p(x_T)) ] + \sum_{t=2}^{T} \mathbb{E}[ D_{KL}(q(x_{t-1}|x_t, x_0) || p_\theta(x_{t-1}|x_t)) ] - \mathbb{E}[\log p_\theta(x_0|x_1)]\] <p>This can be written more compactly as \(≤ L_T + Σ_{t=2}^T L_{t-1} + L_0\). Minimizing this loss is equivalent to making the learned reverse distribution <code class="language-plaintext highlighter-rouge">pθ</code> match the true (but intractable) reverse distribution <code class="language-plaintext highlighter-rouge">q</code>. Each of these terms has a meaning:</p> <p><img src="/assets/img/posts/notes-on-diffusion-models/notion-3bc815d2904b80afb7f1df7e2c761d51.png" alt="notes-on-diffusion-models"/></p> <p><strong>THE LOSS TERMS</strong> \(L_{t-1}\) The core of the training involves minimizing the \(L_{t-1}\) term. I_<strong>f we assume the learned reverse process</strong>_ \(p_θ\) <em><strong>is also Gaussian with a learned mean</strong></em> \(µ_θ(x_t, x_0)\) and a fixed variance \(σ²_t*I\)</p> <p><img src="/assets/img/posts/notes-on-diffusion-models/notion-3bc815d2904b80adaf7bf7ed719e823b.png" alt="notes-on-diffusion-models"/></p> <p>then the KL divergence simplifies significantly. It becomes an MSE term between <strong>the predicted reverse mean</strong> µ_θ and the <strong>true forward posterior mean</strong> \(µ̃_t\), plus constants that do not depend on µ_θ:</p> \[L_{t-1} = \mathbb{E} \left[ \frac{1}{2\sigma_t^2} || \mu_\theta(x_t, t) - \tilde{\mu}_t(x_t, x_0) ||_2^2 \right] + \text{const}\] <p><strong>SIMPLIFYING</strong> \(L_{t-1}\) <strong>FURTHER VIA</strong> \(\epsilon\) Start from the the reparametrization of the t-step forward marginal:</p> \[x_t = \sqrt{\bar{\alpha}_t }x_0+ \sqrt{(1-\bar{\alpha}_t )}\epsilon \;\;\;\; \epsilon \sim N(0,I)\] <p>Then notice this implies:</p> \[x_0 = \frac{(x_t - \sqrt{1-\bar{\alpha_t}}\epsilon)}{\sqrt{\bar{\alpha_t}}}\] <p>Now remember that the forward posterior mean is:</p> <p><img src="/assets/img/posts/notes-on-diffusion-models/notion-3bc815d2904b80ba8294f048ab4b112b.png" alt="notes-on-diffusion-models"/></p> <p>So by using both we arrive to:</p> <p><img src="/assets/img/posts/notes-on-diffusion-models/notion-3bc815d2904b801287fcdfa1c0e5bcad.png" alt="notes-on-diffusion-models"/></p> <p>If now we choose our parametrization for \(\mu_{\theta}\) (we can do whatever we want with it) as:</p> <p><img src="/assets/img/posts/notes-on-diffusion-models/notion-3bc815d2904b80b39bb5d36ecb393bfc.png" alt="notes-on-diffusion-models"/></p> <p>Then \(L_{t-1}\) simplifies to</p> <p><img src="/assets/img/posts/notes-on-diffusion-models/notion-3bc815d2904b80e79055f37e489ef8f0.png" alt="notes-on-diffusion-models"/></p> <details class="notion-toggle"> <summary><strong>Proof</strong></summary> <p><img src="/assets/img/posts/notes-on-diffusion-models/notion-3bc815d2904b80c299a8d9975dddb2a7.png" alt="notes-on-diffusion-models"/></p> </details> <p><strong>DROP WEIGHT</strong> \(w_t\) While the ELBO provides a theoretically sound objective, a simplified version having only the expectation term \(L_{t-1}^{simple} =\mathbb{E}[ \vert \epsilon - \epsilon_\theta(x_t, t) \vert _2^2 ]\) often yields better practical results.</p> <ol> <li><strong>Rebalancing toward hard regimes:</strong> It gives uniform emphasis to all timesteps, forcing the model to become accurate in the highest-noise regimes where reverse sampling begins, which is crucial for forming the sample’s global structure.</li> <li><strong>Lowering loss-scale variation:</strong> It removes per-step loss scaling differences from the <code class="language-plaintext highlighter-rouge">βt</code> schedule, which can stabilize optimization.</li> <li><strong>Improving empirical quality:</strong> Although it no longer optimizes a tight likelihood bound, the uniform emphasis empirically improves perceptual metrics like FID.</li> </ol> <h3 id="3-sampling-process-via-ancestral-sampling">3. Sampling Process Via Ancestral Sampling</h3> <p>Once the noise prediction network ϵ_θ is trained, we can generate new samples using an iterative ancestral sampling process.</p> <p><strong>UPDATE RULES</strong> Starting from pure noise \(x_T ~ N(0, I)\), we iterate backward from \(t=T\) to \(t=1\). Each step \(x_{t-1}\) is sampled from the learned reverse distribution \(p_θ(x_{t-1} \vert x_t).\) Using the <code class="language-plaintext highlighter-rouge">ϵ</code>-prediction parameterization, the update step is:</p> <p><img src="/assets/img/posts/notes-on-diffusion-models/notion-3bc815d2904b801d8f50c4e5a758f7cf.png" alt="notes-on-diffusion-models"/></p> <p><strong>TWO CHOICE FROM AMOUNT OF STOCHASTICITY</strong> \(σ_t\). The term \(σ_t\) controls the amount of stochasticity in the reverse step. There are two common choices:</p> <ul> <li>\(σ_t = √β_t\): This choice injects more noise, leading to higher variance and diversity.</li> <li>\(σt = √β̃_t\): This choice, derived from the true posterior variance, injects less noise. It produces sharper, lower-variance samples but relies more heavily on the network’s accuracy.</li> </ul> <p>This represents a bias-variance trade-off between diversity (higher <code class="language-plaintext highlighter-rouge">σt</code>) and fidelity (lower <code class="language-plaintext highlighter-rouge">σt</code>).</p> <p><strong>FINAL IMAGE ESTIMATOR</strong> At any timestep \(t\), we can get an estimate of the final clean image \(x_₀\) by <em><strong>rearranging the forward process t- step reparameterization</strong></em> and replacing the unknown true noise ϵ with the network’s prediction ϵ_θ. Start from here:</p> \[x_t = \sqrt{\bar{\alpha}_t }x_0+ \sqrt{(1-\bar{\alpha}_t )}\epsilon \;\;\;\; \epsilon \sim N(0,I)\] <p>extract \(x_0\):</p> \[x_0 = \frac{(x_t - \sqrt{1-\bar{\alpha_t}}\epsilon)}{\sqrt{\bar{\alpha_t}}}\] <p>substitute \(\epsilon \to \epsilon_{\theta}\):</p> <p><img src="/assets/img/posts/notes-on-diffusion-models/notion-3bc815d2904b8033b341fd0c2b09010d.png" alt="notes-on-diffusion-models"/></p> <h3 id="4-additional-content-detail-tradeoff">4. Additional: Content-Detail Tradeoff</h3> <p><img src="/assets/img/posts/notes-on-diffusion-models/notion-3bc815d2904b80fbba31e9d42a0ff46b.png" alt="notes-on-diffusion-models"/></p> <hr/> <div class="notion-divider">✠</div> <div class="notion-divider">✠</div> <hr/> <h2 id="b-the-sde-framework-a-continuous-time-perspective">B. The SDE Framework: A Continuous-Time Perspective</h2> <h3 id="1-introduction-generalizing-to-continuous-time">1. Introduction: Generalizing to Continuous Time</h3> <p><em><strong>The discrete-time DDPM process can be viewed as a numerical discretization of a continuous-time Stochastic Differential Equation (SDE)</strong></em>. Adopting this perspective provides a more general mathematical foundation that unifies different diffusion model variants and opens the door to more flexible and powerful sampling methods.</p> <h3 id="2-sde-background">2. SDE BACKGROUND</h3> <p>An ordinary differential equations can be euler approximated via:</p> \[x(t+\Delta t) \approx x(t)+f(x(t),t) \Delta t\] <p>Here it tells you that your particle at time \(t+\Delta t\) will be in the previous position + a <strong>drift term</strong> defined by \(f(position,time)\). An SDE is the same but it adds a random increment on top:</p> \[x(t+\Delta t) \approx x(t)+f(x(t),t) \Delta t + g(t) \sqrt{\Delta t} \epsilon \;\;\; \epsilon \sim N(0,I)\] <p>So the next position does not depend only on the drift but also on a random steps (<strong>diffusion term</strong>). If we want to know the actual SDE rather than this approximation we just need to pull the \(\Delta_t \to dt\) by making the step very small and we get a <strong>Wiener (Brownian) Process</strong>:</p> \[dx_t = f(x_t,t)dt + g(t)dW_t\] <p>The Wiener process is a sequence of rv has the charactersitic distribution:</p> \[\{W_i\}_{i\ge 0}\ \text{s.t.}\ W_j - W_k \sim \mathcal{N}(0,\ j-k)\] <p>So that:</p> \[dW_t \approx W_{t+dt}-W_t \sim \mathcal{N}(0,\ dt)\] <h3 id="3-forward-diffusion">3. Forward Diffusion</h3> <p>By considering an infinitesimally small time step and applying a first-order Taylor expansion to the discrete forward step, we can derive the continuous-time forward SDE.</p> \[dx_t = -\frac{1}{2} \beta(t) x_t dt + \sqrt{\beta(t)} dW_t\] <ul> <li><strong>DRIFT TERM</strong> \(-\frac{1}{2} \beta(t) x_t\) is the <strong>drift</strong> term, which pulls the state toward the origin</li> <li><strong>DIFFUSION TERM</strong> \(\sqrt{\beta(t)}\) is the <strong>diffusion</strong> term, which controls the magnitude of the random noise \(dW_t\) (a Wiener process).</li> </ul> <p><strong>GAUSSIANITY OF t-STEP FORWAD</strong> A very cool step of using this diffusion sde for the noising forward process is that we get a gaussian t-step forward process:</p> \[q(x_t|x_0) = N(\alpha_tx_0, \sigma^2I)\] <p>With \(\alpha_t = \exp\!\left(-\frac{1}{2}\int_{0}^{t}\beta(s)\,ds\right), \qquad\sigma_t^{2} = 1 - \alpha_t^{2}.\) This allow us to sample directly in one shot the diffusion output:</p> \[x_t = \alpha_t x_0 + \sigma_t \epsilon, \qquad \epsilon \sim \mathcal{N}(0, I).\] <h3 id="4-reverse-diffusion">4. Reverse Diffusion</h3> <p>A key result in stochastic calculus shows that <em><strong>any diffusion process has a corresponding reverse-time SDE.</strong></em> The general form is:</p> \[d_{x_t} = [ f(x_t, t) - g(t)²∇_{x_t} log(q_t(x_t)) ] dt + g(t) dW_t\] <p>For our specific SDE, this becomes</p> \[dx_t = \left[ -\frac{1}{2} \beta(t)x_t - \beta(t)\nabla_{x_t} \log q_t(x_t) \right] dt + \sqrt{\beta(t)} dW_t\] <p>his equation has two main components in its drift term:</p> <ul> <li><strong>FORWARD DRIFT TERM</strong> the original forward drift \(f(x_t, t) = -\frac{1}{2} \beta(t)x_t\)</li> <li><strong>CORRECTION TERM</strong> crucial correction term involving the <strong>score function</strong> \(\beta(t)\nabla_{x_t} \log q_t(x_t)\)where \(g(t)= \beta(t)\) , <code class="language-plaintext highlighter-rouge">∇xt log qt(xt)</code>, <em>which steers the sampling process toward high-density regions of the true data distribution.</em></li> </ul> <h3 id="5-denoising-score-matching">5. Denoising Score Matching</h3> <p>To use the reverse SDE for generation (reverse process), we must know the score function \(\nabla_{x_t} \log q_t(x_t)\). Indeed, is the only uknown in the reverse process formula, as we do define in the forward the drift term \(\beta(t)\) ourselves. We can learn it via a neural network \(s_{\theta}\).</p> <p><strong>INTRACTABILITY OF MARGINAL SCORE.</strong> Directly learning the marginal score is intractable:</p> <p><img src="/assets/img/posts/notes-on-diffusion-models/notion-3bc815d2904b80be81d0f865d0e270e5.png" alt="notes-on-diffusion-models"/></p> <p>The key reason why is intractable is that we cannot compute \(q_t(x_t)\) as, to do so, we need to compute \(q_t(x_t) = ∫ q_t(x_t \vert x_0)q_0(x_0)dx_0\) which is a is a complex mixture over the entire dataset, making its score computationally infeasible. Indeed, one can show that the score is equal to this via the <strong>Fisher Identites:</strong></p> <p><img src="/assets/img/posts/notes-on-diffusion-models/notion-3bc815d2904b805e8713efbcf0158195.png" alt="notes-on-diffusion-models"/></p> <p>Now while the conditional score \(\nabla_{x_t} \log q_t(x_t \vert x_0)\) is closed form as \(q_t(x_t \vert x_0)\) is gaussian (showed above), the expectation is on a distribution we do not know and is intractable \(q(x_0 \vert x_t)\) (if we knew it we could directly generate with it )</p> <details class="notion-toggle"> <summary><strong>Proof</strong></summary> <p>Open the derivative + law of total probability</p> <p><img src="/assets/img/posts/notes-on-diffusion-models/notion-3bc815d2904b8027bdfeeb44611cecdc.png" alt="notes-on-diffusion-models"/></p> <p>Leibniz rule interchange integral and delta</p> <p><img src="/assets/img/posts/notes-on-diffusion-models/notion-3bc815d2904b803f9074cf82ae0874d9.png" alt="notes-on-diffusion-models"/></p> <p>Use the log trick to rewrite the delta of prob distribution</p> <p><img src="/assets/img/posts/notes-on-diffusion-models/notion-3bc815d2904b80819635c4557df3092f.png" alt="notes-on-diffusion-models"/></p> </details> <p><strong>CONDITIONAL SCORE LEARNING</strong> The key insight of Denoising Score Matching (DSM) is to sidestep this problem. Instead of training a model on the intractable <em>marginal score</em>, we train it on the <strong>tractable conditional score:</strong> \(\nabla_{x_t} \log q_t(x_t \vert x_0)\). Indeed, if you learn the score \(\hat{y}\) over (x,y) your optimal y is \(y^* = E[Y \vert X=x]\). Therefore, if you learn the score \(s_{\theta} \to s^*\) is like you learn an expectation of that output for a given input:</p> \[s^*(x_t,t)= \mathbb{E}\!\left[\nabla_{x_t}\log q_t(x_t\mid x_0)\ \big|\ x_t\right]\] <p>But by the fisher identity:</p> \[\nabla_{x_t}\log q_t(x_t)= \mathbb{E}\!\left[\nabla_{x_t}\log q_t(x_t\mid x_0)\ \big|\ x_t\right].\] <p>Therefore:</p> \[s^*(x_t,t)=\nabla_{x_t}\log q_t(x_t).\] <p><strong>LEARNABILITY OF CONDITIONAL SCORE</strong> Now the conditional score is much easier to learn as we target this</p> <p><img src="/assets/img/posts/notes-on-diffusion-models/notion-3bc815d2904b80969752df3bf02a4693.png" alt="notes-on-diffusion-models"/></p> <p>This is easier because thanks to the gaussianity of \(q_t(x_t \vert x_0)\) we can rewrite the conditional score in a closed form:</p> \[\nabla_{x_t}\log q_t(x_t\mid x_0)= -\frac{1}{\sigma_t^{2}}\left(x_t-\alpha_t x_0\right).\] <p>Now we saw that the forward t-step marginal can be written as:</p> \[x_t = \alpha_t x_0 + \sigma_t \epsilon, \qquad \epsilon \sim \mathcal{N}(0, I).\] <p>Therefore, we can plug this formula into \(\frac{1}{\sigma_t^{2}}\left(x_t-\alpha_t x_0\right)\) and we get that the conditional score is:</p> <p><img src="/assets/img/posts/notes-on-diffusion-models/notion-3bc815d2904b80288b86c2b2eb6b307d.png" alt="notes-on-diffusion-models"/></p> <p>Now if we make an extra step and we parametrize the score predictor to be a function of the noise:</p> <p><img src="/assets/img/posts/notes-on-diffusion-models/notion-3bc815d2904b80578641e2abb315daae.png" alt="notes-on-diffusion-models"/></p> <p>We get the same predictor of the \(L_{t-1}\) of the previous chapter:</p> <p><img src="/assets/img/posts/notes-on-diffusion-models/notion-3bc815d2904b801fa0f1fdb56b1f8f9b.png" alt="notes-on-diffusion-models"/></p> <p><strong>SAMPLING</strong> Once we trained \(s_{\theta}\) or \(\epsilon_{\theta}\) we can simply plug them in the reverse drift and generate:</p> \[dx_t = \left[ -\frac{1}{2} \beta(t)x_t - \beta(t)s_{\theta}(x_T) \right] dt + \sqrt{\beta(t)} dW_t\] <hr/> <div class="notion-divider">✠</div> <div class="notion-divider">✠</div> <hr/> <h2 id="c-modern-architectures-and-techniques">C. Modern Architectures and Techniques</h2> <h4 id="41-introduction-from-theory-to-practice">4.1. Introduction: From Theory to Practice</h4> <p>This section covers the key architectural components and advanced techniques that have enabled diffusion models to achieve state-of-the-art performance, moving from the foundational theory to practical, high-performance implementations.</p> <h4 id="42-core-architectural-components">4.2. Core Architectural Components</h4> <ul> <li><strong>Backbone:</strong> The neural network architecture for the denoising model is typically a <strong>U-Net</strong> or a <strong>Denoising Autoencoder</strong>. The U-Net’s skip connections are particularly effective at preserving high-resolution details.</li> <li><strong>Time Representation:</strong> The diffusion timestep <code class="language-plaintext highlighter-rouge">t</code> is a critical input, telling the model how much noise to expect. It is typically encoded into a high-dimensional representation using <strong>sinusoidal positional embeddings</strong> or random Fourier features, which are then integrated into the network’s residual blocks.</li> <li><strong>Diffusion Transformers (DiT):</strong> More recent architectures have replaced the convolutional U-Net with a <strong>Vision Transformer (ViT)</strong>. The noisy image is broken down into a sequence of tokenized patches, and the Transformer operates on this sequence to predict the denoised patches, much like an LLM operates on text.</li> </ul> <h4 id="43-achieving-controllable-generation">4.3. Achieving Controllable Generation</h4> <p>To guide generation toward specific concepts (e.g., a text prompt), guidance techniques are essential. The basis for these techniques is the unconditional score <code class="language-plaintext highlighter-rouge">sθ(xt)</code>.</p> <p><strong>Step 1: Classifier Guidance.</strong> This technique uses a separate, pre-trained classifier <code class="language-plaintext highlighter-rouge">pφ(y|xt)</code> to steer the diffusion process. At each sampling step, the score is modified by adding the gradient of the classifier’s log-probability, effectively pushing the sample <code class="language-plaintext highlighter-rouge">xt</code> to be more recognizable as the desired class <code class="language-plaintext highlighter-rouge">y</code>. The guided score is approximated as: s_\theta(x_t) + \nabla_{x_t} \log p_\phi(y|x_t)</p> <p><strong>Step 2: Classifier-Free Guidance (CFG).</strong> A more modern and widely used technique, CFG eliminates the need for an external classifier. It relies on a single conditional diffusion model trained on both conditional and unconditional inputs (by randomly dropping the conditioning information during training). During sampling, the final score is an extrapolation away from the unconditional score and towards the conditional score, creating an “implicit classifier” from the diffusion model itself.</p> <h4 id="44-improving-efficiency-with-latent-diffusion">4.4. Improving Efficiency with Latent Diffusion</h4> <p>Running the diffusion process on high-resolution images is computationally expensive. Latent Diffusion Models (LDMs), such as Stable Diffusion, solve this by performing diffusion in a much smaller, compressed latent space. This is a two-stage process:</p> <ol> <li><strong>Train an Autoencoder:</strong> A powerful convolutional autoencoder is trained first. The encoder maps a high-resolution image to a low-dimensional latent representation, and the decoder reconstructs the image from this latent.</li> <li><strong>Run Diffusion in Latent Space:</strong> The entire forward (noising) and reverse (denoising) diffusion process is then performed on these compact latent embeddings. This approach is significantly more computationally efficient, enabling faster training and inference.</li> </ol> <h4 id="45-flow-matching-an-alternative-view">4.5. Flow Matching: An Alternative View</h4> <p>Flow Matching has emerged as a conceptually simpler alternative to diffusion models. Instead of defining a forward noising SDE, it directly learns the vector field required to interpolate between a simple noise distribution and the target data distribution. It has been shown to be equivalent to diffusion for certain configurations but is often praised for its implementation simplicity, as it avoids the need for SDEs and score matching.</p> <hr/> <div class="notion-divider">✠</div> <div class="notion-divider">✠</div> <hr/> <h2 id="d-comparisons">D. Comparisons</h2> <h4 id="52-ddpm-vs-sdescore-perspective">5.2. DDPM vs. SDE/Score Perspective</h4> <p><img src="/assets/img/posts/notes-on-diffusion-models/notion-3bc815d2904b80f0a612cd33f6df263c.png" alt="notes-on-diffusion-models"/></p> <table> <thead> <tr> <th><strong>Feature</strong></th> <th><strong>DDPM View (Discrete)</strong></th> <th><strong>SDE/Score View (Continuous)</strong></th> </tr> </thead> <tbody> <tr> <td><strong>Time Domain</strong></td> <td>Operates on a fixed, discrete set of timesteps <code class="language-plaintext highlighter-rouge">t = 1, ..., T</code>.</td> <td>Operates in continuous time <code class="language-plaintext highlighter-rouge">t ∈ [0, T]</code>.</td> </tr> <tr> <td><strong>Training Simplicity</strong></td> <td>Training is very straightforward with discrete schedules and simple loss weighting schemes.</td> <td>Provides a deeper theoretical connection to stochastic calculus and score-based modeling.</td> </tr> <tr> <td><strong>Sampling Flexibility</strong></td> <td>Primarily uses the ancestral sampler, with limited flexibility in step size.</td> <td>Enables the use of a wide variety of advanced SDE and ODE solvers, allowing for trade-offs between speed and quality.</td> </tr> </tbody> </table> <h4 id="53-diffusion-models-vs-vaes">5.3. Diffusion Models vs. VAEs</h4> <p>While diffusion models can be seen as a type of hierarchical VAE, they have several key distinguishing features:</p> <ul> <li><strong>Encoder:</strong> The “encoder” in a diffusion model (the forward noising process) is <strong>fixed</strong> and non-learnable, whereas a VAE learns its encoder.</li> <li><strong>Latent Space:</strong> The latent variables (<code class="language-plaintext highlighter-rouge">x₁, ..., xT</code>) in a diffusion model have the <strong>same dimension</strong> as the input data <code class="language-plaintext highlighter-rouge">x₀</code>.</li> <li><strong>Network:</strong> A single denoising neural network’s parameters are <strong>shared</strong> across all timesteps <code class="language-plaintext highlighter-rouge">t</code>.</li> <li><strong>Prior:</strong> The forward process converges to a simple Normal distribution <strong>by construction</strong>, effectively avoiding the “prior holes” or “posterior collapse” issues that can affect VAEs.</li> </ul> <h4 id="54-the-frequency-domain-perspective">5.4. The Frequency Domain Perspective</h4> <p>The noising and denoising processes can be understood from a frequency perspective:</p> <ul> <li><strong>Forward Process:</strong> When adding noise, <em><strong>high-frequency content (fine details, textures) is destroyed much more quickly</strong></em> than low-frequency content (overall shapes, coarse structure).</li> <li> <p><strong>Reverse Process:</strong> Consequently, the denoising network learns to generate content in a <em><strong>coarse-to-fine manner</strong></em>. At high-noise timesteps (large <code class="language-plaintext highlighter-rouge">t</code>), the model focuses on generating the robust, low-frequency structure of the image. As sampling proceeds to low-noise timesteps (small <code class="language-plaintext highlighter-rouge">t</code>), the model refines this structure by adding high-frequency details.</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>   _ _
   \|...\|
   \|   \|  ____\|   \|____ \|        \|   \|        \| \|____\|   \|____\| nergo
</code></pre></div> </div> </li> </ul> <h2 id="diffusions-applications">Diffusions applications</h2> <p>Reweighted ELBO allows diffusion models to spend more capacity on low-frequency components (meaning more semantical aspects instead of the minute details)</p> <p><img src="/assets/img/posts/notes-on-diffusion-models/notion-3bc815d2904b8089b3cdfe7ae1be5584.png" alt="notes-on-diffusion-models"/></p> <h4 id="latent-diffusion-models">Latent diffusion models</h4> <p>The idea of having latent diffusion models allows the diffusion process to denoise in latent hence focussing on large-scale image structure (the semantics of the image), while having an autoencoder + GAN only generating local details.</p> <h4 id="repa">REPA</h4> <div class="notion-columns"> <div class="notion-column" style="flex: 56 1 0%"> <p>Modern approaches like Diffusion Transformers scale effectively with compute and model size.</p> <p>Representation Alignment (REPA) align the diffusion model’s intermediate features with pretrained perceptual visual encoders. This alignment allows the model to leverage perceptual inductive biases, resulting in training that is up to 17.5 times faster</p> </div> <div class="notion-column" style="flex: 44 1 0%"> <p><img src="/assets/img/posts/notes-on-diffusion-models/notion-3bc815d2904b8014a8cef253fbe42967.png" alt="notes-on-diffusion-models"/></p> </div> </div> <h4 id="generative-learning-trilemma">Generative Learning Trilemma</h4> <p><img src="/assets/img/posts/notes-on-diffusion-models/notion-3bc815d2904b8002b934e942598c87c0.png" alt="notes-on-diffusion-models"/></p> <p>GANs are fast and high quality but suffer mode collapse.</p> <p>VAE are fast and have good mode coverage but low quality.</p> <p>Diffusino models are high quality and have good mode coverage but are very slow.</p> <p>three main approaches to accelerate diffusion:</p> <ol> <li><strong>Fast ODE/SDE Solvers:</strong> Because the sampling process involves iterative steps, advanced solvers (like DPM-Solver++ or Heun’s Method) can approximate the Ordinary Differential Equation (ODE) trajectories more efficiently, reducing the number of steps required (e.g., from hundreds down to 15-20 steps).</li> </ol> <p><img src="/assets/img/posts/notes-on-diffusion-models/notion-3bc815d2904b80f19bfbd085f7dc91f8.png" alt="notes-on-diffusion-models"/></p> <ol> <li><strong>Trajectory Distillation:</strong> This method learns a function that reproduces the multi-step mapping of a teacher model in a single step (e.g., consistency models). While it provides a one-to-one mapping, it is difficult to reduce the process to very few steps.</li> </ol> <p><img src="/assets/img/posts/notes-on-diffusion-models/notion-3bc815d2904b807f957dc38ef0012475.png" alt="notes-on-diffusion-models"/></p> <ol> <li> <p><strong>Variational Distillation:</strong> This approach matches the student model’s distribution to the teacher’s via a variational objective (VDKL). It enables very fast, one-step generation but incurs extra compute costs and risks mode collapse. The core idea of DMD distillation is to minimize the KL divergence between the output distributions of the distilled model and the original model</p> <p><img src="/assets/img/posts/notes-on-diffusion-models/notion-3bc815d2904b80f6b609f758a0af7cad.png" alt="notes-on-diffusion-models"/></p> </li> </ol>]]></content><author><name></name></author><category term="blog"/><category term="tutorial"/><category term="ml"/><summary type="html"><![CDATA[Some notes I took at EPFL on diffusion models from C. Bunne’s course. It is still WIP, some references (e.g., for images) are missing.]]></summary></entry><entry><title type="html">Notes on Transformers</title><link href="https://tommasomncttn.github.io/blog/2026/notes-on-transformers/" rel="alternate" type="text/html" title="Notes on Transformers"/><published>2026-08-14T00:00:00+00:00</published><updated>2026-08-14T00:00:00+00:00</updated><id>https://tommasomncttn.github.io/blog/2026/notes-on-transformers</id><content type="html" xml:base="https://tommasomncttn.github.io/blog/2026/notes-on-transformers/"><![CDATA[<h2 id="0-prerequisite-a-mental-model-for-vector-and-matrix-algebra">0. Prerequisite: A Mental Model for Vector and Matrix Algebra</h2> <h4 id="01-vector-dot-product-utv">0.1 Vector Dot Product \(u^Tv\)</h4> <p><strong>REQUIREMENTS</strong> The dot product between two vector requires that \(u,v\) have the same dimension \(d\).</p> <p>\(u^Tv\) the vector dot product \(u^Tv = &lt;u,v&gt;\) is a linear combination of the entry of \(v\) via the entry of \(u\) as weights. <em>It is just an</em> <em><strong>unscaled similarity mechanism</strong></em> <em>between two vector from the same vector space</em></p> <p><img src="/assets/img/posts/notes-on-transformers/notion-3bc815d2904b8064b794e73a7a85371b.png" alt="notes-on-transformers"/></p> <h4 id="02-matrix-left-vector-multiplication-av">0.2 Matrix Left Vector Multiplication \(Av\)</h4> <p><strong>REQUIREMENTS</strong> The number of columns of \(A\) must match the number of entries of \(v\), that is \(A \in R^{ r \times d}, v \in R^r\).</p> <p>\(Av\) is just a linear combination of the column of A with the entries of v. So, when \(v \in \Delta\) <em>it is an</em> <em><strong>column averaging mechanism</strong></em>.</p> <h4 id="03-matrix-right-vector-multiplication-uta">0.3 Matrix Right Vector Multiplication \(u^TA\)</h4> <p><strong>REQUIREMENTS</strong> The number of rows of \(A\) must match the number of entries of \(u\), that is \(A \in R^{ d \times c}, u \in R^d\)</p> <p>\(u^TA\) is just a linear combination of the rows of A. So, when \(u \in \Delta\) <em>it is an</em> <em><strong>row averaging mechanism</strong></em>.</p> <h4 id="04-matrix-matrix-multiplication-ab">0.4 Matrix Matrix Multiplication \(AB\)</h4> <p>The main mental model is the <strong>rlcr-model</strong> (<em>Red Lions Chase Rabbits</em>), that is <strong>r</strong>ow from <strong>l</strong>eft <strong>c</strong>olumns from <strong>r</strong>ight. This is because both for the requirements and the calculation we will all time care about rows from A and columns from B</p> <div class="notion-divider">&lt; -,|&gt;</div> <p><strong>REQUIREMENTS</strong> The dimension of the rows of A (rl) must match the dimension of the columns of B (cr). As “dimension of row of A = number of columns of A” and “dimensions of columns of B = numbers of rows of B” we have \(A \in R^{ r,d}, B \in R^{d,c} \to C^{r,c}\)</p> <p>To compute think to \(C=AB\) we should start from \(C\):</p> <ul> <li><strong>ENTRY-WISE</strong> \(C_{i,j} = A_{i,.}B_{.,j}\) is a dot product between the \(i^{th}\) row of A and the \(j^{th}\) column of B. So, C is a <em><strong>database of similarities between A rows and C columns</strong></em></li> <li>for the row/columns wise view start from the rlcr to identify if we use a row of A or a column of B and after use the full same set of entities from the other matrix <ul> <li><strong>ROW-WISE</strong> \(C_{i,.} = A_{i,.}B\) is the \(i^{th}\) row of A used in a <code class="language-plaintext highlighter-rouge">Matrix Right Vector</code> multiplication of rows of B. That is, we use the <em><strong>row of A to average the rows of B</strong></em></li> <li><strong>COLUMN-WISE</strong> \(C_{.,j} = A B_{.,j}\) is the \(j^{th}\) column of B used in a <code class="language-plaintext highlighter-rouge">Matrix Left Vector</code> multiplication of columns of A. That is, we use the <em><strong>column of B to average the columns of A</strong></em></li> </ul> </li> </ul> <div class="notion-callout notion-callout--gray"> <div class="notion-callout-icon">💡</div> <div class="notion-callout-body"> <p>For both the special cases we start to identify operations from the non-transposed A and after build the intuition about the transposed \(A^T\)</p> </div> </div> <p><strong>SPECIAL CASE</strong> \(A^TA\) In this case C is a <em><strong>symmetric database of similarities between columns of A</strong></em></p> <ul> <li>Indeed, \(C_{i,j}\) is the \(j^{th}\) column of A dot product with the \(i^{th}\) row of \(A^T\) so the \(i^{th}\) column of A</li> <li>\(C_{.,j} = C_{j,.}\) and it is obtained by Matrix Left Vector Multiplication of \(j^{th}\) column of A with</li> </ul> <hr/> <div class="notion-divider">✠</div> <div class="notion-divider">✠</div> <hr/> <h2 id="1-autoregressive-transformers-from-scratch-n5">1. Autoregressive Transformers From Scratch (N5)</h2> <p>Assume you want a function to generate MNIST-like images of digits. A simple option is to train an <em>autoregressive transformer</em>. The core idea is that we can think as a sequence of <em><strong>patches</strong></em>.</p> <h3 id="11-patches">1.1 Patches</h3> <p>The process of turning images into patches means getting a big image into a sequence of small images</p> <p><img src="/assets/img/posts/notes-on-transformers/notion-3bc815d2904b80fa9f4ccf3c119acc36.png" alt="notes-on-transformers"/></p> <p>The important thing to notice is that <em>a patch is still an image</em>. Nevertheless, transformer are trained to predict a single token-id belonging to a vocabulary not a grid of pixels</p> \[p(x_{1:T}) = p(x_1, x_2, \dots, x_T) = \prod_{t=1}^{T} p\!\left(x_t \mid x_{1:t-1}\right)\] <p><strong>TOKENIZER</strong> Therefore, we need a <strong>tokenizer that map each possible patch to an integer id</strong>. For this reason, we can see that the size of the vocabulary of id \(V\) will grow exponentially with the number of possible value of each pixel and the size of the patch:</p> \[\# V = h \times w \times \text{resolution}\] <p>If we allow a pixel \(x\) to be in \(\{1,...,255\}\) and we use patch of \(2 \times 2\) then we will have \(\#V = 255^4 = 4.2 \times 10^9\).</p> <p><strong>BINARIZATON</strong> For this reason we see that we are forced to binarize the pixel in black and white. Now we have \(\# V = 2^4 = 16\) id.</p> <p><strong>INTEGER-ID</strong> Once they are binarised, we can simply define a tokenizer map</p> \[\tau:\{0,1\}^4 \to \{0,...,15\} \\ \tau(\vec{v}) = \sum_{i &lt; len(\vec{v})} \vec{v}_{i} 2^i\] <p>So for example:</p> \[x_t = [1,0,0,1] \quad \Rightarrow \quad \tau(x_t) = 1\times 2^0 + 0 \times 2^1 + 0 \times 2^2 + 1 \times 2^3 = 9\] <div class="notion-callout notion-callout--gray"> <div class="notion-callout-icon">💡</div> <div class="notion-callout-body"> <p>This process is equivalent to the one of mapping text to tokens (”cat”→ “c”, “at” ) and the token to integer-id “c”→ 456, “at” → 19231</p> </div> </div> <hr/> <div class="notion-divider">✠</div> <h3 id="12-transformer-block">1.2 Transformer-Block</h3> <h4 id="121-forward-pass-prepost">1.2.1 Forward Pass (Pre/Post)</h4> <p><strong>READ AND WRITE ON RESIDUALS</strong> Information is read and written from the residual stream by <code class="language-plaintext highlighter-rouge">Attention Layers</code> and <code class="language-plaintext highlighter-rouge">MLP</code>s:</p> \[X \in R^{ \; t \times d}\] <p>Where \(t\) is the number of tokens and \(d\) is the hidden dimension. Therefore, each row is an vector representation of a token.</p> <p><img src="/assets/img/posts/notes-on-transformers/notion-3bc815d2904b80c09dc8c8a0942a54df.png" alt="notes-on-transformers"/></p> <p><strong>POST-LAYER-NORM ARCHITECTURE</strong> Information is <em><strong>written normalised</strong></em> on the residual stream. Pass:</p> \[X' = \text{LayerNorm}(X + \text{MHA}(X)) \\ Y = \text{LayerNorm}(X' + \text{MLP}(X'))\] <p><strong>PRE-LAYER-NORM ARCHITECTURE</strong> Information is <em><strong>read normalised</strong></em> on the residual stream. Pass:</p> \[X' = X + \text{MHA}(\text{LayerNorm}(X)) \\ Y = X' + \text{MLP}(\text{LayerNorm}(X'))\] <hr/> <div class="notion-divider">✠</div> <h4 id="122-multi-head-attention">1.2.2 Multi-Head Attention</h4> <p><strong>FLOW OF COMPUTATION</strong> The flow of the attention mechanism is the following:</p> <ol> <li> <p>linear projection of input \(X \in R^{t \times d}\) in \(Q,K,V \in R^{t \times d}\). Mathematicaly: \(Q = X W_Q, \quad K = X W_K, \quad V = X W_V\)</p> <p>Instead, it can be done programatically by:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code> <span class="c1"># inside the init of the class
</span> <span class="n">self</span><span class="p">.</span><span class="n">w_Q</span> <span class="o">=</span> <span class="n">nn</span><span class="p">.</span><span class="nc">Linear</span><span class="p">(</span><span class="n">model_dim</span><span class="p">,</span> <span class="n">model_dim</span><span class="p">,</span> <span class="n">bias</span> <span class="o">=</span> <span class="bp">False</span><span class="p">)</span>
 <span class="n">self</span><span class="p">.</span><span class="n">w_K</span> <span class="o">=</span> <span class="n">nn</span><span class="p">.</span><span class="nc">Linear</span><span class="p">(</span><span class="n">model_dim</span><span class="p">,</span> <span class="n">model_dim</span><span class="p">,</span> <span class="n">bias</span> <span class="o">=</span> <span class="bp">False</span><span class="p">)</span>
 <span class="n">self</span><span class="p">.</span><span class="n">w_V</span> <span class="o">=</span> <span class="n">nn</span><span class="p">.</span><span class="nc">Linear</span><span class="p">(</span><span class="n">model_dim</span><span class="p">,</span> <span class="n">model_dim</span><span class="p">,</span> <span class="n">bias</span> <span class="o">=</span> <span class="bp">False</span><span class="p">)</span>

 <span class="c1"># inside the forward: b x t x d -&gt; b x t x d
</span> <span class="n">q</span> <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="nf">w_Q</span><span class="p">(</span><span class="n">x</span><span class="p">)</span>
 <span class="n">k</span> <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="nf">w_K</span><span class="p">(</span><span class="n">x</span><span class="p">)</span>
 <span class="n">v</span> <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="nf">w_V</span><span class="p">(</span><span class="n">x</span><span class="p">)</span>
</code></pre></div> </div> </li> <li>creation of \(n\) attention head of dimension \(h = d // n\) such that \(Q_i, K_i, V_i \in R^{t \times h}\). Programtically this is done via <code class="language-plaintext highlighter-rouge">einops.rearrange</code> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code> <span class="c1"># inside the forward: b x t x d -&gt; # b x n x t x h
</span> <span class="n">k</span> <span class="o">=</span> <span class="nf">rearrange</span><span class="p">(</span><span class="n">k</span><span class="p">,</span> <span class="sh">"</span><span class="s">b t (n h) -&gt; b n t h</span><span class="sh">"</span><span class="p">,</span> <span class="n">n</span> <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="n">n</span><span class="p">,</span> <span class="n">h</span> <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="n">h</span><span class="p">)</span> 
 <span class="n">q</span> <span class="o">=</span> <span class="nf">rearrange</span><span class="p">(</span><span class="n">q</span><span class="p">,</span> <span class="sh">"</span><span class="s">b t (n h) -&gt; b n t h</span><span class="sh">"</span><span class="p">,</span> <span class="n">n</span> <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="n">n</span><span class="p">,</span> <span class="n">h</span> <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="n">h</span><span class="p">)</span> 
 <span class="n">v</span> <span class="o">=</span> <span class="nf">rearrange</span><span class="p">(</span><span class="n">v</span><span class="p">,</span> <span class="sh">"</span><span class="s">b t (n h) -&gt; b n t h</span><span class="sh">"</span><span class="p">,</span> <span class="n">n</span> <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="n">n</span><span class="p">,</span> <span class="n">h</span> <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="n">h</span><span class="p">)</span> 
 <span class="c1"># alternatively: k.view(b, t, n, h).permute(0, 3, 1, 2)
</span></code></pre></div> </div> </li> <li>creation of row similarity database between rows of \(Q_i\) and \(K_i\) → \(Q_iK_i^T\). Program: <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code> <span class="c1"># compute similarity scores: b x n x t x h -&gt; b x n x t x t
</span> <span class="n">s</span> <span class="o">=</span> <span class="nf">einsum</span><span class="p">(</span><span class="n">q</span><span class="p">,</span><span class="n">k</span><span class="p">,</span> <span class="sh">"</span><span class="s">b n t1 d, b n t2 d -&gt; b n t1 t2</span><span class="sh">"</span><span class="p">)</span>

 <span class="c1"># alternatively: q @ k.pemute(0,1,3,2)
</span></code></pre></div> </div> </li> <li> <p><em><strong>normalize</strong></em>, apply <em><strong>masking</strong></em> and turn to <em><strong>probability</strong></em> row via <code class="language-plaintext highlighter-rouge">softmax</code> $$ S_i = \text{softmax}!\left(\frac{Q_iK_i^\top}{\sqrt{d_h}} + \text{mask}\right)</p> <p>$$</p> <p>To implement this:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code> <span class="c1"># apply the normalization
</span> <span class="n">s</span> <span class="o">=</span> <span class="n">s</span> <span class="o">/</span> <span class="n">np</span><span class="p">.</span><span class="nf">sqrt</span><span class="p">(</span><span class="n">self</span><span class="p">.</span><span class="n">h</span><span class="p">)</span>
 <span class="c1"># apply the mask 
</span> <span class="n">s</span> <span class="o">=</span> <span class="n">s</span> <span class="o">+</span> <span class="n">mask</span>
 <span class="c1"># apply softmax
</span> <span class="n">s</span> <span class="o">=</span> <span class="n">F</span><span class="p">.</span><span class="nf">softmax</span><span class="p">(</span><span class="n">s</span><span class="p">,</span> <span class="n">dim</span> <span class="o">=</span> <span class="o">-</span><span class="mi">1</span><span class="p">)</span> 
</code></pre></div> </div> </li> <li>get new matrix as with \(t\) rows (one for token) where each row is the convex combination of the row of \(V\) via the probability distribution obtained before \(S_iV_i\) <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code> <span class="c1"># b x n x t x h
</span> <span class="n">weighted_mean</span> <span class="o">=</span> <span class="nf">einsum</span><span class="p">(</span><span class="n">s</span><span class="p">,</span><span class="n">v</span><span class="p">,</span> <span class="sh">"</span><span class="s">b n t1 t2, b n t2 h -&gt; b n t1 h</span><span class="sh">"</span><span class="p">)</span> 

 <span class="c1"># alternatively: s @ v
</span></code></pre></div> </div> </li> <li>Concatenate the results and project it back with <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code> <span class="n">weighted_mean</span> <span class="o">=</span> <span class="nf">rearrange</span><span class="p">(</span><span class="n">weighted_mean</span><span class="p">,</span> <span class="sh">"</span><span class="s"> b n t h -&gt; b t (n h)</span><span class="sh">"</span><span class="p">)</span>
 <span class="n">mha</span> <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="nf">w_O</span><span class="p">(</span><span class="n">weighted_mean</span><span class="p">)</span> 
 <span class="c1"># equivalently weighted_mean.permute(0,2,1).view(b,n, -1)
</span></code></pre></div> </div> </li> </ol> <p><strong>FORMULA ATTENTION</strong> This brings to the following formula for general attention:</p> \[\text{Attention}(Q,K,V) = \text{softmax}\!\left(\frac{QK^\top}{\sqrt{d_h}} + \text{mask}\right)V\] <p><strong>FORMULA MHA</strong> And to its generalization to multihead attention</p> \[\text{MHA}(X) = \bigoplus_{h=1}^H \text{Attention}(Q_h, K_h, V_h) \, W_O\] <hr/> <div class="notion-divider">✠</div> <h4 id="123-layer-norm">1.2.3 Layer Norm</h4> <p>The block computes variance and expectation of \(X \in R^{t \times d}\) along d, so each token as its own information and is not used for other like in batch-norm:</p> \[y = \frac{x - \mathbb{E}[x]}{\sqrt{\mathrm{Var}[x] + \epsilon}} \cdot \gamma + \beta\] <p>Visually they compare like this:</p> <p><img src="/assets/img/posts/notes-on-transformers/notion-3bc815d2904b80e18658e121c2c307a4.png" alt="notes-on-transformers"/></p> <p>Code-wise just init two parameter for bias and scaling and then compute the unbiased var and the mean</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">class</span> <span class="nc">LayerNorm</span><span class="p">(</span><span class="n">nn</span><span class="p">.</span><span class="n">Module</span><span class="p">):</span>

    <span class="k">def</span> <span class="nf">__init__</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">dim</span><span class="p">:</span> <span class="nb">int</span><span class="p">,</span> <span class="n">eps</span> <span class="o">=</span> <span class="mf">1e-05</span><span class="p">,</span> <span class="n">bias</span> <span class="o">=</span> <span class="bp">True</span><span class="p">):</span>

        <span class="nf">super</span><span class="p">().</span><span class="nf">__init__</span><span class="p">()</span>
        <span class="c1"># storing the dimension
</span>        <span class="n">self</span><span class="p">.</span><span class="n">dim</span> <span class="o">=</span> <span class="n">dim</span>
        <span class="n">self</span><span class="p">.</span><span class="n">eps</span> <span class="o">=</span> <span class="n">eps</span>

        <span class="c1"># creating two parameter
</span>        <span class="n">self</span><span class="p">.</span><span class="n">gamma</span> <span class="o">=</span> <span class="n">nn</span><span class="p">.</span><span class="nc">Parameter</span><span class="p">(</span><span class="n">torch</span><span class="p">.</span><span class="nf">ones</span><span class="p">(</span><span class="n">dim</span><span class="p">))</span>
        <span class="n">self</span><span class="p">.</span><span class="n">bias</span> <span class="o">=</span> <span class="n">nn</span><span class="p">.</span><span class="nc">Parameter</span><span class="p">(</span><span class="n">torch</span><span class="p">.</span><span class="nf">zeros</span><span class="p">(</span><span class="n">dim</span><span class="p">))</span>
    
    <span class="k">def</span> <span class="nf">forward</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">x</span><span class="p">):</span>

        <span class="c1"># aggregates
</span>        <span class="n">mean</span> <span class="o">=</span> <span class="n">x</span><span class="p">.</span><span class="nf">mean</span><span class="p">(</span><span class="n">dim</span> <span class="o">=</span> <span class="o">-</span><span class="mi">1</span><span class="p">,</span> <span class="n">keepdim</span> <span class="o">=</span> <span class="bp">True</span><span class="p">)</span>
        <span class="n">var</span> <span class="o">=</span> <span class="n">x</span><span class="p">.</span><span class="nf">var</span><span class="p">(</span><span class="n">dim</span> <span class="o">=</span> <span class="o">-</span><span class="mi">1</span><span class="p">,</span> <span class="n">keepdim</span> <span class="o">=</span> <span class="bp">True</span><span class="p">,</span> <span class="n">unbiased</span><span class="o">=</span><span class="bp">False</span><span class="p">)</span>
        <span class="c1"># scale
</span>        <span class="n">x</span> <span class="o">=</span> <span class="p">(</span><span class="n">x</span><span class="o">-</span><span class="n">mean</span><span class="p">)</span> <span class="o">/</span> <span class="n">torch</span><span class="p">.</span><span class="nf">sqrt</span><span class="p">(</span><span class="n">var</span><span class="o">+</span><span class="n">self</span><span class="p">.</span><span class="n">eps</span><span class="p">)</span>
        <span class="c1"># apply parameter
</span>        <span class="k">return</span> <span class="n">x</span><span class="o">*</span><span class="n">self</span><span class="p">.</span><span class="n">gamma</span> <span class="o">+</span> <span class="n">self</span><span class="p">.</span><span class="n">bias</span>
</code></pre></div></div> <hr/> <div class="notion-divider">✠</div> <h4 id="124-mlp">1.2.4 MLP</h4> <p>Applies two linear layers with an activation in between:</p> \[\text{MLP}(x) = W_2 \,\sigma(W_1 x + b_1) + b_2\] <p>It uses an higher dimensional projection dimension (so W₁ has more rows than column)</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">class</span> <span class="nc">MLP</span><span class="p">(</span><span class="n">nn</span><span class="p">.</span><span class="n">Module</span><span class="p">):</span>

    <span class="k">def</span> <span class="nf">__init__</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">input_dim</span><span class="p">,</span> <span class="n">hidden_dim</span><span class="p">,</span> <span class="n">output_dim</span><span class="p">):</span>
        <span class="sh">"""</span><span class="s">
        input_dim: dimension of input features
        hidden_dim: dimension of hidden layer
        output_dim: dimension of output features
        </span><span class="sh">"""</span>
        <span class="nf">super</span><span class="p">().</span><span class="nf">__init__</span><span class="p">()</span>
        <span class="n">self</span><span class="p">.</span><span class="n">linear1</span> <span class="o">=</span> <span class="n">nn</span><span class="p">.</span><span class="nc">Linear</span><span class="p">(</span><span class="n">input_dim</span><span class="p">,</span> <span class="n">hidden_dim</span><span class="p">)</span> <span class="c1"># bias default true
</span>        <span class="n">self</span><span class="p">.</span><span class="n">activation</span> <span class="o">=</span> <span class="n">nn</span><span class="p">.</span><span class="nc">GELU</span><span class="p">()</span>
        <span class="n">self</span><span class="p">.</span><span class="n">linear2</span> <span class="o">=</span> <span class="n">nn</span><span class="p">.</span><span class="nc">Linear</span><span class="p">(</span><span class="n">hidden_dim</span><span class="p">,</span> <span class="n">output_dim</span><span class="p">)</span> <span class="c1"># bias default true
</span>

    <span class="k">def</span> <span class="nf">forward</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">x</span><span class="p">):</span>
        <span class="sh">"""</span><span class="s">
        x: input tensor of shape (..., input_dim)
        returns: output tensor of shape (..., output_dim)
        </span><span class="sh">"""</span>
        <span class="n">x</span> <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="nf">linear1</span><span class="p">(</span><span class="n">x</span><span class="p">)</span>
        <span class="n">x</span> <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="nf">activation</span><span class="p">(</span><span class="n">x</span><span class="p">)</span>
        <span class="n">x</span> <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="nf">linear2</span><span class="p">(</span><span class="n">x</span><span class="p">)</span>

        <span class="k">return</span> <span class="n">x</span> 
</code></pre></div></div> <hr/> <div class="notion-divider">✠</div> <h3 id="13-autoregressive-transformer">1.3 Autoregressive Transformer</h3> <h4 id="131-forward-pass">1.3.1 Forward Pass</h4> <p>A visualization of the forward pass is <a href="/2d0815d2904b806383f5c805051179fe#2d6815d2904b80eeabe6f637b3d6a468">here</a></p> <p>Otherwise, check it here:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># in the init 
</span><span class="n">self</span><span class="p">.</span><span class="n">blocks</span> <span class="o">=</span> <span class="n">nn</span><span class="p">.</span><span class="nc">ModuleList</span><span class="p">([</span> <span class="nc">TransformerBlock</span><span class="p">(</span><span class="n">self</span><span class="p">.</span><span class="n">d_model</span><span class="p">,</span> <span class="n">self</span><span class="p">.</span><span class="n">n_heads</span><span class="p">,</span> <span class="n">self</span><span class="p">.</span><span class="n">d_ff</span><span class="p">,</span> <span class="bp">False</span><span class="p">)</span> <span class="k">for</span> <span class="n">_</span> <span class="ow">in</span> <span class="nf">range</span><span class="p">(</span><span class="n">self</span><span class="p">.</span><span class="n">n_layers</span><span class="p">)])</span>
<span class="c1"># forward
</span><span class="k">def</span> <span class="nf">forward</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">x</span><span class="p">):</span>
		<span class="c1"># positional and embedding
</span>    <span class="n">x</span> <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="nf">pos_embedding</span><span class="p">(</span><span class="n">x</span><span class="p">)</span> <span class="o">+</span> <span class="n">self</span><span class="p">.</span><span class="nf">embedding</span><span class="p">(</span><span class="n">x</span><span class="p">)</span>
    <span class="c1"># forward pass 
</span>    <span class="k">for</span> <span class="n">layer</span> <span class="ow">in</span> <span class="n">self</span><span class="p">.</span><span class="n">blocks</span><span class="p">:</span>
        <span class="n">x</span> <span class="o">=</span> <span class="nf">layer</span><span class="p">(</span><span class="n">x</span><span class="p">,</span> <span class="n">self_mask</span> <span class="o">=</span> <span class="n">mask</span><span class="p">)</span>
    <span class="c1"># unembedding
</span>    <span class="k">return</span> <span class="n">self</span><span class="p">.</span><span class="nf">projection</span><span class="p">(</span><span class="n">self</span><span class="p">.</span><span class="nf">norm</span><span class="p">(</span><span class="n">x</span><span class="p">))</span>
</code></pre></div></div> <div class="notion-callout notion-callout--gray"> <div class="notion-callout-icon">💡</div> <div class="notion-callout-body"> <p>NB we <strong>normalize</strong> the output before projecting it on the vocabulary</p> </div> </div> <hr/> <div class="notion-divider">✠</div> <h4 id="132-token-embeddings">1.3.2 Token Embeddings</h4> <p>Transforming the discrete tokens (<em>integers id</em>) into a sequence of learnable vector representations. Luckily, <code class="language-plaintext highlighter-rouge">nn.Embeddings</code> is already implemented:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">self</span><span class="p">.</span><span class="n">token_embedding</span> <span class="o">=</span> <span class="n">nn</span><span class="p">.</span><span class="nc">Embedding</span><span class="p">(</span><span class="n">vocab_size</span><span class="p">,</span> <span class="n">model_dim</span><span class="p">)</span>
</code></pre></div></div> <hr/> <div class="notion-divider">✠</div> <h4 id="133-positional-embeddings">1.3.3 Positional Embeddings</h4> <p>For these you can use, for instance, learnable positional embeddings. You should support a maximal sequence length of <code class="language-plaintext highlighter-rouge">max_seq_length</code>. They should be added to the embedded tokens.</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># in the init
</span><span class="n">self</span><span class="p">.</span><span class="n">pos_embedding</span> <span class="o">=</span> <span class="n">nn</span><span class="p">.</span><span class="nc">Embedding</span><span class="p">(</span><span class="n">max_seq_length</span><span class="p">,</span> <span class="n">model_dim</span><span class="p">)</span>

<span class="c1"># to use it we must extract the relative position 
</span><span class="n">batch_size</span><span class="p">,</span> <span class="n">seq_length</span> <span class="o">=</span> <span class="n">x</span><span class="p">.</span><span class="n">shape</span>
<span class="n">positions</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">arange</span><span class="p">(</span><span class="mi">0</span><span class="p">,</span> <span class="n">seq_length</span><span class="p">).</span><span class="nf">unsqueeze</span><span class="p">(</span><span class="mi">0</span><span class="p">).</span><span class="nf">expand</span><span class="p">(</span><span class="n">batch_size</span><span class="p">,</span> <span class="n">seq_length</span><span class="p">)</span>
</code></pre></div></div> <details class="notion-toggle"> <summary>Key tool is <code class="language-plaintext highlighter-rouge">tensor.expand(size)</code> which allow to turn a single (unsqueezed) dimension into more dimension </summary> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">x</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">tensor</span><span class="p">([[</span><span class="mi">1</span><span class="p">],</span> <span class="p">[</span><span class="mi">2</span><span class="p">],</span> <span class="p">[</span><span class="mi">3</span><span class="p">]])</span>
<span class="n">x</span><span class="p">.</span><span class="nf">size</span><span class="p">()</span>
<span class="n">torch</span><span class="p">.</span><span class="nc">Size</span><span class="p">([</span><span class="mi">3</span><span class="p">,</span> <span class="mi">1</span><span class="p">])</span>
<span class="n">x</span><span class="p">.</span><span class="nf">expand</span><span class="p">(</span><span class="mi">3</span><span class="p">,</span> <span class="mi">4</span><span class="p">)</span>
<span class="nf">tensor</span><span class="p">([[</span> <span class="mi">1</span><span class="p">,</span>  <span class="mi">1</span><span class="p">,</span>  <span class="mi">1</span><span class="p">,</span>  <span class="mi">1</span><span class="p">],</span>
        <span class="p">[</span> <span class="mi">2</span><span class="p">,</span>  <span class="mi">2</span><span class="p">,</span>  <span class="mi">2</span><span class="p">,</span>  <span class="mi">2</span><span class="p">],</span>
        <span class="p">[</span> <span class="mi">3</span><span class="p">,</span>  <span class="mi">3</span><span class="p">,</span>  <span class="mi">3</span><span class="p">,</span>  <span class="mi">3</span><span class="p">]])</span>
<span class="n">x</span><span class="p">.</span><span class="nf">expand</span><span class="p">(</span><span class="o">-</span><span class="mi">1</span><span class="p">,</span> <span class="mi">4</span><span class="p">)</span>   <span class="c1"># -1 means not changing the size of that dimension
</span><span class="nf">tensor</span><span class="p">([[</span> <span class="mi">1</span><span class="p">,</span>  <span class="mi">1</span><span class="p">,</span>  <span class="mi">1</span><span class="p">,</span>  <span class="mi">1</span><span class="p">],</span>
        <span class="p">[</span> <span class="mi">2</span><span class="p">,</span>  <span class="mi">2</span><span class="p">,</span>  <span class="mi">2</span><span class="p">,</span>  <span class="mi">2</span><span class="p">],</span>
        <span class="p">[</span> <span class="mi">3</span><span class="p">,</span>  <span class="mi">3</span><span class="p">,</span>  <span class="mi">3</span><span class="p">,</span>  <span class="mi">3</span><span class="p">]])</span>
</code></pre></div> </div> </details> <hr/> <div class="notion-divider">✠</div> <h4 id="134-prediction-head">1.3.4 Prediction Head</h4> <p>As easy as it can be:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">self</span><span class="p">.</span><span class="n">projection</span> <span class="o">=</span> <span class="n">nn</span><span class="p">.</span><span class="nc">Linear</span><span class="p">(</span><span class="n">model_dim</span><span class="p">,</span> <span class="n">vocab_size</span><span class="p">,</span> <span class="n">bias</span><span class="o">=</span><span class="bp">False</span><span class="p">)</span>
</code></pre></div></div> <hr/> <div class="notion-divider">✠</div> <h4 id="135-sampling">1.3.5 Sampling</h4> <p>Mathematically, is simple: we have a multinomial distribution that assign to each index a given probability and that sums to 1. We must sample. The only trick is to use a <code class="language-plaintext highlighter-rouge">temperature parameter</code>. For more details see this chat with <a href="/2d0815d2904b806383f5c805051179fe">gemini</a>:</p> <ul> <li><em>temperature to 1</em> → nothing changes</li> <li><em>temperature &gt; 1</em> (<code class="language-plaintext highlighter-rouge">smoothing</code>)→ shrink everything near 0 (big positive → small positive, big negative → small negative) so probability becomes more mild (convergence to uniform)</li> <li><em>temperature&lt;1</em> (<code class="language-plaintext highlighter-rouge">sharpening</code>)→ makes difference bigger, decreasing the entropy (diverge from uniform) Programatically, we just need to sample with torch.multinomial</li> </ul> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nd">@torch.no_grad</span><span class="p">()</span>
  <span class="k">def</span> <span class="nf">sample_next_token</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">x</span><span class="p">,</span> <span class="n">temperature</span><span class="o">=</span><span class="mf">1.0</span><span class="p">):</span>
      <span class="sh">"""</span><span class="s">
      x: input tensor of shape (batch_size, seq_length)
      returns: output tensor containing logits of shape (batch_size,)
      </span><span class="sh">"""</span>
      <span class="n">logits</span> <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="nf">forward</span><span class="p">(</span><span class="n">x</span><span class="p">)</span> <span class="c1"># (batch_size, seq_length, vocab_size)
</span>      <span class="n">probs</span> <span class="o">=</span> <span class="n">F</span><span class="p">.</span><span class="nf">softmax</span><span class="p">(</span><span class="n">logits</span><span class="p">[:,</span> <span class="o">-</span><span class="mi">1</span><span class="p">,</span> <span class="p">:]</span><span class="o">/</span><span class="n">temperature</span><span class="p">,</span> <span class="n">dim</span><span class="o">=-</span><span class="mi">1</span><span class="p">)</span>
      <span class="n">next_token</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">multinomial</span><span class="p">(</span><span class="n">probs</span><span class="p">,</span> <span class="n">num_samples</span><span class="o">=</span><span class="mi">1</span><span class="p">)[:,</span><span class="mi">0</span><span class="p">]</span> <span class="c1"># clean extradimension
</span>      <span class="k">return</span> <span class="n">next_token</span>
</code></pre></div></div> <details class="notion-toggle"> <summary>It relies on <code class="language-plaintext highlighter-rouge">torch.multinomial</code> which takes as input a tensor (it normalize if it is not in the simplex) and sample num_samples from it</summary> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">weights</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">tensor</span><span class="p">([</span><span class="mi">0</span><span class="p">,</span> <span class="mi">10</span><span class="p">,</span> <span class="mi">3</span><span class="p">,</span> <span class="mi">0</span><span class="p">],</span> <span class="n">dtype</span><span class="o">=</span><span class="n">torch</span><span class="p">.</span><span class="nb">float</span><span class="p">)</span> <span class="c1"># create a teso
</span><span class="n">r</span> <span class="n">of</span> <span class="n">weights</span>
<span class="n">torch</span><span class="p">.</span><span class="nf">multinomial</span><span class="p">(</span><span class="n">weights</span><span class="p">,</span> <span class="mi">2</span><span class="p">)</span>
<span class="nf">tensor</span><span class="p">([</span><span class="mi">1</span><span class="p">,</span> <span class="mi">2</span><span class="p">])</span>
<span class="n">torch</span><span class="p">.</span><span class="nf">multinomial</span><span class="p">(</span><span class="n">weights</span><span class="p">,</span> <span class="mi">5</span><span class="p">)</span> <span class="c1"># ERROR!
</span><span class="nb">RuntimeError</span><span class="p">:</span> <span class="n">cannot</span> <span class="n">sample</span> <span class="n">n_sample</span> <span class="o">&gt;</span> <span class="n">prob_dist</span><span class="p">.</span><span class="nf">size</span><span class="p">(</span><span class="o">-</span><span class="mi">1</span><span class="p">)</span> <span class="n">samples</span> <span class="n">without</span> <span class="n">replacement</span>
<span class="n">torch</span><span class="p">.</span><span class="nf">multinomial</span><span class="p">(</span><span class="n">weights</span><span class="p">,</span> <span class="mi">4</span><span class="p">,</span> <span class="n">replacement</span><span class="o">=</span><span class="bp">True</span><span class="p">)</span>
<span class="nf">tensor</span><span class="p">([</span> <span class="mi">2</span><span class="p">,</span>  <span class="mi">1</span><span class="p">,</span>  <span class="mi">1</span><span class="p">,</span>  <span class="mi">1</span><span class="p">])</span>
</code></pre></div> </div> </details> <hr/> <div class="notion-divider">✠</div> <h2 id="2-t5-gpt-and-bert-n6-e6">2. T5, GPT, and Bert (N6-E6)</h2> <p>Transformer-based architectures have been the foundation of many popular models, such as BERT <strong>(encoder-only)</strong>, T5 <strong>(encoder-decoder)</strong>, and GPT <strong>(decoder-only)</strong>. In this section we will compare them.</p> <h3 id="21-forward-passes">2.1 Forward Passes</h3> <h4 id="211-common-transformer-block">2.1.1 Common Transformer Block</h4> <p>The base block of the three architecture is the same. Let′s work with a pre-layer block (so read information normalised).</p> <ul> <li><strong>ENC VS DEC</strong> Between encoder and decoder the only difference is the mask we pass for the self attention.</li> <li><strong>ENC + DEC</strong> In this case we simply add one step to the forward pass where we read the <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">forward</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">x</span><span class="p">,</span> <span class="n">mask</span><span class="o">=</span><span class="bp">None</span><span class="p">,</span> <span class="n">encoder_output</span><span class="o">=</span><span class="bp">None</span><span class="p">,</span> <span class="n">cross_mask</span><span class="o">=</span><span class="bp">None</span><span class="p">):</span>
      <span class="sh">"""</span><span class="s">
      x: input.
      mask: mask for encoder-only or decoder-only structures.
      encoder_output: used in T5, the source of K and V for cross-attention.
      cross_mask: used in T5, the mask for cross-attention.
      </span><span class="sh">"""</span>
      <span class="n">x</span> <span class="o">=</span> <span class="n">x</span> <span class="o">+</span> <span class="n">self</span><span class="p">.</span><span class="nf">self_attn</span><span class="p">(</span><span class="n">self</span><span class="p">.</span><span class="nf">norm1</span><span class="p">(</span><span class="n">x</span><span class="p">),</span><span class="n">self</span><span class="p">.</span><span class="nf">norm1</span><span class="p">(</span><span class="n">x</span><span class="p">),</span> <span class="n">mask</span><span class="p">)</span>
      <span class="c1"># ONLY FOR T5
</span>      <span class="k">if</span> <span class="n">self</span><span class="p">.</span><span class="n">cross</span> <span class="ow">and</span> <span class="n">encoder_output</span> <span class="ow">is</span> <span class="ow">not</span> <span class="bp">None</span><span class="p">:</span>
          <span class="n">x</span> <span class="o">=</span> <span class="n">x</span> <span class="o">+</span> <span class="n">self</span><span class="p">.</span><span class="nf">self_attn_cross</span><span class="p">(</span><span class="n">self</span><span class="p">.</span><span class="nf">norm_cross</span><span class="p">(</span><span class="n">x</span><span class="p">),</span> <span class="n">encoder_output</span><span class="p">,</span> <span class="n">cross_mask</span><span class="p">)</span>
      <span class="n">x</span> <span class="o">=</span> <span class="n">x</span> <span class="o">+</span> <span class="n">self</span><span class="p">.</span><span class="nf">ffn</span><span class="p">(</span><span class="n">self</span><span class="p">.</span><span class="nf">norm2</span><span class="p">(</span><span class="n">x</span><span class="p">))</span>
      <span class="k">return</span> <span class="n">x</span> 
</code></pre></div> </div> </li> </ul> <h4 id="212-different-transformer-passes">2.1.2 Different Transformer Passes</h4> <p>While the base block is the same there are some difference in the forward pass of the transformer.</p> <p><strong>BERT and GPT</strong> are as simple as:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nf">embedding</span><span class="p">(</span><span class="n">x</span><span class="p">)</span><span class="o">+</span><span class="nf">pos</span><span class="p">(</span><span class="n">x</span><span class="p">)</span><span class="o">-&gt;</span> <span class="nf">block</span><span class="p">(</span><span class="n">x</span><span class="p">)</span> <span class="n">n</span> <span class="n">times</span> <span class="o">-&gt;</span> <span class="n">normalized</span> <span class="n">projection</span><span class="p">.</span>
</code></pre></div></div> <p>Programatically they are:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">forward</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">x</span><span class="p">):</span>
        <span class="c1"># compute mask
</span>        <span class="n">mask</span> <span class="o">=</span> <span class="nf">causal_mask</span><span class="p">(</span><span class="n">X</span><span class="p">)</span> <span class="o">&amp;</span> <span class="nf">pad_mask</span><span class="p">(</span><span class="n">x</span><span class="p">)</span>
        <span class="c1"># embedd
</span>        <span class="n">x</span> <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="nf">pos_embedding</span><span class="p">(</span><span class="n">x</span><span class="p">)</span> <span class="o">+</span> <span class="n">self</span><span class="p">.</span><span class="nf">embedding</span><span class="p">(</span><span class="n">x</span><span class="p">)</span>
        <span class="c1"># forward pass 
</span>        <span class="k">for</span> <span class="n">layer</span> <span class="ow">in</span> <span class="n">self</span><span class="p">.</span><span class="n">blocks</span><span class="p">:</span>
            <span class="n">x</span> <span class="o">=</span> <span class="nf">layer</span><span class="p">(</span><span class="n">x</span><span class="p">,</span> <span class="n">mask</span><span class="p">,</span> <span class="bp">None</span><span class="p">,</span> <span class="bp">None</span><span class="p">)</span>
        <span class="c1"># unembedding
</span>        <span class="k">return</span> <span class="n">self</span><span class="p">.</span><span class="nf">projection</span><span class="p">(</span><span class="n">self</span><span class="p">.</span><span class="nf">norm</span><span class="p">(</span><span class="n">x</span><span class="p">))</span>
</code></pre></div></div> <p><strong>T5</strong> Instead is a bit more convoluted:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">forward</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">x</span><span class="p">,</span> <span class="n">y</span><span class="p">):</span>
				<span class="c1"># encode it, compute output and mask for cross-attention
</span>        <span class="n">encoder_output</span><span class="p">,</span> <span class="n">cross_mask</span> <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="nf">encode</span><span class="p">(</span><span class="n">x</span><span class="p">)</span>
        <span class="c1"># decode it 
</span>        <span class="n">output</span> <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="nf">decode</span><span class="p">(</span><span class="n">y</span><span class="p">,</span> <span class="n">encoder_output</span><span class="p">,</span> <span class="n">cross_mask</span><span class="p">)</span>
        <span class="k">return</span> <span class="n">output</span>
</code></pre></div></div> <p>While the encoder is the same as the BERT, the decoder will need cross-attention so is a bit different:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># INSIDE DECODER FORWARD 
</span><span class="k">for</span> <span class="n">layer</span> <span class="ow">in</span> <span class="n">self</span><span class="p">.</span><span class="n">blocks</span><span class="p">:</span>
	<span class="n">x</span> <span class="o">=</span> <span class="nf">layer</span><span class="p">(</span><span class="n">x</span><span class="p">,</span> <span class="n">mask</span><span class="p">,</span> <span class="n">encoder_output</span><span class="p">,</span> <span class="n">cross_mask</span><span class="p">)</span>
</code></pre></div></div> <h3 id="22-attention">2.2 Attention</h3> <h4 id="221-core-ideas-e61">2.2.1 Core Ideas (E6.1)</h4> <p><strong>BERT</strong> each layer of the encoder use attention with both \(Q,K,V\) ← \(X_{enc}\) read from the residual stream of the encoder</p> <ul> <li> <p><strong>Masking</strong> is the <em>padding mask</em> for bidirectional attention mechanisms <strong>GPT</strong> each layer of the decoder use attention with both \(Q,K,V\) ← \(X_{dec}\) read from the residual stream of the decoder</p> </li> <li> <p><strong>Masking</strong> is the <em>causal masking</em> so that token at position \(t\) from \(V\) is obtained by attending information from \(Q,K\) only from \(\leq t\) <strong>T5</strong> there are three type of attention:</p> </li> <li><em>encoder:</em> the encoder has simply BERT encoder attention with pad-masking and \(Q,K,V\) ← \(X_{enc}\)</li> <li><em>decoder:</em> the decoder has two type of attention: <ul> <li>decoder has a first attention that is GPT like</li> <li>there is another type of attention at each transformer block, added after the first attention, that is <em><strong>cross-attention.</strong></em> <ul> <li>takes as Q←\(X_{dec}\), and \(K,\)V ← \(X_{enc}\).</li> <li>NB: At each transformer-block of the decoder, while \(X_{dec}\) is updated as we proceed along this residual stream, the global information outputed by the encoder \(X_{enc}\) remain the same.</li> <li><em>cross-attention</em> compute \(Q_{dec}K_{enc}^T\) which means that a row of this matrix, let′s say the third, is how similar is the third of row of \(Q_{dec}\), so decoder third token hidden representation, to each row of \(K_{enc}\) so each input of the encoder. <ul> <li>So when we right multiply \(SV_{enc}\) then the output of cross attention, e.g. the 5th row, is the weighted mean of the encodeder hidden representations based on how similar they are to the 5th token/row of \(Q_{dec}\) <h4 id="222-attention-implementation">2.2.2 Attention Implementation</h4> </li> </ul> </li> </ul> </li> </ul> </li> </ul> <p>You can implement all these type of attention with the same code we outlined above form MHA. Indeed, the differences between these types of attention depends only on the:</p> <ul> <li><strong>D1:</strong> input representation (is Q coming from the same place of K,V?) <ul> <li><em>used to distinguish between cross and self-attention</em></li> </ul> </li> <li><strong>D2:</strong> attention mask <ul> <li><em>used to distinguish causal vs non-causal attention</em></li> </ul> </li> </ul> <p>The mask was compute outside and passed to the attention, so D2 is already implemented. The only things we need to do is D1</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># OLD IMPLEMENTATION
</span><span class="k">def</span> <span class="nf">forward</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">x</span><span class="p">,</span>  <span class="n">mask</span><span class="o">=</span><span class="bp">None</span><span class="p">):</span>
	<span class="sh">"""</span><span class="s">
	input arg becomes q_input, kv_input.
	if self-attention q_input == kv_input
	</span><span class="sh">"""</span>
	<span class="n">q</span> <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="nf">w_Q</span><span class="p">(</span><span class="n">x</span><span class="p">)</span>
	<span class="n">k</span> <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="nf">w_K</span><span class="p">(</span><span class="n">x</span><span class="p">)</span>
	<span class="n">v</span> <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="nf">w_V</span><span class="p">(</span><span class="n">x</span><span class="p">)</span>
		
	<span class="c1"># same
</span>	<span class="bp">...</span>
		
<span class="c1"># NEW MECHANISM
</span><span class="k">def</span> <span class="nf">forward</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">q_input</span><span class="p">,</span> <span class="n">kv_input</span><span class="p">,</span> <span class="n">mask</span><span class="o">=</span><span class="bp">None</span><span class="p">):</span>
	<span class="sh">"""</span><span class="s">
	input arg becomes q_input, kv_input.
	if self-attention q_input == kv_input
	</span><span class="sh">"""</span>
	<span class="c1"># projections are modified to have D1
</span>	<span class="n">q</span> <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="nf">w_Q</span><span class="p">(</span><span class="n">q_input</span><span class="p">)</span> <span class="c1"># b x t x d
</span>	<span class="n">k</span> <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="nf">w_K</span><span class="p">(</span><span class="n">kv_input</span><span class="p">)</span>
	<span class="n">v</span> <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="nf">w_V</span><span class="p">(</span><span class="n">kv_input</span><span class="p">)</span>
	
	<span class="c1"># same
</span>	<span class="bp">...</span>
</code></pre></div></div> <h3 id="23-masking">2.3 Masking</h3> <h4 id="231-pad-mask">2.3.1 Pad Mask</h4> <p><strong>GOAL</strong> Blocking attention row-similarity database to look at PAD tokens as contain no info</p> <p>We can do this with this code</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">pad_mask</span><span class="p">(</span><span class="n">x</span><span class="p">,</span> <span class="n">pad</span><span class="o">=</span><span class="n">Tokenizer</span><span class="p">[</span><span class="sh">"</span><span class="s">PAD</span><span class="sh">"</span><span class="p">]):</span>
    <span class="sh">"""</span><span class="s">
    x: b x t
    output -&gt; 1 x 1 
    </span><span class="sh">"""</span>
    <span class="nf">return </span><span class="p">(</span><span class="n">x</span> <span class="o">!=</span> <span class="n">pad</span><span class="p">).</span><span class="nf">unsqueeze</span><span class="p">(</span><span class="mi">1</span><span class="p">).</span><span class="nf">unsqueeze</span><span class="p">(</span><span class="mi">2</span><span class="p">)</span>
</code></pre></div></div> <details class="notion-toggle"> <summary><strong>Code Breakdown</strong></summary> <p><code class="language-plaintext highlighter-rouge">Tokenizer["PAD"]</code> is a given integer-id of the tokenizer. <code class="language-plaintext highlighter-rouge">(x!=pad)</code> check when the token does not match, and after we add two mock dimension. Unsqueeze get you b x 1 x 1 x t and is useful for implementation details</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># 1. fake input 
</span><span class="n">x</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">arange</span><span class="p">(</span><span class="mi">0</span><span class="p">,</span><span class="mi">9</span><span class="p">).</span><span class="nf">reshape</span><span class="p">(</span><span class="mi">3</span><span class="p">,</span><span class="mi">3</span><span class="p">)</span><span class="o">+</span> <span class="mi">31992</span>
<span class="c1"># output
</span><span class="nf">tensor</span><span class="p">([[</span><span class="mi">31992</span><span class="p">,</span> <span class="mi">31993</span><span class="p">,</span> <span class="mi">31994</span><span class="p">],</span>
        <span class="p">[</span><span class="mi">31995</span><span class="p">,</span> <span class="mi">31996</span><span class="p">,</span> <span class="mi">31997</span><span class="p">],</span>
        <span class="p">[</span><span class="mi">31998</span><span class="p">,</span> <span class="mi">31999</span><span class="p">,</span> <span class="mi">32000</span><span class="p">]])</span>

<span class="c1"># 2. apply mask 
</span><span class="n">pad</span> <span class="o">=</span> <span class="mi">32000</span>
<span class="p">(</span><span class="n">x</span> <span class="o">!=</span> <span class="n">pad</span><span class="p">)</span>
<span class="c1"># output
</span><span class="nf">tensor</span><span class="p">([[</span> <span class="bp">True</span><span class="p">,</span>  <span class="bp">True</span><span class="p">,</span>  <span class="bp">True</span><span class="p">],</span>
        <span class="p">[</span> <span class="bp">True</span><span class="p">,</span>  <span class="bp">True</span><span class="p">,</span>  <span class="bp">True</span><span class="p">],</span>
        <span class="p">[</span> <span class="bp">True</span><span class="p">,</span>  <span class="bp">True</span><span class="p">,</span> <span class="bp">False</span><span class="p">]])</span>
        

</code></pre></div> </div> </details> <h4 id="232-causal-masks">2.3.2 Causal Masks</h4> <p><strong>GOAL</strong> Blocking attention row-similarity database to look at future tokens as it creates label leaking</p> <p><strong>IDEA</strong> The idea is that we want to mask the matrix \(QK^T\) such that for a given row, the right position after the diagonal count are False. It is easier to see:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="bp">True</span><span class="p">,</span> <span class="bp">False</span><span class="p">,</span> <span class="bp">False</span>
<span class="bp">True</span><span class="p">,</span> <span class="bp">True</span><span class="p">,</span> <span class="bp">False</span>
<span class="bp">True</span><span class="p">,</span> <span class="bp">True</span><span class="p">,</span> <span class="bp">True</span>
</code></pre></div></div> <p>We can do this with this code:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">causal_mask</span><span class="p">(</span><span class="n">t</span><span class="p">):</span>
    <span class="sh">"""</span><span class="s">Causal mask for decoders.</span><span class="sh">"""</span>
    <span class="k">return</span> <span class="n">torch</span><span class="p">.</span><span class="nf">tril</span><span class="p">(</span><span class="n">torch</span><span class="p">.</span><span class="nf">ones</span><span class="p">(</span><span class="n">t</span><span class="p">,</span> <span class="n">t</span><span class="p">,</span> <span class="n">dtype</span><span class="o">=</span><span class="n">torch</span><span class="p">.</span><span class="nb">bool</span><span class="p">)).</span><span class="nf">view</span><span class="p">(</span><span class="mi">1</span><span class="p">,</span> <span class="mi">1</span><span class="p">,</span> <span class="n">t</span><span class="p">,</span> <span class="n">t</span><span class="p">)</span>
</code></pre></div></div> <details class="notion-toggle"> <summary><strong>Code Breakdown</strong></summary> <p>L is the number of token. The view operation is needed for implementation details</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># 1. get a bool matrix tokens x tokens
</span><span class="n">t</span> <span class="o">=</span> <span class="mi">3</span>
<span class="n">bool_matrix</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">ones</span><span class="p">(</span><span class="n">t</span><span class="p">,</span> <span class="n">t</span><span class="p">,</span> <span class="n">dtype</span><span class="o">=</span><span class="n">torch</span><span class="p">.</span><span class="nb">bool</span><span class="p">)</span>
<span class="c1"># output 
</span><span class="nf">tensor</span><span class="p">([[</span><span class="bp">True</span><span class="p">,</span> <span class="bp">True</span><span class="p">,</span> <span class="bp">True</span><span class="p">],</span>
        <span class="p">[</span><span class="bp">True</span><span class="p">,</span> <span class="bp">True</span><span class="p">,</span> <span class="bp">True</span><span class="p">],</span>
        <span class="p">[</span><span class="bp">True</span><span class="p">,</span> <span class="bp">True</span><span class="p">,</span> <span class="bp">True</span><span class="p">]])</span>
        
<span class="c1"># 2. get a upper triangular matrix
</span><span class="n">mask</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">tril</span><span class="p">(</span><span class="n">bool_matrix</span><span class="p">)</span>
<span class="c1"># output
</span><span class="nf">tensor</span><span class="p">([[</span> <span class="bp">True</span><span class="p">,</span> <span class="bp">False</span><span class="p">,</span> <span class="bp">False</span><span class="p">],</span>
        <span class="p">[</span> <span class="bp">True</span><span class="p">,</span>  <span class="bp">True</span><span class="p">,</span> <span class="bp">False</span><span class="p">],</span>
        <span class="p">[</span> <span class="bp">True</span><span class="p">,</span>  <span class="bp">True</span><span class="p">,</span>  <span class="bp">True</span><span class="p">]])</span>
</code></pre></div> </div> </details> <h3 id="24-positional-encoding">2.4 Positional Encoding</h3> <h4 id="241-positional-encoders">2.4.1 Positional Encoders</h4> <p><strong>TYPES</strong> Broadly, positional encodings fall into two families: absolute and relative.</p> <p><strong>ABSOLUTE ENCODERS</strong> Attach an absolute position signal to each token independently. Examples are:</p> <ol> <li><em>learned encoding</em> for a max_context window</li> <li> <p><em>sinusoidal encoding</em>, which use deterministic sine/cosine features without learned parameters (attach to each index of the hidden dimension a cos/sin function with a given frequency and take as input the token position) <strong>RELATIVE ENCODERS</strong> inject information about the offset between a query at position i and a key at position j inside the attention module, so that the attention score depends on (j − i). So, if two tokens differ for 5 positions, the model will not have a distinct signal if they are the 1 and 6 or the 13024 and the 13029. <em><strong>The important property is that they are applied to attention logits</strong></em></p> </li> <li><em>T5 original encoding</em> is not done via <code class="language-plaintext highlighter-rouge">x = embedding(x)+encoding(x)</code> at the beginning of the forward pass. Instead, it computes a scalar values for each \((i-j)\) and store it to <em>add it to the attention logits</em> <details class="notion-toggle"> <summary>more details</summary> <p><img src="/assets/img/posts/notes-on-transformers/notion-10b815d2904b83d6975a81fd2da7e9df.png" alt="notes-on-transformers"/></p> </details> </li> <li><em>ROPE</em> it rotates the Q and K attention logits by applying a rotation matrix <code class="language-plaintext highlighter-rouge">R</code> so that in the attention computation of the score \(QK^T\) the dot product for two rows corresponging to two different positions depends only on the relative difference in position \((i-j)\) rather than the absolute position \(i,j\) <details class="notion-toggle"> <summary>more details</summary> <p><img src="/assets/img/posts/notes-on-transformers/notion-56b815d2904b83f9980c01ae6cde570e.png" alt="notes-on-transformers"/></p> <p><img src="/assets/img/posts/notes-on-transformers/notion-f48815d2904b83f0b97501fa5b81f07f.png" alt="notes-on-transformers"/></p> </details> </li> </ol> <p><strong>NO EXPLICIT ENCODING</strong> Recently, <em>NoPE (No explicit Positional Encoding)</em> is also being explored. In the <strong>decoder-only</strong> models, one can omit explicit positional embeddings and <em><strong>rely on the causal mask to indicate positions</strong></em></p> <p><strong>PROS AND CONS</strong></p> <ul> <li><em>Absolute Learned positional encodings</em> are <strong>simple</strong> but <strong>struggle with length extrapolation</strong> (train short, test long). For example, learned absolute embeddings cannot extend to lengths beyond the training range, because positions unseen during training have no learned parameters.</li> <li><em>Relative positional encodings</em> typically <strong>handle length generalization better</strong> and are common in modern LLMs.</li> <li>NoPE is extremely <strong>simple and parameter-free</strong>; it relies only on the causal mask for an implicit positional bias and can yield the best length generalization. However, <strong>training NoPE models from scratch can be difficult.</strong></li> </ul> <h3 id="25-tokenizer">2.5 Tokenizer</h3> <ul> <li>standard extra token of the three architectures</li> </ul> <h3 id="26-losses">2.6 Losses</h3> <h4 id="264-torch-implementation">2.6.4 Torch Implementation</h4> <p>In practice all loses are implemented in the same way in torch:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># logits and labels extracted from forward pass and dataloader
</span><span class="n">logits</span><span class="p">.</span><span class="n">shape</span> <span class="o">==</span> <span class="p">(</span><span class="n">batch_size</span><span class="p">,</span> <span class="n">seq_len</span><span class="p">,</span> <span class="n">vocab_size</span><span class="p">)</span>
<span class="n">y</span><span class="p">.</span><span class="n">shape</span> <span class="o">==</span> <span class="p">(</span><span class="n">batch_size</span><span class="p">,</span> <span class="n">seq_len</span><span class="p">)</span>

<span class="c1"># turning stackin batch on the seq_len dimension 
</span><span class="n">y_stacked</span> <span class="o">=</span> <span class="n">y</span><span class="p">.</span><span class="nf">view</span><span class="p">(</span><span class="o">-</span><span class="mi">1</span><span class="p">)</span> <span class="c1"># (batch_size * seq_len)
</span><span class="n">logit_stacked</span> <span class="o">=</span> <span class="n">logits</span><span class="p">.</span><span class="nf">view</span><span class="p">(</span><span class="o">-</span><span class="mi">1</span><span class="p">,</span> <span class="n">vocab_size</span><span class="p">)</span><span class="c1"># (batch_size * seq_len, vocab_size)
</span>
<span class="c1"># compute cross-entropy and ignore -100, we set to those mask
</span><span class="n">loss</span> <span class="o">=</span> <span class="n">F</span><span class="p">.</span><span class="nf">cross_entropy</span><span class="p">(</span><span class="n">logit_stacked</span><span class="p">,</span><span class="n">y_stacked</span><span class="p">,</span> <span class="n">ignore_index</span><span class="o">=-</span><span class="mi">100</span><span class="p">)</span>
</code></pre></div></div> <h4 id="open-questions">Open Questions</h4> <ul> <li> <ul> <li>What will happen if you increase or decrease the <code class="language-plaintext highlighter-rouge">p_mask</code> variable in BERT and T5 training?</li> </ul> </li> <li> <ul> <li>If keep increasing the sequence length in the <code class="language-plaintext highlighter-rouge">Config</code> in <code class="language-plaintext highlighter-rouge">model.py</code>, do you observe any memory issues in training the models? Do you know why?</li> </ul> </li> </ul> <hr/> <div class="notion-divider">✠</div> <div class="notion-divider">✠</div> <hr/> <h2 id="e5-attention-transformers-and-tokenisation">E5: Attention Transformers and Tokenisation</h2> <h4 id="ram-systems">RAM Systems</h4> <h3 id="floating-point-operations"><strong>Floating Point Operations</strong></h3> <p>Count the raw number of arithmetic operations (sum, multiplication …) required, ignoring parallelism or hardware specifics.</p> <ul> <li>Vector Addition (\(v + v\)): Given \(v \in \mathbb{R}^d\). Require \(d\) additions/FLOPs.</li> <li>Scalar Multiplication (\(c \cdot v\)): Given \(v \in \mathbb{R}^d\). Requires \(d\) multiplications/FLOPs.</li> </ul> <h4 id="cost-of-vector-dot-product"><strong>Cost of Vector Dot Product</strong></h4> <ul> <li>\(v^T w\): Given \(v, w \in \mathbb{R}^d\). Requires \(d\) multiplications and \(d-1\) additions. Total: \(\approx 2d\) FLOPs.</li> </ul> <h4 id="cost-of-matrix-vector-multiplication"><strong>Cost of Matrix-Vector Multiplication</strong></h4> <ul> <li>\(Av\): Given matrix \(A \in \mathbb{R}^{b \times d}\) and vector \(v \in \mathbb{R}^d\) The result is a vector in \(\mathbb{R}^b\). We perform \(b\) dot products, each involving a vector of size \(d\). Cost per dot product \(\approx 2d\). Total: \(2bd\) FLOPs.</li> </ul> <h4 id="matrix-matrix-multiplication"><strong>Matrix-Matrix Multiplication</strong></h4> <ul> <li>\(AB\): Given matrices \(A \in \mathbb{R}^{b \times d}\) and \(B \in \mathbb{R}^{d \times c}\). The result is a \(b \times c\) matrix. We must compute \(b \times c\) individual elements. Each element is the result of a dot product of size \(d\) (row of \(A\) dotted with column of \(B\)). Total: \((bc) \times 2d = 2bdc\) FLOPs.</li> <li>In the same scenario, given \(A, B\) and \(k \in R^{c}\), then \(ABk\) requires \(2bd + 2bc\) FLOPs</li> </ul> <h3 id="the-forward-pass-of-attention-models">The Forward Pass of Attention Models</h3> <h4 id="number-of-learnable-parameters-of-attention">Number of Learnable Parameters of Attention</h4> <p>We are estimating the storage footprint of a single multi-head attention block (ignoring biases)</p> <ul> <li>Key Components: <ul> <li>Input Projections: We have three distinct weight matrices for the Query (\(W^Q\)), Key (\(W^K\)), and Value (\(W^V\)). Each has a shape of \(d_{model} \times d_{model}\).</li> <li>Output Projection: After the heads are concatenated, there is one final linear projection \(W^O\), also with shape \(d_{model} \times d_{model}\)</li> <li>Total Parameters: \(4 d_{model}^2 = O(d_{model}^2)\)</li> </ul> </li> <li>Note: The parameter count is independent of the number of heads ($h$) because the splitting into heads happens via reshaping the computed tensors, not by changing the size of the initial weight matrices</li> </ul> <h4 id="flops-for-attention-pass">FLOPs for Attention Pass</h4> <p>input tensor \((1, n, d_{model})\).</p> <h4 id="step-a-q-k-v-projections-linear-layers">Step A: Q, K, V Projections (Linear Layers)</h4> <ul> <li>Operation: Three matrix multiplications of \((n \times d_{model}) \times (d_{model} \times d_{model})\)</li> <li>Calculation: \(3 \times (2 \cdot n \cdot d_{model} \cdot d_{model})\)</li> <li>Cost: \(6 n d_{model}^2\)</li> </ul> <h4 id="step-b-attention-scores-q-kt">Step B: Attention Scores (\(Q K^T\))</h4> <ul> <li>Operation: For each head, multiply \((n \times d_k)\) by \((d_k \times n)\).</li> <li>Calculation: \(h \times (2 \cdot n \cdot d_k \cdot n) = 2 n^2 (h \cdot d_k)\).</li> <li>Cost: \(2 n^2 d_{model}\).</li> </ul> <h4 id="step-c-scaling--softmax">Step C: Scaling &amp; Softmax</h4> <ul> <li>Operation: Element-wise scaling by \(1/\sqrt{d_k}\) and softmax exponentials/divisions on scores \((n, n, h)\)</li> <li>Cost: \(O(n^2 h)\)</li> </ul> <h4 id="step-d-weighted-sum-attention-cdot-v">Step D: Weighted Sum (\(Attention \cdot V\))</h4> <ul> <li>Operation: For each head, multiply Softmax output \((n \times n)\) by \(V\) \((n \times d_k)\).</li> <li>Calculation: \(h \times (2 \cdot n \cdot n \cdot d_k) = 2 n^2 (h \cdot d_k)\).</li> <li>Cost: \(2 n^2 d_{model}\).</li> </ul> <h4 id="step-e-output-projection">Step E: Output Projection</h4> <ul> <li>Operation: One matrix multiplication of \((n \times d_{model}) \times (d_{model} \times d_{model})\)</li> <li>Calculation: \(1 \times (2 \cdot n \cdot d_{model} \cdot d_{model})\)</li> <li>Cost: \(2 n d_{model}^2\)</li> </ul> <h4 id="total-flops-for-foward-pass-of-attention-block">Total FLOPs for foward pass of attention block</h4> <ul> <li>Summing all components: \(\text{Total} \approx 8 n d_{model}^2 + 4 n^2 d_{model} + O(n^2 h)\)</li> <li>In paractice \(O(n^2 h)\) is dominated by the rest</li> </ul> <h3 id="efficiency-bottleneck">Efficiency Bottleneck</h3> <ul> <li>high number of heads ⇒ slow down scaling + softmax as \(O(n^2 h)\)</li> <li>high latent dimentions ⇒ slow down projection as \(O(n d_{model}^2)\)</li> <li>high numner of tokens ⇒ slow down \(QKV\) circuits as \(O(n^2 d_{model})\)</li> </ul> <hr/> <div class="notion-divider">✠</div> <div class="notion-divider">✠</div> <hr/> <h2 id="e6-attention-transformers-and-tokenisation">E6: Attention Transformers and Tokenisation</h2> <h3 id="bert-t5-and-gpt-models">BERT, T5, and GPT models</h3> <h4 id="model-architectures--attention-flows">Model Architectures &amp; Attention Flows</h4> <p>The key difference lies in Masking and where the Query/Key/Value vectors come from.</p> <ul> <li>Encoder-Only (BERT) ◦ Goal: Learn contextual representations of the whole input ◦ Attention: Uses Self-Attention where Q, K, V all come from the encoder hidden states ◦ Masking: Bidirectional. Every token attends to every other token (no mask)</li> <li>Decoder-Only (GPT) <ul> <li>Goal: Learn left-to-right generative modelling</li> <li>Attention: Uses Self-Attention where Q, K, V come from the decoder hidden states</li> <li>Masking: Causal (Upper-Triangular). At position t can only attend to positions \(\le t\)</li> </ul> </li> <li>Encoder-Decoder (T5) <ul> <li>Goal: Separate source encoding from target generation (sequence-to-sequence)</li> <li>Attention Types: <ol> <li>Encoder Self-Attention: Bidirectional (like BERT)</li> <li>Decoder Self-Attention: Causal (like GPT)</li> <li>Cross-Attention: The Decoder supplies the Queries while the Encoder supplies the Keys and Values. This allows the decoder to “look back” at the whole input</li> </ol> </li> </ul> </li> </ul> <h4 id="positional-encodings">Positional Encodings</h4> <ul> <li>Absolute Encoding (BERT &amp; GPT) <ul> <li>Mechanism: A specific vector is learned for each position, and added to the token embedding</li> <li>Limitation: It struggles with Length Extrapolation. If trained on 512 tokens, the model has no parameter for position 51312.</li> </ul> </li> <li>Relative Encoding (T5) <ul> <li>Mechanism: Instead of input embeddings, T5 adds a learnable bias directly to the attention logits (scores) based on the offset (i-j) between tokens <ul> <li>maintains separate learned bias tables for each of its three attention types</li> </ul> </li> <li>Bucketing: Distances are “bucketed” (nearby positions are distinct; far positions are grouped logarithmically) to handle long sequences efficiently</li> <li>Advantage: Generalizes better to sequence lengths unseen during training</li> <li>How it works: Define one parameter per bucket and simply add it to the attention \(e_{i,j} = \frac{Q_i K_j^T}{\sqrt{d_k}} + \beta_{(i-j)}\)</li> </ul> </li> <li>Modern Alternatives <ul> <li>RoPE (Rotary): Rotates query/key vectors so their dot product reflects their relative distance. Standard in Llama/Mistral</li> <li>NoPE: Uses no explicit encoding; relies entirely on the causal mask to provide position information. Difficult to train from scratch</li> </ul> </li> </ul> <h4 id="training-pipelines-and-objectives">Training pipelines and objectives</h4> <ul> <li>BERT: Masked Language Modeling (MLM) <ul> <li>Method: Randomly selects 15% of tokens. Replaces them with <code class="language-plaintext highlighter-rouge">[MASK]</code> (80%), random token (10%), or keeps original (10%)18.</li> <li>Objective: Predict the original token for the masked positions only based on context \(\mathcal{L}_{MLM} = -\sum_{i \in M} \log p_{\theta}(x_i \vert x_{\setminus M})\)</li> </ul> </li> <li>GPT: Autoregressive (Causal) Modeling <ul> <li>Method: Predicts the next token in the sequence given all previous tokens</li> <li>Objective: Standard left-to-right likelihood maximization \(\mathcal{L}_{AR} = -\sum_{i=1}^{t} \log p_{\theta}(x_i \vert x_{&lt;i})\)</li> </ul> </li> <li>T5: Span-Corruption <ul> <li>Method: Replaces contiguous spans of text in the encoder input with sentinel tokens (e.g., <code class="language-plaintext highlighter-rouge">&lt;extra_id_0&gt;</code>)</li> <li>Target: The decoder must generate <em>only</em> the dropped-out spans, separated by the sentinels \(\mathcal{L}_{T5} = -\sum_{s=1}^{ \vert y \vert } \log p_{\theta}(y_s \vert y_{&lt;s}, \text{Enc}(x^{masked}))\)</li> </ul> </li> </ul> <h3 id="a-mental-model-for-gpu-memory-and-its-management">A Mental Model for GPU memory and its management</h3> <ul> <li>GPU memory is hierarchical <ul> <li>We have big slow (global) memory (DRAM), and small fast (shared) memory (SRAM). To perform computations, data has to be moved from the slow memory to the fast memory</li> <li>There are two possible memory bottlenecks: -storage: when global memory is not large enough -memory traffic: when moving data from global to shared memory is slow</li> </ul> </li> <li>To analyse whether an operation is “Compute-Bound” (limited by calculation speed) or “Memory-Bound” (limited by data transfer speed). We measure this using Arithmetic Intensity (AI), defined as the ratio of FLOPs performed to bytes moved <ul> <li> \[AI = \frac{\text{Total FLOPs}}{\text{Total Bytes Accessed}}\] <ul> <li>If the AI is low, the GPU spends more time waiting for data than calculating (Memory-Bound). If AI is high, it spends more time calculating (Compute-Bound). <ul> <li><em><strong>we want it to be compute bound so we use the most of the computational power</strong></em></li> </ul> </li> </ul> </li> <li>We can compare it with the detailed statistics of GPUs. Let the critical arithmetic intensity (ridge point) be \(I^∗\) = Peak FLOPs/Peak memory bandwidth. For A100 80GB (HBM2e ≈ 2.0 TB/s, BF16 Tensor Core ≈ 312 TFLOPs), \(I^∗\) ≈312/2.0≈ 156 FLOPs/Byte. For H100 SXM (HBM3 ≈ 3.35 TB/s, BF16 Tensor Core ≈ 989 TFLOPs), \(I^∗\) ≈989/3.35≈295 FLOPs/Byte <ul> <li>if an operation’s AI exceed the \(I^*\) it is Compute bound; if it is below it is Memory bound</li> </ul> </li> </ul> </li> </ul> <h4 id="memory-traffic">Memory traffic</h4> <p>We now analyse the memory traffic for training/prefilling and decoding modes.</p> <ul> <li>Linear Projections <ul> <li>Scenario: Input \(X\) ( \(b \times t \times d\) ) multiplied by Weights \(W\) ( \(d \times d\) ).</li> <li>Data Moved (Bytes): We must read \(X\), read \(W\), and write Output \(Y\) ( \(b \times t \times d\) ) + ( \(d \times d\) ) + ( \(b \times t \times d\) ) = ( \(2btd\) + \(d^2\) ) \(\times s\) (where \(s\) is bytes per element, e.g., 2 for bf16)</li> <li>Operations done (FLOPS) \(\approx 2btd^2\)</li> <li> \[AI_{lin} \approx \frac{2btd^2}{(2btd + d^2)s}\] </li> <li>Training (\(bt \gg d\)): The \(2btd\) term dominates the denominator. The ratio simplifies to \(\approx d/s\). For a standard model (\(d=4096, s=2\)), AI is \(\approx 2048\) which is Compute-Bound.</li> <li>Decoding (\(t=1\) ): The data movement for weights (\(d^2\)) becomes significant compared to the small amount of computation. The AI drops to \(\approx 2b/s\), making it Memory-Bound</li> </ul> </li> <li>Scaled Dot-Product Attention <ul> <li>Core attention mechanism: \(Softmax(QK^T)V\)</li> <li><strong>Scenario:</strong> Full score matrix \(S = Softmax(QK^T)\) of size ( \(b \times a \times t \times t\) ), where \(a\) is the number of heads.</li> <li>Data Moved (Bytes): read Q + read K + write S + read S + read V + write O Reading Q, K, V, O is small ( \(btd\) each), but reading/writing the massive score matrix \(S\) ( \(bat^2\) ) is huge: \(\text{Bytes} \approx (4btd + 2bat^2)s\)</li> <li>Compute (FLOPs) \(\approx 4bt^2d\)</li> <li> \[AI_{attn} \approx \frac{2td}{s(2d + at)}\] </li> <li>Memory-Bound Nature: When \(t ≫ 2d/a\) (often since \(d/a = h \in \{64,128\}\)) \(AI_{attn} \approx \frac{2td}{s(2d + at)} \approx \frac{2d}{sa}\) which is quite small, hence, Memory bound Decoding: we must load the entire KV cache for every new token generated. This creates massive memory traffic, hence, Memory bound <ul> <li>approaches like FlashAttention-style kernels improve</li> </ul> </li> </ul> </li> </ul> <h4 id="memory-storage">Memory storage</h4> <p>We now look at whether the GPU has enough DRAM (e.g., 80GB on an A100) to hold the necessary data</p> <ul> <li>Decoding (KV cache bottleneck) <ul> <li>We must store the Key and Value matrices for every previous token to avoid re-computing them at every step To cache a batch \(b\) of sequences with length \(t\), across all layers \(L\):</li> </ul> <p>\(\text{Total Bytes} = L \times b \times (2 \cdot d \cdot t) \times s\) (Where 2 accounts for K and V, and \(s\) is bytes per element, usually 2)</p> <p>For a 7B model (\(L=32, d=4096\)) on an A100 M=80GB GPU: If the sequence length is 4096, the memory is so full that the maximum batch size is only \(\approx\) 40. <img src="/assets/img/posts/notes-on-transformers/notion-d9c815d2904b83c592db01e1eacb27bb.png" alt="notes-on-transformers"/></p> <ul> <li>In practice, because model weights also take space (7B weights in bf16 ≈ 14 GiB), it is closer to 28-32 Note: Still to use the cached K and V we still have to load them to fast memory which slows down the decoding more</li> </ul> </li> <li>Training: The Optimizer State Bottleneck <ul> <li>Training runs out of memory faster than inference because of the Optimizer States (like Adam), which are often stored in higher precision (FP32)</li> <li>Rule of thumb: Memory for every parameter in the model: \(\text{Bytes per Param} \approx \underbrace{2}_{\text{weight}} + \underbrace{2}_{\text{gradient}} + \underbrace{4}_{\text{momentum } m} + \underbrace{4}_{\text{variance } v} = \mathbf{12}\) There is also an optional fp32 master copy (optional +4)</li> <li>Example: A 7B parameter model requires \(\approx\) 84GB (105GB if we keep a master copy of the parameters) just for these states, ignoring activations needed for backprop</li> </ul> <p>This forces the use of Sharding: splitting the model/states across multiple GPUs, e.g., ZeRO</p> </li> </ul>]]></content><author><name></name></author><category term="blog"/><category term="tutorial"/><category term="ml"/><summary type="html"><![CDATA[Some notes I took at EPFL on transformers from C. Bunne’s course. It is still WIP, some references (e.g., for images) are missing.]]></summary></entry><entry><title type="html">Notes on Vision Models</title><link href="https://tommasomncttn.github.io/blog/2026/notes-on-vision-models/" rel="alternate" type="text/html" title="Notes on Vision Models"/><published>2026-08-14T00:00:00+00:00</published><updated>2026-08-14T00:00:00+00:00</updated><id>https://tommasomncttn.github.io/blog/2026/notes-on-vision-models</id><content type="html" xml:base="https://tommasomncttn.github.io/blog/2026/notes-on-vision-models/"><![CDATA[<h2 id="a-mae-and-ibot-e7">A. MAE, and iBOT (E7)</h2> <p>Our focus is on a trio of seminal self-supervised learning methods for computer vision:</p> <ul> <li>Masked Autoencoders (MAE),</li> <li>Image BERT Pre-Training with Online Tokenizer (iBOT)</li> <li>knowledge DIstillation with NO labels (DINO).</li> </ul> <h3 id="10-masked-autoencoders-mae">1.0 Masked Autoencoders (MAE)</h3> <p><strong>CORE IDEA MAE</strong> Masked Autoencoders (MAE) core concept is intuitive: the model learns rich visual representations by reconstructing masked or missing portions of an input image.</p> <h4 id="11-core-architecture-and-pre-training-mechanism">1.1 Core Architecture and Pre-training Mechanism</h4> <p>The MAE pre-training process, as illustrated in the architectural overview, is notable for its efficiency and elegant design. The procedure unfolds in several key steps:</p> <p><img src="/assets/img/posts/notes-on-vision-models/notion-3bc815d2904b80ff9d6fdddd00bdd536.png" alt="notes-on-vision-models"/></p> <ol> <li><strong>Patching and Masking:</strong> An input image is first divided into a grid of non-overlapping patches. A large, random subset of these patches (e.g., 75%) is then masked, making them invisible to the encoder.</li> <li><strong>Encoding Visible Patches:</strong> The MAE encoder, a standard Vision Transformer, processes <em>only the small subset of visible patches</em>. This is a critical design choice for efficiency.</li> <li><strong>Introducing Mask Tokens:</strong> After the visible patches have been encoded, learnable “mask tokens” are introduced to represent the positions of the patches that were originally masked.</li> <li><strong>Decoding and Reconstruction:</strong> A <em>small, lightweight</em> decoder takes the full sequence of encoded patches and mask tokens as input. Its sole task is to reconstruct the original, uncorrupted image in pixel space from this limited information.</li> </ol> <div class="notion-callout notion-callout--red"> <div class="notion-callout-icon">🚨</div> <div class="notion-callout-body"> <p><strong>COMPUTATIONAL EFFICIENCY</strong> This <strong>asymmetric encoder-decoder design</strong> is the cornerstone of MAE’s computational efficiency. By applying the large, computationally expensive encoder to only a fraction of the input (e.g., 25%), <em><strong>MAE dramatically reduces training time and memory requirements.</strong></em></p> </div> </div> <h4 id="12-adapting-masked-modeling-from-language-bert-to-vision-mae">1.2 Adapting Masked Modeling from Language (BERT) to Vision (MAE)</h4> <p><strong>CHALLENGE OF MAE TEXT → VISION</strong> The success of masked modeling in natural language processing (NLP) with models like BERT inspired MAE, but these are challenges:</p> <ul> <li><strong>Information Redundancy:</strong> Images exhibit high spatial redundancy; neighboring patches often contain very similar information. <ul> <li><em>SOLUTION →</em> To create a challenging learning task, MAE must use a very high masking ratio (e.g., 75%). In text, information is denser and a lower masking ratio (e.g., 15% in BERT) is sufficient.</li> </ul> </li> <li><strong>Tokenization:</strong> Language has a natural, discrete unit: the word or sub-word token. Images are continuous signals. <ul> <li><em>SOLUTION →</em> MAE must therefore make an architectural decision to segment an image into a grid of patches, creating a set of “visual tokens.”</li> </ul> </li> <li><strong>Semantic Density:</strong> A single word token in text typically carries a concentrated semantic meaning. An individual image patch, however, may contain very little semantic information on its own, forcing the model to learn spatial relationships and global context.</li> <li><strong>Reconstruction Target:</strong> BERT predicts discrete tokens from a finite vocabulary. MAE’s decoder reconstructs continuous pixel values → <strong>output space fundamentally different</strong> <ul> <li>SOLUTION → use different loss such as <em><strong>MSE</strong></em></li> </ul> </li> <li><strong>Computational Efficiency:</strong> The quadratic complexity of transformers is more problematic for images, which typically yield a much larger number of tokens (patches) than a sentence. <ul> <li>SOLUTION → MAE’s asymmetric design has big encoder that process few tokens and small decoder that process everything</li> </ul> </li> </ul> <h4 id="13-analysis-of-maes-pre-training-effectiveness">1.3 Analysis of MAE’s Pre-training Effectiveness</h4> <p><strong>OLD LOSS</strong> Prior to MAE, several other self-supervised pretext tasks for vision were proposed:</p> <ul> <li><strong>jigsaw</strong> puzzles → predict the right position of the patch</li> </ul> <p><img src="/assets/img/posts/notes-on-vision-models/notion-3bc815d2904b8055bef7c752787b4852.png" alt="notes-on-vision-models"/></p> <ul> <li><strong>in-coloring</strong> → (predicting color from a grayscale image)</li> </ul> <p><img src="/assets/img/posts/notes-on-vision-models/notion-3bc815d2904b808094f4c6b05c082b7a.png" alt="notes-on-vision-models"/></p> <p><strong>ADVANTAGE OF MAE</strong> . The MAE framework ultimately prevailed due to a combination of factors:</p> <ul> <li><strong>simplicity →</strong> The pixel-reconstruction objective is straightforward to implement</li> <li><strong>scalability →</strong> <em>computational efficiency</em> of the asymmetric encoder-decoder design, allows MAE to scale to enormous models and datasets.</li> </ul> <h4 id="14-a-deeper-look-at-masking-strategies">1.4 A Deeper Look at Masking Strategies</h4> <p><strong>MAE MASKING TYPES</strong> we say that MAE reconstruct mask but how should we mask? These are three possible type coherent with masking</p> <p><img src="/assets/img/posts/notes-on-vision-models/notion-3bc815d2904b807e9d72ef56983fbf62.png" alt="notes-on-vision-models"/></p> <ul> <li><strong>Random Masking:</strong> This strategy proved <em><strong>to be the most effective</strong></em>. <ul> <li>By removing patches without any discernible spatial structure → prevents the model from “cheating” by exploiting <em><strong>local continuity or predictable patterns</strong></em>.</li> <li>This forces the development of a global contextual understanding and the learning of holistic, robust representations.</li> </ul> </li> <li><strong>Block-wise Masking:</strong> This approach removes contiguous rectangular regions <ul> <li>While this may encourage object-level reasoning</li> <li>the task is generally <em><strong>easier</strong></em> because the predictable boundaries and larger areas of intact local context provide stronger clues for reconstruction.</li> </ul> </li> <li><strong>Grid-based Masking:</strong> This strategy maintains a uniform spatial sampling of visible patches <ul> <li>The regular intervals make reconstruction easier → the model can often rely on simple spatial interpolation from neighboring visible patches</li> </ul> </li> </ul> <div class="notion-callout notion-callout--red"> <div class="notion-callout-icon">🚨</div> <div class="notion-callout-body"> <p><strong>random masking</strong> is the best one + mask 75% not 15% as in text</p> </div> </div> <hr/> <div class="notion-divider">✠</div> <h3 id="20-information-theoretic-rationale-for-optimal-masking-ratios-todo">2.0 Information-Theoretic Rationale for Optimal Masking Ratios (TODO)</h3> <p>A central question in masked modeling is why the optimal masking ratio differs so dramatically between text (BERT at ~15%) and images (MAE at ~75%). The answer lies in the fundamental structure of the data itself. This section provides a formal, information-theoretic argument using the concepts of mutual information and entropy to explain this critical difference.</p> <h4 id="21-mutual-information-in-text-sequences">2.1 Mutual Information in Text Sequences</h4> <p>For a text sequence, we can quantify the amount of information the visible tokens (<code class="language-plaintext highlighter-rouge">xv</code>) provide about the masked tokens (<code class="language-plaintext highlighter-rouge">xm</code>) using mutual information:</p> <p><code class="language-plaintext highlighter-rouge">I(xm;xv) = H(xm) − H(xm|xv)</code></p> <p>Here, <code class="language-plaintext highlighter-rouge">H(xm)</code> is the entropy (uncertainty) of the masked tokens, and <code class="language-plaintext highlighter-rouge">H(xm|xv)</code> is the conditional entropy (the remaining uncertainty given the visible tokens).</p> <ul> <li>The entropy of <code class="language-plaintext highlighter-rouge">m</code> masked tokens, each from a vocabulary of size <code class="language-plaintext highlighter-rouge">V</code>, can be approximated as: \(H(xm) ≈ m log V\) <ul> <li>The context from visible tokens reduces this uncertainty. We can model this with an “effective vocabulary size” <code class="language-plaintext highlighter-rouge">Veff(r)</code>, which represents the plausible tokens given the context. The conditional entropy is thus:</li> </ul> </li> </ul> <p>This gives us the final expression for mutual information in text:</p> <p><code class="language-plaintext highlighter-rouge">I(xm;xv) = m log(V / Veff(r))</code></p> <p>For text, this mutual information <strong>decays rapidly</strong> as the masking ratio <code class="language-plaintext highlighter-rouge">r</code> increases. Text has a 1D sequential structure where information flow is highly dependent on adjacent tokens. Masking even a few words can break this chain in multiple places, severely shrinking the contextual window and making prediction difficult. As more context is removed, <code class="language-plaintext highlighter-rouge">Veff(r)</code> quickly approaches the full vocabulary size <code class="language-plaintext highlighter-rouge">V</code>, causing the mutual information to plummet towards zero. For example, predicting “sat” in “The cat <em>__ on the mat” is easy, but becomes nearly impossible with a 50% masking ratio in “The __</em> <em>__ on the __</em>.”</p> <h4 id="22-mutual-information-in-image-patches">2.2 Mutual Information in Image Patches</h4> <p>For images, we use the analogous concept of differential entropy for continuous data:</p> <p><code class="language-plaintext highlighter-rouge">I(pm;pv) = h(pm) − h(pm|pv)</code></p> <p>In stark contrast to text, the mutual information in images <strong>decays slowly</strong> as the masking ratio <code class="language-plaintext highlighter-rouge">r</code> increases. This resilience is due to several properties of visual data:</p> <ul> <li><strong>High Spatial Redundancy:</strong> Neighboring patches are highly correlated.</li> <li><strong>2D Connectivity:</strong> Each patch is surrounded by up to 8 neighbors, compared to only 2 for a word in a sequence. <em><strong>This creates multiple, redundant information pathways. Even if some neighbors are masked, others can provide context.</strong></em></li> </ul> <p>This property can be analogized to a <em><strong>Markov Random Field (MRF), where a patch is conditionally independent of the rest of the image given its immediate neighbors</strong></em>.</p> <ul> <li>Because of the dense 2D connectivity, context is preserved far more effectively than in a 1D text sequence.</li> <li>Consequently, the difficulty of the prediction task scales differently. At low masking ratios, reconstruction can be solved with simple <strong>low-level texture interpolation</strong> from neighbors.</li> <li>However, at a high masking ratio like MAE’s 75%, the gaps are too large for local interpolation. The model is forced to develop a <strong>high-level semantic understanding</strong> of objects, shapes, and scenes to infer the missing content, leading to the learning of more powerful representations. <h4 id="23-mathematical-derivation-of-optimal-ratios">2.3 Mathematical Derivation of Optimal Ratios</h4> </li> </ul> <p>The goal of the pre-training task is to find a masking ratio <code class="language-plaintext highlighter-rouge">r</code> that maximizes the learning objective. This objective can be modeled as a trade-off between the difficulty of the task (how much information is available) and the strength of the training signal (how much the model has to predict).</p> <p><code class="language-plaintext highlighter-rouge">L(r) = I(r) · r</code> where <code class="language-plaintext highlighter-rouge">I(r)</code> is the mutual information (task solvability) and <code class="language-plaintext highlighter-rouge">r</code> is the masking ratio (training signal).</p> <p>We can model the information decay as an exponential function <code class="language-plaintext highlighter-rouge">I(r) = I0 * e^(−λr)</code>, where <code class="language-plaintext highlighter-rouge">λ</code> is the decay rate. Text has a high decay rate (<code class="language-plaintext highlighter-rouge">λtext</code>) and images have a low one (<code class="language-plaintext highlighter-rouge">λimages</code>). The learning objective becomes:</p> <p><code class="language-plaintext highlighter-rouge">L(r) = (I0 * e^(−λr)) · r</code></p> <p>To find the optimal masking ratio <code class="language-plaintext highlighter-rouge">r*</code>, we take the derivative of <code class="language-plaintext highlighter-rouge">L(r)</code> with respect to <code class="language-plaintext highlighter-rouge">r</code> and set it to zero:</p> <p><code class="language-plaintext highlighter-rouge">dL/dr = I0 * e^(−λr) * [1 − λr] = 0</code></p> <p>Since <code class="language-plaintext highlighter-rouge">I0</code> and <code class="language-plaintext highlighter-rouge">e^(−λr)</code> are always positive, the expression is zero only when <code class="language-plaintext highlighter-rouge">1 − λr = 0</code>. This gives us the optimal ratio:</p> <p><code class="language-plaintext highlighter-rouge">r* = 1/λ</code></p> <p>This elegant result provides our proof. We know from the structural properties of text and images that information decays much faster in text. Therefore, <code class="language-plaintext highlighter-rouge">λtext &gt; λimages</code>. Taking the reciprocal of this inequality reverses its direction:</p> <p><code class="language-plaintext highlighter-rouge">r*text = 1/λtext &lt; 1/λimages = r*images</code></p> <p>This formally demonstrates that the optimal masking ratio for text must be lower than for images. A numerical verification using the empirical values (<code class="language-plaintext highlighter-rouge">r*text ≈ 0.15</code> and <code class="language-plaintext highlighter-rouge">r*images ≈ 0.75</code>) confirms this:</p> <p><code class="language-plaintext highlighter-rouge">λtext ≈ 1 / 0.15 ≈ 6.67</code> <code class="language-plaintext highlighter-rouge">λimages ≈ 1 / 0.75 ≈ 1.33</code></p> <p>The information decay rate is over 5 times higher for text, consistent with the fundamental difference in data structure.</p> <p>Having explored the reconstruction-based approach of MAE, we now transition to iBOT, which combines reconstruction with a self-distillation framework.</p> <hr/> <div class="notion-divider">✠</div> <h3 id="30-ibot-fusing-masked-modeling-with-self-distillation">3.0 iBOT: Fusing Masked Modeling with Self-Distillation</h3> <p><strong>CORE IDEA</strong> iBOT (Image BERT Pre-Training with Online Tokenizer) represents a sophisticated evolution in self-supervised learning. Its core innovation is the synergistic <em><strong>combination of two powerful paradigms</strong></em>: the <strong>masked prediction task</strong>, characteristic of models like BERT and MAE, and the <strong>student-teacher self-distillation framework</strong>, seen in models like BYOL and DINO. This hybrid approach allows iBOT to learn both fine-grained local features and high-level semantic representations simultaneously.</p> <h4 id="31-core-architecture-and-training-methodology">3.1 Core Architecture and Training Methodology</h4> <p><strong>ARCHITECTURE</strong> The iBOT architecture is built around a student-teacher framework.</p> <p><img src="/assets/img/posts/notes-on-vision-models/notion-3bc815d2904b8009a35bef9f8c23350e.png" alt="notes-on-vision-models"/></p> <ul> <li><strong>Student and Teacher Networks:</strong> The model consists of a <strong>student network (</strong><code class="language-plaintext highlighter-rouge">fs</code>) that learns via gradient descent and a <strong>teacher network (</strong><code class="language-plaintext highlighter-rouge">ft</code>) whose weights are an exponential moving average (EMA) of the student’s weights. The teacher provides stable, slowly evolving prediction targets. <ul> <li>the name <strong>online tokenizer stands for teacher network</strong></li> </ul> </li> <li><strong>Augmented Views:</strong> For each input image, two differently augmented views, <code class="language-plaintext highlighter-rouge">u</code> and <code class="language-plaintext highlighter-rouge">v</code>, are created. The student processes a view with randomly masked patches, while the teacher processes an unmasked view.</li> <li><strong>Stop-Gradient:</strong> Crucially, no gradients flow back through the teacher network (<code class="language-plaintext highlighter-rouge">stop grad</code>). This ensures the student learns to predict the teacher’s output, not the other way around.</li> </ul> <p><strong>TRAINING</strong> Training is driven by a dual-objective loss function:</p> <ol> <li><strong>Global Self-Distillation Loss (</strong><code class="language-plaintext highlighter-rouge">L[CLS]</code>): The global representation from the student’s <code class="language-plaintext highlighter-rouge">[CLS]</code> token is trained to match the global representation from the teacher’s <code class="language-plaintext highlighter-rouge">[CLS]</code> token. This encourages the model to <em><strong>learn view-invariant, high-level semantic features.</strong></em></li> <li><strong>Masked Image Modeling Loss (</strong><code class="language-plaintext highlighter-rouge">LMIM</code>): The student’s patch tokens corresponding to the masked input regions are trained to predict the feature representations from the teacher’s corresponding patch tokens. This forces the model to <em><strong>learn fine-grained, local information.</strong></em></li> </ol> <div class="notion-callout notion-callout--gray"> <div class="notion-callout-icon">💡</div> <div class="notion-callout-body"> <p>The output are not actually patches as show in the picture. Instead, we assume that there are N classes or prototype vectors (we can assume we are predicting these prototype vector, e.g. a VQ-VAE quantization) and we get from the heads \(h_s\) a probability distribution. Then we <em><strong>can the cross-entropy:</strong></em></p> <p><img src="/assets/img/posts/notes-on-vision-models/notion-3bc815d2904b806b9b64fcd392758fa8.png" alt="notes-on-vision-models"/></p> </div> </div> <div class="notion-callout notion-callout--red"> <div class="notion-callout-icon">🚨</div> <div class="notion-callout-body"> <p><strong>CORE CONCEPT:</strong></p> <ul> <li><strong>DUAL CE LOSS → cross entropy loss for both CLS and Masked-patch penalization</strong></li> <li><strong>Masked invariance for STUDENT TEACHER rather than AUGMENTATION</strong></li> </ul> </div> </div> <h4 id="32-feature-space-prediction-ibot-vs-pixel-space-reconstruction-mae">3.2 Feature-Space Prediction (iBOT) vs. Pixel-Space Reconstruction (MAE)</h4> <p>Instead of reconstructing pixels like MAE, iBOT predicts the teacher’s feature representations. This design choice carries a distinct set of trade-offs.</p> <table> <thead> <tr> <th><strong>Advantages of Feature-Space Prediction (iBOT) vs input space prediciton (MAE)</strong></th> <th><strong>Disadvantages of Feature-Space Prediction (iBOT) vs input space prediciton (MAE)</strong></th> </tr> </thead> <tbody> <tr> <td><strong>Semantic-Level Learning:</strong> Encourages capturing abstract concepts rather than low-level textures (<em>as you do cross entropy on features not on low level pixel</em>).</td> <td><strong>Dependency on Teacher Quality:</strong> Representations are capped by the teacher’s feature quality.</td> </tr> <tr> <td><strong>Robustness to Noise:</strong> Less sensitive to pixel-level noise and minor variations.</td> <td><strong>Increased Complexity:</strong> Requires maintaining and updating a teacher network via EMA.</td> </tr> <tr> <td><strong>Computational Efficiency:</strong> Avoids a large decoder mapping back to high-dimensional pixel space to reconstruct image.</td> <td><strong>Reduced Interpretability:</strong> Feature predictions are not directly visualizable like pixel outputs.</td> </tr> <tr> <td><strong>Avoids Trivial Solutions:</strong> Prevents simple blurring or interpolation strategies from satisfying the loss (you can do with pixel).</td> <td><strong>Potential Information Loss:</strong> Operating in a compressed feature space may discard fine-grained details.</td> </tr> </tbody> </table> <h4 id="33-ibots-evolution-from-bert-and-byol">3.3 iBOT’s Evolution from BERT and BYOL</h4> <p>iBOT cleverly adapts and combines concepts from both its language and vision predecessors.</p> <p><strong>FROM BERT TO IBOT</strong> Compared to <strong>BERT</strong>, iBOT makes several adaptations for vision:</p> <ul> <li><strong>self-distillation</strong> → It replaces BERT’s single-model design with a <strong>student-teacher framework</strong>. This is necessary because images lack a predefined discrete vocabulary (like words), so targets must be generated dynamically.</li> <li><strong>dual-objective</strong> → combines masked patch prediction with global [CLS] token alignment through contrastive learning, whereas BERT relies solely on masked language modeling.</li> <li><strong>no vocabulary →</strong> <ul> <li>BERT predict token from a vocabulary → iBOT predict teacher’s <strong>continuous feature representations</strong></li> <li>BERT tokenize via a vocabbulary → iBOT simply patchify the image without <em><strong>predefined discrete vocabulary</strong></em></li> </ul> </li> <li><strong>view augmentation</strong> → IBOT relies on augmaentation views as common in <em>visual self supervised learning</em> (BERT does not use any augmentation)</li> </ul> <p><strong>FROM BYOL TO IBOT:</strong> Compared to <strong>BYOL</strong>, another student-teacher model,</p> <ul> <li><strong>Approach</strong> <ul> <li>BYOL focuses exclusively on matching global representations between two views <ul> <li><em><strong>iBOT introduces also masked token prediction.</strong></em></li> </ul> </li> </ul> </li> <li><strong>dual-level learning Impact</strong> → operating on both patch and <code class="language-plaintext highlighter-rouge">[CLS]</code> tokens <ul> <li>forces the model to learn explicit <strong>local correspondences</strong> and spatial reasoning, a learning signal absent in BYOL (learn only global representation/feature).</li> <li>allows iBOT to build a richer, more hierarchical understanding</li> </ul> </li> <li><strong>source of asymmetry</strong> <ul> <li>in BYOL, it comes from an added predictor head</li> <li>in iBOT it stems from the masked input to the student and the dual-objective loss</li> </ul> </li> </ul> <h4 id="34-the-strategic-importance-of-dual-token-representations">3.4 The Strategic Importance of Dual Token Representations</h4> <p>Maintaining separate representations for patch-level tokens and a global <code class="language-plaintext highlighter-rouge">[CLS]</code> token provides significant practical advantages:</p> <ul> <li><strong>Task Flexibility:</strong> The <code class="language-plaintext highlighter-rouge">[CLS]</code> token provides a ready-made global feature vector ideal <em><strong>for image classification</strong></em>, while the patch tokens can be directly used for dense prediction tasks like <em><strong>semantic segmentation or object detection</strong></em>.</li> </ul> <hr/> <div class="notion-divider">✠</div> <h3 id="40-preventing-mode-collapse-with-centering-todo">4.0 Preventing Mode Collapse with Centering (TODO)</h3> <p><strong>CHALLENGE</strong> A critical failure mode in self-distillation models is <strong>mode collapse</strong>, where the network learns a trivial solution by mapping all inputs to the same constant output.</p> <ul> <li><strong>CAUSE</strong> This occurs because of a reinforcing feedback loop: if the teacher develops a slight bias towards one output dimension, the student learns to match it to minimize the loss. The teacher, being an EMA of the student, then inherits and strengthens this bias, leading to a complete collapse.</li> </ul> <p><strong>SOLUTION</strong> iBOT prevents this with a <strong>centering</strong> operation applied to the teacher’s outputs before the softmax function.</p> <p><code class="language-plaintext highlighter-rouge">zt = softmax((ht(ft(x)) − c) / τ)</code></p> <p>The center <code class="language-plaintext highlighter-rouge">c</code> is a running vector updated via an exponential moving average of the teacher’s outputs from each batch:</p> <p><code class="language-plaintext highlighter-rouge">c ← λ · c + (1 − λ) · Ebatch[ht(ft(x))]</code></p> <p>This simple operation has a profound effect. At equilibrium, when the training process reaches a stationary distribution, the center vector <code class="language-plaintext highlighter-rouge">c*</code> can be shown to converge to the mean of the teacher’s outputs over the entire dataset.</p> <p><strong>Proof of Equilibrium:</strong> At equilibrium, <code class="language-plaintext highlighter-rouge">c*</code> does not change:</p> <p><code class="language-plaintext highlighter-rouge">c* = λ · c* + (1 − λ) · E[ht(ft(x))]</code> <code class="language-plaintext highlighter-rouge">c* - λ · c* = (1 - λ) · E[ht(ft(x))]</code> <code class="language-plaintext highlighter-rouge">(1 - λ)c* = (1 - λ) · E[ht(ft(x))]</code> <code class="language-plaintext highlighter-rouge">c* = E[ht(ft(x))]</code></p> <p>Therefore, the centered outputs, <code class="language-plaintext highlighter-rouge">ht(ft(x)) - c*</code>, are guaranteed to have a <strong>zero mean</strong> over the dataset. This prevents any single output dimension from consistently dominating across all samples, effectively breaking the feedback loop and preventing mode collapse.</p> <p>We now briefly turn to DINO, the final model in our overview, which provides important context for iBOT’s self-distillation mechanism.</p> <div class="notion-callout notion-callout--gray"> <div class="notion-callout-icon">💡</div> <div class="notion-callout-body"> <p>ADD THAT IN SOME CASE YOU FIX VIA TEMEPERATURE SEE SLIDES</p> </div> </div> <hr/> <div class="notion-divider">✠</div> <div class="notion-divider">✠</div> <hr/> <h2 id="b-dino-and-dinov2-knowledge-distillation-with-no-labels">B. DINO and DINOv2: Knowledge Distillation with No Labels</h2> <p>DINO, which stands for knowledge <strong>DI</strong>stillation with <strong>NO</strong> labels, is another landmark method in self-supervised learning and a direct conceptual predecessor to iBOT’s distillation component.</p> <ul> <li><strong>Like iBOT</strong>, it leverages a student-teacher framework with teacher as EMA of the student</li> <li><strong>The core idea</strong> is to learn representations that are <em><strong>invariant to data augmentations</strong></em></li> </ul> <h3 id="1-from-byol-to-dino">1. From BYOL to DINO</h3> <div class="notion-callout notion-callout--gray"> <div class="notion-callout-icon">💡</div> <div class="notion-callout-body"> <p>NB this is <em><strong>not contrastive</strong></em> as it does not explicitly defines <strong>positive pairs</strong> <em>and</em> <strong>negative pairs</strong></p> </div> </div> <p><strong>BOOTSTRAPPING</strong> is the idea of using a model own prediction as targets to allow to learn a better model.</p> <p><strong>BYOL IDEA</strong> learns representations by training a network to predict a <em>stable, time-delayed representation</em> of another augmented view of the same image, so that minimizing the loss forces invariance to data augmentations rather than reliance on labels or negatives.</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">student</span><span class="p">:</span>   <span class="n">encoder</span> <span class="err">→</span> <span class="n">projector</span> <span class="err">→</span> <span class="n">predictor</span> <span class="err">→</span> <span class="n">prediction</span>
<span class="n">target</span><span class="p">:</span>    <span class="n">encoder</span> <span class="err">→</span> <span class="n">projector</span> <span class="err">→</span> <span class="n">representation</span>
</code></pre></div></div> <p><img src="/assets/img/posts/notes-on-vision-models/notion-3bc815d2904b801f98c3f5cfaa5f78ec.png" alt="notes-on-vision-models"/></p> <p><strong>BYOL LOSS</strong> as seen above is simply the the cosine between normal image from <em>student predictor</em> and augmented image from EMA</p> <p><strong>CHALLENGE</strong> is mode collapse. If two network are identical they maximize the cosine similarity so they are <em>optimal</em>. They can be identical simply outputting a constant vector. This is called <strong>mode collapse</strong>.</p> <blockquote> <p><em><strong>BYOL doesn’t prevent collapse by saying “don’t collapse” — it prevents collapse by making collapse an unstable solution to a prediction problem.</strong></em></p> </blockquote> <details class="notion-toggle"> <summary><strong>SOLUTION</strong> the solution relies on two key ingredients: <em><strong>EMA</strong></em> and <strong>PREDICTION HEAD</strong> make more constant output a non-stable solution </summary> <ul> <li> <p>without prediction head constant output are stable optima <img src="/assets/img/posts/notes-on-vision-models/notion-3bc815d2904b809c95aed96514455484.png" alt="notes-on-vision-models"/></p> </li> <li> <p>with ema is not enough <img src="/assets/img/posts/notes-on-vision-models/notion-3bc815d2904b80bcb4e4e307f4accf15.png" alt="notes-on-vision-models"/></p> </li> <li> <p>but with ema + prediction constant is not anymore a stable solution <img src="/assets/img/posts/notes-on-vision-models/notion-3bc815d2904b800b9d6edae171c8473d.png" alt="notes-on-vision-models"/></p> </li> </ul> </details> <p><strong>TYPE OF COLLAPSE IN DINOV1</strong> As DINOV1 trains via a cross-entropy loss on a probability distribution divergence between teacher and student, distribution obtained from projecting CLS on a list of prototsypes (linear + softmax), there are two types of possible collapse. We define collapse learning a probability distribution that achieve a small loss but it is unrelated to the actual input:</p> <ul> <li><em><strong>uniform collapse</strong></em> → happens when the student and the teacher start getting both a uniform distribution. This is a rolling stones, that make at each steps each other more uniform <ul> <li><strong>solution → temperature of softmax,</strong> allows you to get more sharp distribution. <ul> <li>if now we have a uniform distribution, we just make a big difference from small numerical difference, which means that the distribution is highly unstable. Therefore, we are forced to commit instead of staying uniform</li> </ul> </li> </ul> </li> <li><strong>mode collapse</strong> → we put all mass only on one item. Also this is a rolling stone.</li> <li><strong>solution → teacher logit mean centering</strong> Just change the teacher output by keeping an ema of the mean of the logits. In this way, if the logit start to grow steadily, going towards mode collapse, also centering grows and situtation is solved <ul> <li> \[f_t(x)= f_t(x) - c\] </li> </ul> </li> </ul> <hr/> <div class="notion-divider">✠</div> <h3 id="2-from-byol-to-dino-from-mviola--n8">2. From BYOL to DINO (from M.Viola + N8)</h3> <p><strong>SIMILARITIES</strong> Like BYOL Dino has <strong>student–teacher framework</strong> consisting of two networks: a student that is actively trained and a teacher that is not directly optimised but updated through an exponential moving average of the student’s parameters.</p> <ul> <li><em>DATA AUGMENTATION</em> DINO follows the same set of data augmentations of BYOL, i.e., a mix of horizontal flips, color jittering, grayscale conversion, Gaussian blur, and solarization.</li> </ul> <p><strong>DIFFERENCES</strong> DINO still innovate the architecture quite a bit:</p> <ul> <li><em>SIMPLIFIED ARCHITECTURE</em> it removes the need for an extra prediction head, resulting in the same design for student and teacher networks: <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code>  <span class="n">student</span><span class="p">:</span>   <span class="n">encoder</span> <span class="err">→</span> <span class="n">projector</span> <span class="err">→</span> <span class="n">representation</span> <span class="n">predictor</span> <span class="err">→</span> <span class="n">prediction</span>
  <span class="n">target</span><span class="p">:</span>    <span class="n">encoder</span> <span class="err">→</span> <span class="n">projector</span> <span class="err">→</span> <span class="n">representation</span>
</code></pre></div> </div> <ul> <li>Now the <strong>architecture</strong> is image encoder (VIT) + MLP (projector)</li> </ul> <p><img src="/assets/img/posts/notes-on-vision-models/notion-3bc815d2904b807cb074c64e4d1e428c.png" alt="notes-on-vision-models"/></p> </li> <li><em>DIFFERENT LOSS</em> DINO uses cross-entropy to align the student and teacher output distribution instead of mean squared error or cosine similarity</li> <li><em>DIFFERENT INPUT HANDLING</em> DINO follows a new <strong>multi-crop strategy</strong>: first, it generates two large crops (<strong>global</strong>, covering &gt;50% of the image) and several smaller ones (<strong>local</strong>, &lt;50%) from the same image. <ul> <li>All crops are passed through the student network, but only the two global crops are processed by the teacher, encouraging what the authors call <em><strong>“local-to-global”</strong></em> correspondence.</li> <li>Output matching with cross-entropy happens between all valid pairs of crops, excluding identical view pairs <ul> <li><em><strong>NB</strong></em> [CLS] tokens are used for comparison of a given image crop with another</li> </ul> </li> </ul> </li> </ul> <blockquote> <p><em><strong>If it removes prediction, how does it avoid mode collapse?</strong></em></p> </blockquote> <div class="notion-callout notion-callout--red"> <div class="notion-callout-icon">🚨</div> <div class="notion-callout-body"> <p><strong>AVOIDING MODE COLLAPSE →</strong> The always-present danger of representation collapse is prevented using two tricks: <strong>centering</strong> the teacher output by subtracting the mean computed over the batch and <strong>sharpening</strong> by dividing the sotfmax logits by a temperature T &lt; 1.</p> </div> </div> <hr/> <div class="notion-divider">✠</div> <h3 id="3-from-dino-to-ibot">3. From DINO to iBOT</h3> <p>While we introduce iBOT before DINO, it is its follow up. <strong>DINO</strong> adapted this philosophy for Vision Transformers by replacing the predictor with centering and sharpening operations on the teacher’s outputs, which proved particularly effective at producing semantic visual features and attention maps. <strong>iBOT</strong> then extended DINO’s image-level self-distillation by <em><strong>adding a masked image modeling objective at the patch level</strong></em>, combining global semantic learning with fine-grained local feature prediction in a unified framework.</p> <ul> <li>so it changed the method of DINO of producing <em><strong>local-to-global matching</strong></em> to masked matching</li> <li>DINO’s <em><strong>centering/sharpening mechanism</strong></em> (instead of a predictor) produces richer semantic structure especially in ViTs <ul> <li>e.g., yielding <em><strong>attention maps that naturally segment objects</strong></em> without any supervision—making features more interpretable and semantically meaningful.</li> </ul> </li> </ul> <hr/> <div class="notion-divider">✠</div> <h3 id="4-dinov2">4. DINOV2</h3> <h4 id="41-from-ibol-to-dinov2">4.1 From iBOL to DINOV2</h4> <p><strong>DINOv2</strong> scales up iBOT’s to create a robust, general-purpose foundation model approach with:</p> <ul> <li>a carefully curated 142M image dataset</li> <li>longer training</li> <li>architectural refinements</li> </ul> <h4 id="42-dinov2-innovations">4.2 DINOV2 Innovations</h4> <p><strong>DATASET</strong> DINOv2 made the choice to prioritize quality over quantity: it builds its own curated dataset, named LVD-142M, starting from an initial set of ~1.2B web images. The pipeline:</p> <ul> <li>build <strong>high quality dataset</strong> using curated sources (e.g., imagenet 1k)</li> <li><strong>filter near-duplicate images</strong> with a copy detection pipeline</li> <li>retrieve similar image (so high quality) from bigger dataset with selecting the k-nearest neigh (k=4)</li> </ul> <p><img src="/assets/img/posts/notes-on-vision-models/notion-3bc815d2904b8056b5a9c164dc5bf36e.png" alt="notes-on-vision-models"/></p> <p><strong>TRAINING</strong> is a combination of loss and desiderata:</p> <ul> <li><em>global and view-invariant reprsentation →</em> use <strong>cross-entropy comparison as in DINO</strong>, where student and teacher compare [CLS] token features from different crops of the same image.</li> <li><em>local-information →</em> use the iBOT prediction of masked patches</li> <li><em>increase pixel level precision →</em> use mixed resolution (namely pass e.g. to some 518x518 high resolution compared to 256x256 standard). This boost the final granularity of the local features and increase performance on dense task such as segmentation</li> </ul> <p><strong>ARCHITECTURE</strong> Also here there are some difference</p> <ul> <li><em>mixed MLP →</em> A trick used is to use different head from the output of the encoder. Also DINO used a projection head, but now we have different head for CLS and local patches, to avoid interference</li> <li><em>Improved centering and feature regularization</em> → the original teacher centering + sharpening is replaced by a special batch normalization (<code class="language-plaintext highlighter-rouge">Sinkhorn-Knopp centering</code>). Furthermore, a regularizer that encourages features to be uniformly spread out on the unit sphere is added (KoLeo). This acts on the [CLS] projections of the first global crops within the batch, representative enough to enforce feature spread.</li> </ul> <p><strong>LOSS</strong> This is the final loss that is been optimized</p> <p><img src="/assets/img/posts/notes-on-vision-models/notion-3bc815d2904b8038a97fd203e1413382.png" alt="notes-on-vision-models"/></p> <p><strong>KNOWLEDGE DISTILLATION</strong> Rather than training different version from scratch, train firstly the biggest and use it as teacher:</p> <p><img src="/assets/img/posts/notes-on-vision-models/notion-3bc815d2904b80c3ae6ddbb3f87f4907.png" alt="notes-on-vision-models"/></p> <p><strong>VISION REGISTERY</strong> this are some tokens add to the input to capture global info on top of the CLS</p> <p><img src="/assets/img/posts/notes-on-vision-models/notion-3bc815d2904b804382e5ea5f0042bd7f.png" alt="notes-on-vision-models"/></p> <p>Indeed, once we train too much we still improve accuracy on sparse info task as classification on imagenet. But our performance decreases dramatically on dense task such as segmentation. This is because some patches hidden representation becomes very high-norm as full of information. This normally are useless background patches that transformer smartly use as blank spaces where to write global informations to carry complex reasoning</p> <p><img src="/assets/img/posts/notes-on-vision-models/notion-3bc815d2904b801d893bf6d076f24a01.png" alt="notes-on-vision-models"/></p> <h4 id="43-dinov2-experiments">4.3 DINOV2 Experiments</h4> <p><img src="/assets/img/posts/notes-on-vision-models/notion-3bc815d2904b80c3b37af0723309284d.png" alt="notes-on-vision-models"/></p> <p><strong>LOSSES COMPARISON:</strong></p> <ul> <li><em><strong>DINO</strong></em> loss (therefore CE between EMA and teacher for crops) dominates showing self-distillation is the primary learning mechanism</li> <li><em><strong>iBOT</strong></em>’s rapid decay suggests masked modeling only helps bootstrap initial features</li> <li><em><strong>KoLeo</strong></em>’s negligible impact means the architecture inherently resists collapse.</li> </ul> <p><strong>MODE COLLAPSE</strong> both CLS and patch tokens maintain high diversity (0.64) far above collapse threshold. Identical diversity levels indicate well-balanced global/local representations with no feature degeneration.</p> <p><strong>PATCH VS CLS</strong> patch features converge way faster than CLS tokens. Local patterns are easier to learn; global semantic understanding requires sustained refinement</p> <h4 id="44-dinov2-core-code">4.4 DINOV2 Core Code</h4> <p>Teacher Init</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">self</span><span class="p">.</span><span class="n">student</span> <span class="o">=</span> <span class="nc">TinyViT</span><span class="p">(</span><span class="o">**</span><span class="n">model_config</span><span class="p">).</span><span class="nf">to</span><span class="p">(</span><span class="n">device</span><span class="p">)</span>
<span class="n">self</span><span class="p">.</span><span class="n">teacher</span> <span class="o">=</span> <span class="nc">TinyViT</span><span class="p">(</span><span class="o">**</span><span class="n">model_config</span><span class="p">).</span><span class="nf">to</span><span class="p">(</span><span class="n">device</span><span class="p">)</span>

<span class="c1"># 1. Load the same init of studen
</span><span class="n">self</span><span class="p">.</span><span class="n">teacher</span><span class="p">.</span><span class="nf">load_state_dict</span><span class="p">(</span><span class="n">self</span><span class="p">.</span><span class="n">student</span><span class="p">.</span><span class="nf">state_dict</span><span class="p">())</span>

<span class="c1"># 2. Teacher doesn't need gradients
</span><span class="k">for</span> <span class="n">p</span> <span class="ow">in</span> <span class="n">self</span><span class="p">.</span><span class="n">teacher</span><span class="p">.</span><span class="nf">parameters</span><span class="p">():</span>
    <span class="n">p</span><span class="p">.</span><span class="n">requires_grad</span> <span class="o">=</span> <span class="bp">False</span>
    
<span class="c1"># 3. Teacher is in eval mode 
</span><span class="n">self</span><span class="p">.</span><span class="n">teacher</span><span class="p">.</span><span class="nf">eval</span><span class="p">()</span>
</code></pre></div></div> <p>Teacher Update</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nd">@torch.no_grad</span><span class="p">()</span>
<span class="k">def</span> <span class="nf">update_teacher</span><span class="p">(</span><span class="n">self</span><span class="p">):</span>
	<span class="sh">"""</span><span class="s">EMA update of teacher weights</span><span class="sh">"""</span>
	<span class="k">for</span> <span class="n">param_s</span><span class="p">,</span> <span class="n">param_t</span> <span class="ow">in</span> <span class="nf">zip</span><span class="p">(</span><span class="n">self</span><span class="p">.</span><span class="n">student</span><span class="p">.</span><span class="nf">parameters</span><span class="p">(),</span> <span class="n">self</span><span class="p">.</span><span class="n">teacher</span><span class="p">.</span><span class="nf">parameters</span><span class="p">()):</span>
	    <span class="n">param_t</span><span class="p">.</span><span class="n">data</span><span class="p">.</span><span class="nf">mul_</span><span class="p">(</span><span class="n">self</span><span class="p">.</span><span class="n">momentum</span><span class="p">).</span><span class="nf">add_</span><span class="p">(</span><span class="n">param_s</span><span class="p">.</span><span class="n">data</span><span class="p">,</span> <span class="n">alpha</span><span class="o">=</span><span class="mi">1</span> <span class="o">-</span> <span class="n">self</span><span class="p">.</span><span class="n">momentum</span><span class="p">)</span>
	
</code></pre></div></div> <p><em><strong>Centering is done only for the CLS output of the teacher</strong></em></p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">update_center</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">teacher_cls</span><span class="p">):</span>
  <span class="sh">"""</span><span class="s">Update center for teacher CLS outputs only (DINOv2 change)</span><span class="sh">"""</span>
  <span class="c1"># EMA update
</span>  <span class="n">cls_center</span> <span class="o">=</span> <span class="n">teacher_cls</span><span class="p">.</span><span class="nf">mean</span><span class="p">(</span><span class="n">dim</span><span class="o">=</span><span class="mi">0</span><span class="p">,</span> <span class="n">keepdim</span><span class="o">=</span><span class="bp">True</span><span class="p">)</span>
  <span class="n">self</span><span class="p">.</span><span class="n">center_cls</span> <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="n">center_cls</span> <span class="o">*</span> <span class="n">self</span><span class="p">.</span><span class="n">center_momentum</span> <span class="o">+</span> <span class="n">cls_center</span> <span class="o">*</span> <span class="p">(</span><span class="mi">1</span> <span class="o">-</span> <span class="n">self</span><span class="p">.</span><span class="n">center_momentum</span><span class="p">)</span>
</code></pre></div></div> <p>iBOT masking → <em><strong>an important details is that masking is applied only in the student forward pass and only on one of the two global crop (and none for the locals)</strong></em></p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">generate_mask</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">batch_size</span><span class="p">):</span>
	<span class="sh">"""</span><span class="s">Generate random mask for iBOT (masked image modeling)</span><span class="sh">"""</span>
	
	<span class="n">num_masked</span> <span class="o">=</span> <span class="nf">int</span><span class="p">(</span><span class="n">self</span><span class="p">.</span><span class="n">ibot_mask_ratio</span> <span class="o">*</span> <span class="n">self</span><span class="p">.</span><span class="n">num_patches</span><span class="p">)</span>
	<span class="n">mask</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">zeros</span><span class="p">(</span><span class="n">batch_size</span><span class="p">,</span> <span class="n">self</span><span class="p">.</span><span class="n">num_patches</span><span class="p">,</span> <span class="n">dtype</span><span class="o">=</span><span class="n">torch</span><span class="p">.</span><span class="nb">bool</span><span class="p">,</span> <span class="n">device</span><span class="o">=</span><span class="n">device</span><span class="p">)</span>
	
	<span class="k">for</span> <span class="n">i</span> <span class="ow">in</span> <span class="nf">range</span><span class="p">(</span><span class="n">batch_size</span><span class="p">):</span>
	    <span class="c1"># Random masking for each sample
</span>	    <span class="n">masked_indices</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">randperm</span><span class="p">(</span><span class="n">self</span><span class="p">.</span><span class="n">num_patches</span><span class="p">)[:</span><span class="n">num_masked</span><span class="p">]</span>
	    <span class="n">mask</span><span class="p">[</span><span class="n">i</span><span class="p">,</span> <span class="n">masked_indices</span><span class="p">]</span> <span class="o">=</span> <span class="bp">True</span>
	
	<span class="k">return</span> <span class="n">mask</span>
</code></pre></div></div> <p>DINO Losses</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">dino_loss</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">student_output</span><span class="p">,</span> <span class="n">teacher_output</span><span class="p">):</span>
  <span class="sh">"""</span><span class="s">
  Stable cross-entropy for DINO:
  - student_output: B x D
  - teacher_output: B x D 
  ----
  return scores B 
  </span><span class="sh">"""</span>
  <span class="c1"># get prob distribution and log prob
</span>  <span class="n">student_out</span> <span class="o">=</span> <span class="n">F</span><span class="p">.</span><span class="nf">log_softmax</span><span class="p">(</span><span class="n">student_output</span><span class="p">,</span> <span class="n">dim</span><span class="o">=-</span><span class="mi">1</span><span class="p">)</span>
  <span class="n">teacher_out</span> <span class="o">=</span> <span class="n">F</span><span class="p">.</span><span class="nf">softmax</span><span class="p">(</span><span class="n">teacher_output</span><span class="p">,</span> <span class="n">dim</span><span class="o">=-</span><span class="mi">1</span><span class="p">)</span>
  <span class="c1"># compute ce for each batch and take mean accross batches
</span>  <span class="k">return</span> <span class="o">-</span><span class="p">(</span><span class="n">teacher_out</span> <span class="o">*</span> <span class="n">student_out</span><span class="p">).</span><span class="nf">sum</span><span class="p">(</span><span class="n">dim</span><span class="o">=-</span><span class="mi">1</span><span class="p">).</span><span class="nf">mean</span><span class="p">()</span>
  
<span class="c1"># inside forward -&gt; skip first global where you apply iBOT loss
</span><span class="k">if</span> <span class="n">not_first_global</span><span class="p">:</span>
	<span class="n">loss</span> <span class="o">+=</span> <span class="nf">dino_loss</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">student_cls</span><span class="p">,</span> <span class="n">teacher_cls</span><span class="p">)</span>
	<span class="c1"># treat patches as batch
</span>	<span class="n">loss</span> <span class="o">+=</span> <span class="nf">dino_loss</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">student_local</span><span class="p">.</span><span class="nf">reshape</span><span class="p">(</span><span class="o">-</span><span class="mi">1</span><span class="p">,</span> <span class="n">hidden_dim</span><span class="p">),</span><span class="n">teacher_local</span><span class="p">.</span><span class="nf">reshape</span><span class="p">(</span><span class="o">-</span><span class="mi">1</span><span class="p">,</span> <span class="n">hidden_dim</span><span class="p">))</span>
</code></pre></div></div> <p>iBOT Loss is literally dino → NB how this is applied to each patch and the patched image is treated as a batch dimension</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code> <span class="k">if</span> <span class="n">self</span><span class="p">.</span><span class="n">ibot_weight</span> <span class="o">&gt;</span> <span class="mi">0</span> <span class="ow">and</span> <span class="n">ibot_mask</span> <span class="ow">is</span> <span class="ow">not</span> <span class="bp">None</span><span class="p">:</span>
    <span class="c1"># Extract masked patch predictions from first global crop
</span>    <span class="c1"># Student: predictions at masked positions
</span>    <span class="n">student_masked</span> <span class="o">=</span> <span class="n">student_patch</span><span class="p">[</span><span class="mi">0</span><span class="p">][</span><span class="n">ibot_mask</span><span class="p">]</span>  <span class="c1"># [num_masked_total, out_dim]
</span>    
    <span class="c1"># Teacher: ground truth at masked positions
</span>    <span class="n">teacher_masked</span> <span class="o">=</span> <span class="n">teacher_patch</span><span class="p">[</span><span class="mi">0</span><span class="p">][</span><span class="n">ibot_mask</span><span class="p">]</span>  <span class="c1"># [num_masked_total, out_dim]
</span>    
    <span class="c1"># Temperature scaling for iBOT
</span>    <span class="n">student_masked</span> <span class="o">=</span> <span class="n">student_masked</span> <span class="o">/</span> <span class="n">self</span><span class="p">.</span><span class="n">student_temp</span>
    <span class="n">teacher_masked</span> <span class="o">=</span> <span class="n">teacher_masked</span> <span class="o">/</span> <span class="n">current_temp</span>
    
    <span class="c1"># Compute loss only on masked positions
</span>    <span class="n">loss_ibot</span> <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="nf">dino_loss</span><span class="p">(</span><span class="n">student_masked</span><span class="p">,</span> <span class="n">teacher_masked</span><span class="p">)</span>
</code></pre></div></div>]]></content><author><name></name></author><category term="blog"/><category term="tutorial"/><category term="ml"/><summary type="html"><![CDATA[Some notes I took at EPFL on vision models from C. Bunne’s course. It is still WIP, some references (e.g., for images) are missing.]]></summary></entry><entry><title type="html">Notes on World Models</title><link href="https://tommasomncttn.github.io/blog/2026/notes-on-world-models/" rel="alternate" type="text/html" title="Notes on World Models"/><published>2026-08-14T00:00:00+00:00</published><updated>2026-08-14T00:00:00+00:00</updated><id>https://tommasomncttn.github.io/blog/2026/notes-on-world-models</id><content type="html" xml:base="https://tommasomncttn.github.io/blog/2026/notes-on-world-models/"><![CDATA[<h2 id="a-world-models-l12">A. World Models (L12)</h2> <p>In this paragraph we are going to introduce world models.</p> <ul> <li>World models are <em><strong>generative model</strong></em> that learn a probability distribution over a state space.</li> <li>World models are therefore encouraged to learn <em><strong>rich internal representations</strong></em> of environment.</li> <li>These internal representations allows for predicting future state in down-stream task such as planning and decision-making</li> </ul> <p>NB, in world models <strong>generation means simulating how the world evolves over time:</strong></p> <p><img src="/assets/img/posts/notes-on-world-models/notion-3bc815d2904b8010ac9ac1d2358554a9.png" alt="notes-on-world-models"/></p> <h4 id="mental-models">Mental Models</h4> <p><code class="language-plaintext highlighter-rouge">World models : Mental model = AI Agent : Human Agent</code> While mental model comes from cognitive psychology / philosophy and they are necessarily the same thing as world model, they play for human a similar role than the one played by world models for AI Agent. Indeed, mental model are a simplified model of reality which allow us to quickly:</p> <ul> <li><em>predict</em> future before based on possible actions (”if i do X, then Y will happen”)</li> <li><em>integrate</em> different elements such as perception, memory, and knowledge</li> <li><em>adaptive</em> they are easily update based on new evidence</li> </ul> <blockquote> <p><em><strong>A human world model is not a static library of facts but a flexible and ever-evolving mental construct rooted in perception and memory</strong></em></p> </blockquote> <p>Furthermore, our mental models require a ton of data. A four year-old child has seen more data than an LLM!</p> <hr/> <div class="notion-divider">✠</div> <h3 id="the-first-world-model-dyna">The First World Model (Dyna)</h3> <h4 id="1-reinforcement-learning-context">1. Reinforcement Learning Context</h4> <p>There are two main type of RL Algorithms:</p> <ul> <li><strong>model-free RL</strong> which are for example (<em>Q-Learning, SARSA, Reinforce</em>). They do not learn a transition matrix, therefore they do not learn \(P(S \vert a,s)\). Instead they learn a <em><strong>quality function</strong></em> that tells you how good is either a state (\(V(s)\)) or a state-action pair (\(Q(s,a)\)).For example the idea of Q-Learning is to act with the world and learn time by time this quality function. While we learn we sample whether to act at random or according to them</li> </ul> <p><img src="/assets/img/posts/notes-on-world-models/notion-3bc815d2904b802c8f55d84952af7ab4.png" alt="notes-on-world-models"/></p> <blockquote> <p><em><strong>I don’t know the rules of the game. I just remember which moves worked. So i need to try and learn</strong></em></p> </blockquote> <ul> <li><strong>model-based RL,</strong> for example → DP, Value Iteration, Policy Iteration. You learn a <em><strong>model of the environment</strong></em> \(P(S \vert a,s)\). In Value Iteration for example you use that probability model \(P(S \vert s,a)\) to pick the action that maximize the expected reward based on an optimal value function \(V^*\):</li> </ul> <p><img src="/assets/img/posts/notes-on-world-models/notion-3bc815d2904b8084a738e1ce79d8733d.png" alt="notes-on-world-models"/></p> <blockquote> <p><em><strong>I learned the rule of the game before, so I can think instead of trying. I can play instead of learn</strong></em></p> </blockquote> <h4 id="2-sutton-invented-dyna">2. Sutton Invented Dyna</h4> <p>On the contrary to the two paradigm <strong>DYNA</strong> is both model-based and model-free. Like model-based architecture it has a model of the environment \(M(s,a) \sim P(S \vert s,a)\) which allows to predict next state. But differently from model-based RL <em>it is not learned before and used to sample actions</em>. Instead, it is used to generate fake trajectory, which are used to update Q(s,a) as in Q-Learning. Actions are chosen as in Q-Learning either at random or argmax_a Q(s,a)</p> <blockquote> <p><em><strong>Dyna is a hybrid RL architecture that learns a model of the environment like model-based RL, but uses it to generate simulated experience for model-free updates.</strong></em></p> </blockquote> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">Initialize</span> <span class="nc">Q</span><span class="p">(</span><span class="n">s</span><span class="p">,</span><span class="n">a</span><span class="p">)</span>
<span class="n">Initialize</span> <span class="nc">Model</span><span class="p">(</span><span class="n">s</span><span class="p">,</span><span class="n">a</span><span class="p">)</span>

<span class="n">repeat</span> <span class="k">for</span> <span class="n">each</span> <span class="n">real</span> <span class="n">step</span><span class="p">:</span>
    <span class="n">observe</span> <span class="n">s</span>
    <span class="n">choose</span> <span class="n">a</span> <span class="n">using</span> <span class="n">ε</span><span class="o">-</span><span class="nf">greedy</span><span class="p">(</span><span class="n">Q</span><span class="p">)</span>
    <span class="n">execute</span> <span class="n">a</span> <span class="ow">in</span> <span class="n">real</span> <span class="n">world</span>
    <span class="n">observe</span> <span class="n">r</span><span class="p">,</span> <span class="n">s</span><span class="sh">'</span><span class="s">

    // 1. Real experience update
    Q(s,a) ← Q(s,a) + α [ r + γ max_a</span><span class="sh">'</span> <span class="nc">Q</span><span class="p">(</span><span class="n">s</span><span class="sh">'</span><span class="s">,a</span><span class="sh">'</span><span class="p">)</span> <span class="err">−</span> <span class="nc">Q</span><span class="p">(</span><span class="n">s</span><span class="p">,</span><span class="n">a</span><span class="p">)</span> <span class="p">]</span>

    <span class="o">//</span> <span class="mf">2.</span> <span class="n">Learn</span> <span class="n">the</span> <span class="n">model</span>
    <span class="nc">Model</span><span class="p">(</span><span class="n">s</span><span class="p">,</span><span class="n">a</span><span class="p">)</span> <span class="err">←</span> <span class="p">(</span><span class="n">r</span><span class="p">,</span> <span class="n">s</span><span class="sh">'</span><span class="s">)

    // 3. Planning with fake experience
    repeat N times:
        sample (ŝ, â) from previously seen state–action pairs
        (r̂, ŝ</span><span class="sh">'</span><span class="p">)</span> <span class="err">←</span> <span class="nc">Model</span><span class="p">(</span><span class="n">ŝ</span><span class="p">,</span> <span class="n">â</span><span class="p">)</span>
        <span class="nc">Q</span><span class="p">(</span><span class="n">ŝ</span><span class="p">,</span><span class="n">â</span><span class="p">)</span> <span class="err">←</span> <span class="nc">Q</span><span class="p">(</span><span class="n">ŝ</span><span class="p">,</span><span class="n">â</span><span class="p">)</span> <span class="o">+</span> <span class="n">α</span> <span class="p">[</span> <span class="n">r</span><span class="err">̂</span> <span class="o">+</span> <span class="n">γ</span> <span class="n">max_a</span><span class="sh">'</span><span class="s"> Q(ŝ</span><span class="sh">'</span><span class="p">,</span><span class="n">a</span><span class="sh">'</span><span class="s">) − Q(ŝ,â) ]

    s ← s</span><span class="sh">'</span>

</code></pre></div></div> <p>The learned <em><strong>M(s,a) is a world model</strong></em> for the agent.</p> <h3 id="schmidhuber-invented-world-models">Schmidhuber Invented World Models</h3> <h4 id="core-idea">Core Idea</h4> <p>Schmidhuber propose to organize AI Agents into three fundamental modules:</p> <ul> <li><strong>Vision (V)</strong> elaborate perception. E.g., if vision, maps input images from agent’s sensors to compressed low dimensional latent vectors</li> <li><strong>Memory (M)</strong> it take as input previous memory and current compressed (by V) sensorial information and update the current memory</li> <li><strong>Controller (C)</strong> it decide which action to take based on current compressed sense (V) and memory info (M)</li> </ul> <p><img src="/assets/img/posts/notes-on-world-models/notion-3bc815d2904b80828f73c61d734fbfaf.png" alt="notes-on-world-models"/></p> <p>Let′s outline some terminology for this chapter:</p> <ul> <li><strong>x</strong> → observation, 64 x 64 x 3 image</li> <li><strong>z</strong> → latent code from the V</li> <li><strong>q(z|x)</strong> → encoder distribution from V</li> <li><strong>p(z)</strong> → prior on latent distribution from V (standard normal)</li> <li><strong>p(x|z)</strong> → decoder distribution from V</li> <li><strong>h</strong> → memory outputted by sampling the M</li> <li><strong>a</strong> → action</li> <li><strong>r</strong> → reward</li> </ul> <h4 id="advantage-of-equipping-agent-with-world-model">Advantage of Equipping Agent with World Model</h4> <div class="notion-callout notion-callout--red"> <div class="notion-callout-icon">🚨</div> <div class="notion-callout-body"> <p><strong>Explicit World Modelling</strong> can help convergence of policy as we need to update only the tiny model with the reward signal, which uses already informative representations (V+M) learned via self-supervision.</p> </div> </div> <p>As written above the core benefit is that we use a very tiny controller, rather than a massive one (as normal in DeepRL). So only this tiny guy is trained with signal from RL. This signal is noisy so it was problematic to tune milions of params as in DRL. Instead, we tune the massive guys (V,M) via self-supervised learning</p> <h4 id="usage-of-world-model">Usage of World Model</h4> <p>To train V and M, we collect some history of type \((a_i,x_i)_0^{t=10k}\) by random actions. Now for training:</p> <ul> <li>V → we just need \(X\)</li> <li>M → we create a new dataset by passing X → V(X) = Z. Then we feed to M the current compressed vision \(z_i\), the memory up to know \(h_{i}\), and the action taken \(a_{i}\) and we teach it to predict what is going to be the future compressed sensed V′s output (so M input), that is \(z_{i+1}\)</li> <li>C → we have two usage that share the usage of CMAES for training given the reward signal: <ul> <li>complete dreaming (DOOM Game): we use the collected training history playing at random to train both V,M. After we train the controller by using the future predicted by M as future input of M, creating a circle in the world model of imagined states. Nevertheless, M requires both \(z\) and \(a\) as actions → indeed, we use the a found by the policy (finally, the reward \(r(z)\) is implemented by defualt in openai ennvironment).</li> <li>augmented perception (Cars Game): compared what happen if we trained C in a real world when the input is only z from V, or when we augment it via M. It seems, world model aka memory augmentation helps a lot in the task</li> </ul> </li> </ul> <p>See here the <strong>rollout and training</strong> dynamics:</p> <details class="notion-toggle"> <summary><code class="language-plaintext highlighter-rouge">The code</code> </summary> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># --- Setup ---
</span><span class="n">z_dim</span> <span class="o">=</span> <span class="mi">32</span>
<span class="n">action_dim</span> <span class="o">=</span> <span class="mi">3</span>
<span class="n">hidden_dim</span> <span class="o">=</span> <span class="mi">256</span>

<span class="n">vae</span> <span class="o">=</span> <span class="nc">V</span><span class="p">(</span><span class="n">z_dim</span><span class="o">=</span><span class="n">z_dim</span><span class="p">)</span>
<span class="n">rnn</span> <span class="o">=</span> <span class="nc">M</span><span class="p">(</span><span class="n">z_dim</span><span class="o">=</span><span class="n">z_dim</span><span class="p">,</span> <span class="n">action_dim</span><span class="o">=</span><span class="n">action_dim</span><span class="p">,</span> <span class="n">hidden_size</span><span class="o">=</span><span class="n">hidden_dim</span><span class="p">)</span>
<span class="n">controller</span> <span class="o">=</span> <span class="nc">Controller</span><span class="p">(</span><span class="n">z_dim</span><span class="o">=</span><span class="n">z_dim</span><span class="p">,</span> <span class="n">hidden_size</span><span class="o">=</span><span class="n">hidden_dim</span><span class="p">,</span> <span class="n">action_dim</span><span class="o">=</span><span class="n">action_dim</span><span class="p">)</span>

<span class="c1"># --- Stage 1: Train VAE ---
# Gather 10,000 random rollouts, train VAE to reconstruct frames
# optimizer_vae = torch.optim.Adam(vae.parameters())
# for batch in data:
#    recon, mu, logvar = vae(batch)
#    loss = vae_loss(recon, batch, mu, logvar)
#    loss.backward()
</span>
<span class="c1"># --- Stage 2: Train Memory (RNN) ---
# Use trained VAE to encode all frames into Z. Save (z_t, a_t, z_{t+1}) tuples.
# optimizer_rnn = torch.optim.Adam(rnn.parameters())
# for z_t, a_t, z_next in latent_data:
#    pi, mu, sigma, _ = rnn(z_t, a_t)
#    loss = mdn_loss(pi, mu, sigma, z_next)
#    loss.backward()
</span>
<span class="c1"># --- Stage 3: Train Controller (Evolution) ---
# VAE and RNN are now FROZEN. We only train the Controller's weights.
# Since we can't backprop reward easily through the environment, we use 
# Evolution Strategies (CMA-ES) instead of Gradient Descent.
</span>
<span class="k">def</span> <span class="nf">run_rollout</span><span class="p">(</span><span class="n">controller</span><span class="p">,</span> <span class="n">env</span><span class="p">):</span>
    <span class="n">obs</span> <span class="o">=</span> <span class="n">env</span><span class="p">.</span><span class="nf">reset</span><span class="p">()</span>
    <span class="n">h</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">zeros</span><span class="p">(</span><span class="mi">1</span><span class="p">,</span> <span class="mi">1</span><span class="p">,</span> <span class="n">hidden_dim</span><span class="p">)</span> <span class="c1"># RNN hidden state
</span>    <span class="n">c</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">zeros</span><span class="p">(</span><span class="mi">1</span><span class="p">,</span> <span class="mi">1</span><span class="p">,</span> <span class="n">hidden_dim</span><span class="p">)</span> <span class="c1"># RNN cell state
</span>    
    <span class="n">done</span> <span class="o">=</span> <span class="bp">False</span>
    <span class="n">total_reward</span> <span class="o">=</span> <span class="mi">0</span>
    
    <span class="k">while</span> <span class="ow">not</span> <span class="n">done</span><span class="p">:</span>
        <span class="c1"># 1. Vision: Encode frame
</span>        <span class="n">obs_tensor</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">tensor</span><span class="p">(</span><span class="n">obs</span><span class="p">).</span><span class="nf">permute</span><span class="p">(</span><span class="mi">2</span><span class="p">,</span><span class="mi">0</span><span class="p">,</span><span class="mi">1</span><span class="p">).</span><span class="nf">unsqueeze</span><span class="p">(</span><span class="mi">0</span><span class="p">).</span><span class="nf">float</span><span class="p">()</span> <span class="o">/</span> <span class="mf">255.0</span>
        <span class="k">with</span> <span class="n">torch</span><span class="p">.</span><span class="nf">no_grad</span><span class="p">():</span>
            <span class="n">mu</span><span class="p">,</span> <span class="n">logvar</span> <span class="o">=</span> <span class="n">vae</span><span class="p">.</span><span class="nf">encode</span><span class="p">(</span><span class="n">obs_tensor</span><span class="p">)</span>
            <span class="n">z</span> <span class="o">=</span> <span class="n">vae</span><span class="p">.</span><span class="nf">reparameterize</span><span class="p">(</span><span class="n">mu</span><span class="p">,</span> <span class="n">logvar</span><span class="p">)</span>
            
        <span class="c1"># 2. Control: Decide action based on Vision (z) + Memory (h)
</span>        <span class="k">with</span> <span class="n">torch</span><span class="p">.</span><span class="nf">no_grad</span><span class="p">():</span>
            <span class="n">action</span> <span class="o">=</span> <span class="nf">controller</span><span class="p">(</span><span class="n">z</span><span class="p">,</span> <span class="n">h</span><span class="p">[</span><span class="mi">0</span><span class="p">])</span> <span class="c1"># Pass hidden state h (not c)
</span>            <span class="n">action</span> <span class="o">=</span> <span class="n">action</span><span class="p">.</span><span class="nf">squeeze</span><span class="p">().</span><span class="nf">numpy</span><span class="p">()</span>
            
        <span class="c1"># 3. Environment Step
</span>        <span class="n">obs</span><span class="p">,</span> <span class="n">reward</span><span class="p">,</span> <span class="n">done</span><span class="p">,</span> <span class="n">_</span> <span class="o">=</span> <span class="n">env</span><span class="p">.</span><span class="nf">step</span><span class="p">(</span><span class="n">action</span><span class="p">)</span>
        <span class="n">total_reward</span> <span class="o">+=</span> <span class="n">reward</span>
        
        <span class="c1"># 4. Memory: Update hidden state for next step
</span>        <span class="c1"># RNN predicts next Z (we ignore the prediction, we just want the new h)
</span>        <span class="n">action_tensor</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">tensor</span><span class="p">(</span><span class="n">action</span><span class="p">).</span><span class="nf">view</span><span class="p">(</span><span class="mi">1</span><span class="p">,</span> <span class="mi">1</span><span class="p">,</span> <span class="o">-</span><span class="mi">1</span><span class="p">)</span>
        <span class="n">z_tensor</span> <span class="o">=</span> <span class="n">z</span><span class="p">.</span><span class="nf">view</span><span class="p">(</span><span class="mi">1</span><span class="p">,</span> <span class="mi">1</span><span class="p">,</span> <span class="o">-</span><span class="mi">1</span><span class="p">)</span>
        <span class="k">with</span> <span class="n">torch</span><span class="p">.</span><span class="nf">no_grad</span><span class="p">():</span>
            <span class="n">_</span><span class="p">,</span> <span class="n">_</span><span class="p">,</span> <span class="n">_</span><span class="p">,</span> <span class="p">(</span><span class="n">h</span><span class="p">,</span> <span class="n">c</span><span class="p">)</span> <span class="o">=</span> <span class="nf">rnn</span><span class="p">(</span><span class="n">z_tensor</span><span class="p">,</span> <span class="n">action_tensor</span><span class="p">,</span> <span class="p">(</span><span class="n">h</span><span class="p">,</span> <span class="n">c</span><span class="p">))</span>
            
    <span class="k">return</span> <span class="n">total_reward</span>

<span class="c1"># Evolution loop would repeatedly call run_rollout with different controller weights
</span>
</code></pre></div> </div> </details> <h4 id="vision-module">Vision Module</h4> <p>The V module is a straightforward VAE model.</p> <p><img src="/assets/img/posts/notes-on-world-models/notion-3bc815d2904b80f0a9ebd88225841d2d.png" alt="notes-on-world-models"/></p> <p>It learn to minimize a reconstruction term (e.g., the likelihood that the decoder generate the input data) and it uses a regularizer to force normality (required to sample from z afterwards, and to force the encoder q to actually learn a distribution rather than memorizing)</p> \[L_{VAE} = \mathbb{E}_{q(z|x)}[\log p(x|z)] - D_{KL}(q(z|x)||p(z))\] <p><strong>NB</strong>, if you model the pixel generation of the decoder p(x| μ, σ) as a normal <em><strong>maximizing the log-likelihood is equivalent to minimize the MSE</strong></em></p> <details class="notion-toggle"> <summary><code class="language-plaintext highlighter-rouge">The code</code> </summary> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">class</span> <span class="nc">V</span><span class="p">(</span><span class="n">nn</span><span class="p">.</span><span class="n">Module</span><span class="p">):</span>
    <span class="k">def</span> <span class="nf">__init__</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">img_channels</span><span class="o">=</span><span class="mi">3</span><span class="p">,</span> <span class="n">z_dim</span><span class="o">=</span><span class="mi">32</span><span class="p">):</span>
        <span class="nf">super</span><span class="p">(</span><span class="n">VAE</span><span class="p">,</span> <span class="n">self</span><span class="p">).</span><span class="nf">__init__</span><span class="p">()</span>
        
        <span class="c1"># Encoder
</span>        <span class="n">self</span><span class="p">.</span><span class="n">enc_conv</span> <span class="o">=</span> <span class="n">nn</span><span class="p">.</span><span class="nc">Sequential</span><span class="p">(</span>
            <span class="n">nn</span><span class="p">.</span><span class="nc">Conv2d</span><span class="p">(</span><span class="n">img_channels</span><span class="p">,</span> <span class="mi">32</span><span class="p">,</span> <span class="mi">4</span><span class="p">,</span> <span class="n">stride</span><span class="o">=</span><span class="mi">2</span><span class="p">),</span> <span class="n">nn</span><span class="p">.</span><span class="nc">ReLU</span><span class="p">(),</span>
            <span class="n">nn</span><span class="p">.</span><span class="nc">Conv2d</span><span class="p">(</span><span class="mi">32</span><span class="p">,</span> <span class="mi">64</span><span class="p">,</span> <span class="mi">4</span><span class="p">,</span> <span class="n">stride</span><span class="o">=</span><span class="mi">2</span><span class="p">),</span> <span class="n">nn</span><span class="p">.</span><span class="nc">ReLU</span><span class="p">(),</span>
            <span class="n">nn</span><span class="p">.</span><span class="nc">Conv2d</span><span class="p">(</span><span class="mi">64</span><span class="p">,</span> <span class="mi">128</span><span class="p">,</span> <span class="mi">4</span><span class="p">,</span> <span class="n">stride</span><span class="o">=</span><span class="mi">2</span><span class="p">),</span> <span class="n">nn</span><span class="p">.</span><span class="nc">ReLU</span><span class="p">(),</span>
            <span class="n">nn</span><span class="p">.</span><span class="nc">Conv2d</span><span class="p">(</span><span class="mi">128</span><span class="p">,</span> <span class="mi">256</span><span class="p">,</span> <span class="mi">4</span><span class="p">,</span> <span class="n">stride</span><span class="o">=</span><span class="mi">2</span><span class="p">),</span> <span class="n">nn</span><span class="p">.</span><span class="nc">ReLU</span><span class="p">()</span>
        <span class="p">)</span>
        <span class="n">self</span><span class="p">.</span><span class="n">fc_mu</span> <span class="o">=</span> <span class="n">nn</span><span class="p">.</span><span class="nc">Linear</span><span class="p">(</span><span class="mi">2</span><span class="o">*</span><span class="mi">2</span><span class="o">*</span><span class="mi">256</span><span class="p">,</span> <span class="n">z_dim</span><span class="p">)</span> <span class="c1"># get mean
</span>        <span class="n">self</span><span class="p">.</span><span class="n">fc_logvar</span> <span class="o">=</span> <span class="n">nn</span><span class="p">.</span><span class="nc">Linear</span><span class="p">(</span><span class="mi">2</span><span class="o">*</span><span class="mi">2</span><span class="o">*</span><span class="mi">256</span><span class="p">,</span> <span class="n">z_dim</span><span class="p">)</span> <span class="c1"># get variance
</span>        
        <span class="c1"># Decoder
</span>        <span class="n">self</span><span class="p">.</span><span class="n">dec_fc</span> <span class="o">=</span> <span class="n">nn</span><span class="p">.</span><span class="nc">Linear</span><span class="p">(</span><span class="n">z_dim</span><span class="p">,</span> <span class="mi">2</span><span class="o">*</span><span class="mi">2</span><span class="o">*</span><span class="mi">256</span><span class="p">)</span>
        <span class="n">self</span><span class="p">.</span><span class="n">dec_conv</span> <span class="o">=</span> <span class="n">nn</span><span class="p">.</span><span class="nc">Sequential</span><span class="p">(</span>
            <span class="n">nn</span><span class="p">.</span><span class="nc">ConvTranspose2d</span><span class="p">(</span><span class="mi">256</span><span class="p">,</span> <span class="mi">128</span><span class="p">,</span> <span class="mi">5</span><span class="p">,</span> <span class="n">stride</span><span class="o">=</span><span class="mi">2</span><span class="p">),</span> <span class="n">nn</span><span class="p">.</span><span class="nc">ReLU</span><span class="p">(),</span>
            <span class="n">nn</span><span class="p">.</span><span class="nc">ConvTranspose2d</span><span class="p">(</span><span class="mi">128</span><span class="p">,</span> <span class="mi">64</span><span class="p">,</span> <span class="mi">5</span><span class="p">,</span> <span class="n">stride</span><span class="o">=</span><span class="mi">2</span><span class="p">),</span> <span class="n">nn</span><span class="p">.</span><span class="nc">ReLU</span><span class="p">(),</span>
            <span class="n">nn</span><span class="p">.</span><span class="nc">ConvTranspose2d</span><span class="p">(</span><span class="mi">64</span><span class="p">,</span> <span class="mi">32</span><span class="p">,</span> <span class="mi">6</span><span class="p">,</span> <span class="n">stride</span><span class="o">=</span><span class="mi">2</span><span class="p">),</span> <span class="n">nn</span><span class="p">.</span><span class="nc">ReLU</span><span class="p">(),</span>
            <span class="n">nn</span><span class="p">.</span><span class="nc">ConvTranspose2d</span><span class="p">(</span><span class="mi">32</span><span class="p">,</span> <span class="n">img_channels</span><span class="p">,</span> <span class="mi">6</span><span class="p">,</span> <span class="n">stride</span><span class="o">=</span><span class="mi">2</span><span class="p">),</span> <span class="n">nn</span><span class="p">.</span><span class="nc">Sigmoid</span><span class="p">()</span>
        <span class="p">)</span>

    <span class="k">def</span> <span class="nf">encode</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">x</span><span class="p">):</span>
        <span class="n">h</span> <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="nf">enc_conv</span><span class="p">(</span><span class="n">x</span><span class="p">)</span>
        <span class="n">h</span> <span class="o">=</span> <span class="n">h</span><span class="p">.</span><span class="nf">view</span><span class="p">(</span><span class="n">h</span><span class="p">.</span><span class="nf">size</span><span class="p">(</span><span class="mi">0</span><span class="p">),</span> <span class="o">-</span><span class="mi">1</span><span class="p">)</span>
        <span class="k">return</span> <span class="n">self</span><span class="p">.</span><span class="nf">fc_mu</span><span class="p">(</span><span class="n">h</span><span class="p">),</span> <span class="n">self</span><span class="p">.</span><span class="nf">fc_logvar</span><span class="p">(</span><span class="n">h</span><span class="p">)</span>

    <span class="k">def</span> <span class="nf">reparameterize</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">mu</span><span class="p">,</span> <span class="n">logvar</span><span class="p">):</span>
        <span class="n">std</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">exp</span><span class="p">(</span><span class="mf">0.5</span> <span class="o">*</span> <span class="n">logvar</span><span class="p">)</span>
        <span class="n">eps</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">randn_like</span><span class="p">(</span><span class="n">std</span><span class="p">)</span>
        <span class="k">return</span> <span class="n">mu</span> <span class="o">+</span> <span class="n">eps</span> <span class="o">*</span> <span class="n">std</span> <span class="c1"># sample the encoder 
</span>
    <span class="k">def</span> <span class="nf">decode</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">z</span><span class="p">):</span>
        <span class="n">h</span> <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="nf">dec_fc</span><span class="p">(</span><span class="n">z</span><span class="p">)</span>
        <span class="n">h</span> <span class="o">=</span> <span class="n">h</span><span class="p">.</span><span class="nf">view</span><span class="p">(</span><span class="n">h</span><span class="p">.</span><span class="nf">size</span><span class="p">(</span><span class="mi">0</span><span class="p">),</span> <span class="mi">256</span><span class="p">,</span> <span class="mi">2</span><span class="p">,</span> <span class="mi">2</span><span class="p">)</span>
        <span class="k">return</span> <span class="n">self</span><span class="p">.</span><span class="nf">dec_conv</span><span class="p">(</span><span class="n">h</span><span class="p">)</span>

    <span class="k">def</span> <span class="nf">forward</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">x</span><span class="p">):</span>
        <span class="n">mu</span><span class="p">,</span> <span class="n">logvar</span> <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="nf">encode</span><span class="p">(</span><span class="n">x</span><span class="p">)</span>
        <span class="n">z</span> <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="nf">reparameterize</span><span class="p">(</span><span class="n">mu</span><span class="p">,</span> <span class="n">logvar</span><span class="p">)</span>
        <span class="n">recon_x</span> <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="nf">decode</span><span class="p">(</span><span class="n">z</span><span class="p">)</span>
        <span class="k">return</span> <span class="n">recon_x</span><span class="p">,</span> <span class="n">mu</span><span class="p">,</span> <span class="n">logvar</span> <span class="c1"># return a distribution over the x 
</span>
<span class="c1"># Loss: Reconstruction + KL Divergence
</span><span class="k">def</span> <span class="nf">vae_loss</span><span class="p">(</span><span class="n">recon_x</span><span class="p">,</span> <span class="n">x</span><span class="p">,</span> <span class="n">mu</span><span class="p">,</span> <span class="n">logvar</span><span class="p">):</span>
    <span class="n">BCE</span> <span class="o">=</span> <span class="n">F</span><span class="p">.</span><span class="nf">mse_loss</span><span class="p">(</span><span class="n">recon_x</span><span class="p">,</span> <span class="n">x</span><span class="p">,</span> <span class="n">reduction</span><span class="o">=</span><span class="sh">'</span><span class="s">sum</span><span class="sh">'</span><span class="p">)</span> <span class="c1"># prefered to log-likelihoof
</span>    <span class="n">KLD</span> <span class="o">=</span> <span class="o">-</span><span class="mf">0.5</span> <span class="o">*</span> <span class="n">torch</span><span class="p">.</span><span class="nf">sum</span><span class="p">(</span><span class="mi">1</span> <span class="o">+</span> <span class="n">logvar</span> <span class="o">-</span> <span class="n">mu</span><span class="p">.</span><span class="nf">pow</span><span class="p">(</span><span class="mi">2</span><span class="p">)</span> <span class="o">-</span> <span class="n">logvar</span><span class="p">.</span><span class="nf">exp</span><span class="p">())</span>
    <span class="k">return</span> <span class="n">BCE</span> <span class="o">+</span> <span class="n">KLD</span>

</code></pre></div> </div> </details> <h4 id="memory-module">Memory Module</h4> <p>The memory module is trained via attaching a MDN (<em>mixed density network with</em> <em><strong>5 gaussians</strong></em>) instead of final linear layer to the output of the RNN</p> <p><img src="/assets/img/posts/notes-on-world-models/notion-3bc815d2904b80e98bf2dfdadc1d7ab2.png" alt="notes-on-world-models"/></p> <p>This trick allows us to treat the RNN output as parametrization for a gaussian, and rather than having 1 predicted \(\hat z_{i+1}\) we will have a distribution where to sample many, which <em>is what we want from our generative world models. Therefore we train by minimizing the following function</em></p> \[L = - \log \left( \sum_{k=1}^5 \pi_k \mathcal{N}(z_{t+1} | \mu_k, \sigma_k) \right)\] <p>with π,μ,σ be a projection of RNN(zᵢ) output.</p> <details class="notion-toggle"> <summary><code class="language-plaintext highlighter-rouge">The code</code> </summary> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">class</span> <span class="nc">M</span><span class="p">(</span><span class="n">nn</span><span class="p">.</span><span class="n">Module</span><span class="p">):</span>

    <span class="k">def</span> <span class="nf">__init__</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">z_dim</span><span class="o">=</span><span class="mi">32</span><span class="p">,</span> <span class="n">action_dim</span><span class="o">=</span><span class="mi">3</span><span class="p">,</span> <span class="n">hidden_size</span><span class="o">=</span><span class="mi">256</span><span class="p">,</span> <span class="n">n_gaussians</span><span class="o">=</span><span class="mi">5</span><span class="p">):</span>
        <span class="nf">super</span><span class="p">(</span><span class="n">MDNRNN</span><span class="p">,</span> <span class="n">self</span><span class="p">).</span><span class="nf">__init__</span><span class="p">()</span>
        <span class="c1"># hyperpam
</span>        <span class="n">self</span><span class="p">.</span><span class="n">z_dim</span> <span class="o">=</span> <span class="n">z_dim</span>
        <span class="n">self</span><span class="p">.</span><span class="n">n_gaussians</span> <span class="o">=</span> <span class="n">n_gaussians</span>
        
        <span class="c1"># core network
</span>        <span class="n">self</span><span class="p">.</span><span class="n">lstm</span> <span class="o">=</span> <span class="n">nn</span><span class="p">.</span><span class="nc">LSTM</span><span class="p">(</span><span class="n">z_dim</span> <span class="o">+</span> <span class="n">action_dim</span><span class="p">,</span> <span class="n">hidden_size</span><span class="p">,</span> <span class="n">batch_first</span><span class="o">=</span><span class="bp">True</span><span class="p">)</span>
        
        <span class="c1"># Mixture Density Network Heads -&gt; simply output how may, their mean, and std
</span>        <span class="c1"># not standard -&gt; each feature of output vector is modelled
</span>        <span class="c1"># by 5 gaussian
</span>        <span class="n">self</span><span class="p">.</span><span class="n">fc_pi</span> <span class="o">=</span> <span class="n">nn</span><span class="p">.</span><span class="nc">Linear</span><span class="p">(</span><span class="n">hidden_size</span><span class="p">,</span> <span class="n">n_gaussians</span> <span class="o">*</span> <span class="n">z_dim</span><span class="p">)</span> <span class="c1"># probability that is modelled by gaussian 1,2,3,4,5
</span>        <span class="n">self</span><span class="p">.</span><span class="n">fc_mu</span> <span class="o">=</span> <span class="n">nn</span><span class="p">.</span><span class="nc">Linear</span><span class="p">(</span><span class="n">hidden_size</span><span class="p">,</span> <span class="n">n_gaussians</span> <span class="o">*</span> <span class="n">z_dim</span><span class="p">)</span>
        <span class="n">self</span><span class="p">.</span><span class="n">fc_sigma</span> <span class="o">=</span> <span class="n">nn</span><span class="p">.</span><span class="nc">Linear</span><span class="p">(</span><span class="n">hidden_size</span><span class="p">,</span> <span class="n">n_gaussians</span> <span class="o">*</span> <span class="n">z_dim</span><span class="p">)</span>

    <span class="k">def</span> <span class="nf">forward</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">z</span><span class="p">,</span> <span class="n">action</span><span class="p">,</span> <span class="n">hidden</span><span class="o">=</span><span class="bp">None</span><span class="p">):</span>
        <span class="c1"># Input: Concat current latent z and action a
</span>        <span class="n">x</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">cat</span><span class="p">([</span><span class="n">z</span><span class="p">,</span> <span class="n">action</span><span class="p">],</span> <span class="n">dim</span><span class="o">=-</span><span class="mi">1</span><span class="p">)</span> <span class="c1"># [Batch, Seq, Z+Action]
</span>        <span class="n">output</span><span class="p">,</span> <span class="n">hidden</span> <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="nf">lstm</span><span class="p">(</span><span class="n">x</span><span class="p">,</span> <span class="n">hidden</span><span class="p">)</span>
        
        <span class="c1"># Output parameters for GMM
</span>        <span class="n">pi</span> <span class="o">=</span> <span class="n">F</span><span class="p">.</span><span class="nf">softmax</span><span class="p">(</span><span class="n">self</span><span class="p">.</span><span class="nf">fc_pi</span><span class="p">(</span><span class="n">output</span><span class="p">).</span><span class="nf">view</span><span class="p">(</span><span class="o">-</span><span class="mi">1</span><span class="p">,</span> <span class="n">self</span><span class="p">.</span><span class="n">n_gaussians</span><span class="p">,</span> <span class="n">self</span><span class="p">.</span><span class="n">z_dim</span><span class="p">),</span> <span class="n">dim</span><span class="o">=</span><span class="mi">1</span><span class="p">)</span>
        <span class="n">mu</span> <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="nf">fc_mu</span><span class="p">(</span><span class="n">output</span><span class="p">).</span><span class="nf">view</span><span class="p">(</span><span class="o">-</span><span class="mi">1</span><span class="p">,</span> <span class="n">self</span><span class="p">.</span><span class="n">n_gaussians</span><span class="p">,</span> <span class="n">self</span><span class="p">.</span><span class="n">z_dim</span><span class="p">)</span>
        <span class="n">sigma</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">exp</span><span class="p">(</span><span class="n">self</span><span class="p">.</span><span class="nf">fc_sigma</span><span class="p">(</span><span class="n">output</span><span class="p">)).</span><span class="nf">view</span><span class="p">(</span><span class="o">-</span><span class="mi">1</span><span class="p">,</span> <span class="n">self</span><span class="p">.</span><span class="n">n_gaussians</span><span class="p">,</span> <span class="n">self</span><span class="p">.</span><span class="n">z_dim</span><span class="p">)</span>
        
        <span class="k">return</span> <span class="n">pi</span><span class="p">,</span> <span class="n">mu</span><span class="p">,</span> <span class="n">sigma</span><span class="p">,</span> <span class="n">hidden</span>

<span class="c1"># Loss: Negative Log Likelihood of the true next_z
</span><span class="k">def</span> <span class="nf">mdn_loss</span><span class="p">(</span><span class="n">pi</span><span class="p">,</span> <span class="n">mu</span><span class="p">,</span> <span class="n">sigma</span><span class="p">,</span> <span class="n">next_z</span><span class="p">):</span>
    <span class="c1"># next_z shape: [Batch*Seq, Z_dim] -&gt; Expand for Gaussians
</span>    <span class="n">target</span> <span class="o">=</span> <span class="n">next_z</span><span class="p">.</span><span class="nf">unsqueeze</span><span class="p">(</span><span class="mi">1</span><span class="p">).</span><span class="nf">expand_as</span><span class="p">(</span><span class="n">mu</span><span class="p">)</span>
    
    <span class="c1"># Gaussian Probability Density Function
</span>    <span class="n">prob</span> <span class="o">=</span> <span class="p">(</span><span class="mf">1.0</span> <span class="o">/</span> <span class="p">(</span><span class="n">sigma</span> <span class="o">*</span> <span class="p">(</span><span class="mi">2</span> <span class="o">*</span> <span class="n">torch</span><span class="p">.</span><span class="n">pi</span><span class="p">)</span><span class="o">**</span><span class="mf">0.5</span><span class="p">))</span> <span class="o">*</span> <span class="n">torch</span><span class="p">.</span><span class="nf">exp</span><span class="p">(</span><span class="o">-</span><span class="mf">0.5</span> <span class="o">*</span> <span class="p">((</span><span class="n">target</span> <span class="o">-</span> <span class="n">mu</span><span class="p">)</span> <span class="o">/</span> <span class="n">sigma</span><span class="p">)</span><span class="o">**</span><span class="mi">2</span><span class="p">)</span>
    
    <span class="c1"># Weighted sum over mixture components
</span>    <span class="n">prob</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">sum</span><span class="p">(</span><span class="n">prob</span> <span class="o">*</span> <span class="n">pi</span><span class="p">,</span> <span class="n">dim</span><span class="o">=</span><span class="mi">1</span><span class="p">)</span>
    
    <span class="c1"># Negative Log Likelihood
</span>    <span class="n">nll</span> <span class="o">=</span> <span class="o">-</span><span class="n">torch</span><span class="p">.</span><span class="nf">log</span><span class="p">(</span><span class="n">prob</span> <span class="o">+</span> <span class="mf">1e-8</span><span class="p">)</span>
    <span class="k">return</span> <span class="n">torch</span><span class="p">.</span><span class="nf">mean</span><span class="p">(</span><span class="n">nll</span><span class="p">)</span>

</code></pre></div> </div> </details> <h4 id="controller-module">Controller Module</h4> <p>The idea:</p> <p><img src="/assets/img/posts/notes-on-world-models/notion-3bc815d2904b8030bb20f0a60a55d097.png" alt="notes-on-world-models"/></p> <details class="notion-toggle"> <summary><code class="language-plaintext highlighter-rouge">The code</code> </summary> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">class</span> <span class="nc">Controller</span><span class="p">(</span><span class="n">nn</span><span class="p">.</span><span class="n">Module</span><span class="p">):</span>
    <span class="k">def</span> <span class="nf">__init__</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">z_dim</span><span class="o">=</span><span class="mi">32</span><span class="p">,</span> <span class="n">hidden_size</span><span class="o">=</span><span class="mi">256</span><span class="p">,</span> <span class="n">action_dim</span><span class="o">=</span><span class="mi">3</span><span class="p">):</span>
        <span class="nf">super</span><span class="p">(</span><span class="n">Controller</span><span class="p">,</span> <span class="n">self</span><span class="p">).</span><span class="nf">__init__</span><span class="p">()</span>
        <span class="n">self</span><span class="p">.</span><span class="n">fc</span> <span class="o">=</span> <span class="n">nn</span><span class="p">.</span><span class="nc">Linear</span><span class="p">(</span><span class="n">z_dim</span> <span class="o">+</span> <span class="n">hidden_size</span><span class="p">,</span> <span class="n">action_dim</span><span class="p">)</span>

    <span class="k">def</span> <span class="nf">forward</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">z</span><span class="p">,</span> <span class="n">h</span><span class="p">):</span>
        <span class="c1"># Concatenate Vision (z) and Memory (h)
</span>        <span class="n">inp</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">cat</span><span class="p">([</span><span class="n">z</span><span class="p">,</span> <span class="n">h</span><span class="p">],</span> <span class="n">dim</span><span class="o">=-</span><span class="mi">1</span><span class="p">)</span>
        <span class="k">return</span> <span class="n">torch</span><span class="p">.</span><span class="nf">tanh</span><span class="p">(</span><span class="n">self</span><span class="p">.</span><span class="nf">fc</span><span class="p">(</span><span class="n">inp</span><span class="p">))</span> <span class="c1"># Actions usually in range [-1, 1]
</span>
</code></pre></div> </div> </details> <hr/> <div class="notion-divider">✠</div> <h3 id="type-of-world-models">Type of World Models</h3> <p>In a world model ideally the environment is characterized by two quantities:</p> <p><img src="/assets/img/posts/notes-on-world-models/notion-3bc815d2904b805f9b3ddda239605723.png" alt="notes-on-world-models"/></p> <p>\(T\) maps <em>state of the environment s</em> and <em>action</em> of the controller to new state of the environment. Instead, \(O\) maps state of the environment to observation of the agent.</p> <div class="notion-callout notion-callout--red"> <div class="notion-callout-icon">🚨</div> <div class="notion-callout-body"> <p>In <strong>World Model 2018</strong> the observation are the pixel (so the input of the visionary V routine). Instead, the states are [z, h] the input of the control C, namely memory and compressed input. The memory maps action and state (z) to a new state (z’), being therefore insensible to observations.</p> </div> </div> <div class="notion-columns"> <div class="notion-column" style="flex: 50 1 0%"> <p>These are the 4 main types of world models and their most popular exmaples:</p> </div> <div class="notion-column" style="flex: 50 1 0%"> <p><img src="/assets/img/posts/notes-on-world-models/notion-3bc815d2904b802cbabedaa6838dc2d9.png" alt="notes-on-world-models"/></p> </div> </div> <hr/> <div class="notion-divider">✠</div> <h3 id="implicit-world-model">Implicit world Model</h3> <p><strong>CORE IDEA</strong> Agent′s world model maps from latent observation to latent future observatios. Therefore, it never generate in the same domain of the ambient of the agent (e.g., pixel), but state of this world model are latent vectors of a deep learning aarchitecture. It is called <strong>latent dynamic function:</strong></p> <p><img src="/assets/img/posts/notes-on-world-models/notion-3bc815d2904b8016958fc80be4a0526a.png" alt="notes-on-world-models"/></p> <p><img src="/assets/img/posts/notes-on-world-models/notion-3bc815d2904b80ff8cb4f808eeaf7bd6.png" alt="notes-on-world-models"/></p> <ul> <li>The decoder is useful only for training, and it is never used to actually reconstruct state into the original one (if not for paper viz)</li> <li>Therefore, <em><strong>agent does not reconstruct latent states in observation during planning</strong></em>.</li> </ul> <blockquote> <p><em><strong>Thinking in abstract symbols about the game state, without ever “rendering” a picture of the board</strong></em></p> </blockquote> <h4 id="muzero">MuZero</h4> <p><strong>CORE IDEA</strong> integrates a learned latent dynamics model with Monte Carlo Tree Search. It use this latent dynamic model to imagine future trajectories, and MCTS in the sense that it picks few best actions to create few future trajectories in paralle . At each step/node it use the policy to chose an action, it uses the latent dynamic model to predict the future state, and it applies a reward function on this action and future state. It choose finally the action that has been chosen most time (rather than the one with highest short term reward)</p> <blockquote> <p><em><strong>Use learned latent dynamic for planning (pick best action) at decision time</strong></em></p> </blockquote> <p><img src="/assets/img/posts/notes-on-world-models/notion-3bc815d2904b80a098dffa49639a931f.png" alt="notes-on-world-models"/></p> <p><strong>ARCHITECTURE</strong> MuZero learns three functions:</p> <ol> <li><code class="language-plaintext highlighter-rouge">h</code>a representation function that maps the initial observation to a latent state</li> <li><code class="language-plaintext highlighter-rouge">g</code>a dynamics function that predicts the next latent state</li> <li><code class="language-plaintext highlighter-rouge">f</code>a prediction function that outputs a policy (distribution over action) and a value estimate (expected reward of keep playing from that latent state) from a latent state.</li> </ol> <h4 id="dreamerv3">DreamerV3</h4> <p><strong>CORE IDEA</strong> The core idea of Dreamer is to learn a latent dynamics model of the environment from real experience and then use this model to generate imagined future trajectories entirely in latent space. These imagined trajectories are used to optimize a policy and a value function, allowing the agent to improve its behaviors without interacting with the real environment at every update.</p> <blockquote> <p><em><strong>Learn a latent dynamic, generate a lot of trajectory (before decision time), train your policy on these trajectories</strong></em></p> </blockquote> <p><img src="/assets/img/posts/notes-on-world-models/notion-3bc815d2904b80309f3bd7c92d29e743.png" alt="notes-on-world-models"/></p> <p>(<em>in the image: the encoder is what we train and it has a deterministic hidden output h and a stochastic z. The stochastic is reconstructed into x by the decoder to train the encoder via MSE)</em></p> <p><strong>ARCHITECTURE</strong> The world model in Dreamer is implemented as a <em><strong>recurrent state-space model</strong></em> that combines deterministic and stochastic components. The <strong>d</strong><em><strong>eterministic hidden state captures</strong></em> information that is predictable from past experience, while the <em><strong>stochastic latent variables</strong></em> model uncertainty and variability in the environment.</p> <p><img src="/assets/img/posts/notes-on-world-models/notion-3bc815d2904b80ec89d5dab061352be9.png" alt="notes-on-world-models"/></p> <p><strong>TRAINING</strong> Observations are encoded into latent variables during training, and the recurrent dynamics model predicts how these latents evolve given actions.</p> <p><strong>MUZERO VS DREAMER</strong> MuZero is especially effective in discrete action spaces with well-defined planning horizons, while Dreamer scales more naturally to continuous actions and long-horizon control tasks. Both approaches demonstrate that accurate prediction of raw observations is not necessary for strong performance, as long as the latent dynamics capture the aspects of the environment that matter for decision-making.</p> <hr/> <div class="notion-divider">✠</div> <h2 id="jepa-models-n11">JEPA Models (N11)</h2> <p>While JEPA models are still <em><strong>implicit world models</strong></em>, we will make a deep dive in their structure.</p> <h4 id="i-jepa-core-idea">I-JEPA Core Idea</h4> <p>I-Jepa is a <em><strong>joint embedding predictive architecture</strong></em>. Such architecture learn meaningful representation by trying to match masked representations of a student model with masked representation of a teacher model.</p> <blockquote> <p><em><strong>INTUITION: JEPA predicts abstract representations rather than raw high-dimensional inputs, avoiding modeling irrelevant details</strong></em></p> </blockquote> <p><img src="/assets/img/posts/notes-on-world-models/notion-3bc815d2904b80fe9aa5e2b068600dbb.png" alt="notes-on-world-models"/></p> <div class="notion-callout notion-callout--red"> <div class="notion-callout-icon">🚨</div> <div class="notion-callout-body"> <p>Predictor project student output in joint embedding with teacher and is used to avoid mode collapse as in BYOL, iBOT, DINO…</p> </div> </div> <p>There are a lot of details here:</p> <ul> <li>differently from simple <strong>joint embedding architecture</strong> (see BYOL) or <strong>contrastive methods</strong> (SIMCLR), JEPA do not work with two different version of the same input where difference relies on different augmentation → <em><strong>no explicit look for representation invariance.</strong></em></li> <li>differently from <strong>masked autoencoder,</strong> model performance are not checked by reconstructing pixel image and checking mean squared error. Rather, they MSE is measured between patch-level hidden representation of the predictor and the teacher encoder. So, <em><strong>everything happens in a joint embedding space</strong></em>.</li> </ul> <blockquote> <p><em><strong>INNOVATION: JEPA take student-teacher comparison from contrastive methods and masking from MAEs. It leaves augmentation of contrastive methods and reconstruction via decoder of MAEs</strong></em></p> </blockquote> <p><img src="/assets/img/posts/notes-on-world-models/notion-3bc815d2904b80d0b587dd0648b009c1.png" alt="notes-on-world-models"/></p> <h4 id="i-jepa-tokenization">I-JEPA Tokenization</h4> <p><strong>MASK</strong> The first intuition is that here masks are not the covered but the used part of the image. It is a bit counterintuitive, but these masks are <code class="language-plaintext highlighter-rouge">indices</code> used to select patches from the image.</p> <p><strong>MASKING ALGORITHM</strong> A tricky detail of I-JEPA lies in the way it tokenize the image. The algorithm is:</p> <ol> <li>sample for each image <code class="language-plaintext highlighter-rouge">T</code> masks made by <code class="language-plaintext highlighter-rouge">Kt</code> patches→ _<strong>each mask is a big rectangles (</strong>_much bigger than what we used in MAEs)</li> <li>sample for each image <code class="language-plaintext highlighter-rouge">M</code> masks made by <code class="language-plaintext highlighter-rouge">Kc</code> patches: <ul> <li>each context mask of the M is used to predict each target mask</li> <li>we explicitely never pick patches (masks) already present in the target to avoid dumb tasks</li> </ul> </li> </ol> <p><img src="/assets/img/posts/notes-on-world-models/notion-3bc815d2904b80f0926ecd2da09f1100.png" alt="notes-on-world-models"/></p> <h4 id="i-jepa-forward-pass">I-JEPA Forward Pass</h4> <p>Once we created the masks the image masks we can use it in the forward pass.</p> <blockquote> <p><em><strong>The core intuition is to the context mask patches to predict each of the target mask patches</strong></em></p> </blockquote> <p>The forward pass follow this logic:</p> <ul> <li>select patches via context encoder masking</li> <li>pass through the encoder</li> <li>append position embeddings of the patches of the target masks to the encoder output <ul> <li><em>this is done so that the predictors know which patch predict</em></li> </ul> </li> <li>pass through the predictor to get as output as many vector as target mask patch <ul> <li><em>now we are in the joint embedding shared between the predictor and</em></li> </ul> </li> <li>compute target patches via target encoder</li> <li>compute loss <img src="/assets/img/posts/notes-on-world-models/notion-3bc815d2904b8085813df1635e088085.png" alt="notes-on-world-models"/></li> </ul> <h4 id="i-jepa-training">I-JEPA Training</h4> <p>Differently from what is shown above the loss used by I-JEPA is <code class="language-plaintext highlighter-rouge">smooth-l1</code> rather than <code class="language-plaintext highlighter-rouge">l2</code> loss.</p> <p><img src="/assets/img/posts/notes-on-world-models/notion-3bc815d2904b808d90eace1ae102c507.png" alt="notes-on-world-models"/></p> <p>The context_decoder is clearly not trained by updated via an EMA of the context encoder:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">ema_update</span><span class="p">(</span><span class="n">context_encoder</span><span class="p">,</span> <span class="n">target_encoder</span><span class="p">,</span> <span class="n">beta</span><span class="p">):</span>
    <span class="sh">"""</span><span class="s">
    for each parameter pair (context_param, target_param):
        target_param = beta * target_param + (1 - beta) * context_param
    </span><span class="sh">"""</span>
    <span class="c1"># loop through parameters
</span>    <span class="k">with</span> <span class="n">torch</span><span class="p">.</span><span class="nf">no_grad</span><span class="p">():</span>
        <span class="k">for</span> <span class="n">student_param</span><span class="p">,</span> <span class="n">teacher_param</span> <span class="ow">in</span> <span class="nf">zip</span><span class="p">(</span><span class="n">context_encoder</span><span class="p">.</span><span class="nf">parameters</span><span class="p">(),</span> <span class="n">target_encoder</span><span class="p">.</span><span class="nf">parameters</span><span class="p">()):</span>
            <span class="c1"># we need .data otherwise "target_param = something" will not write in the model
</span>            <span class="c1"># we need .add_ and .mul_ to not create copy in memory
</span>            <span class="n">teacher_param</span><span class="p">.</span><span class="n">data</span><span class="p">.</span><span class="nf">mul_</span><span class="p">(</span><span class="n">other</span><span class="o">=</span><span class="n">beta</span><span class="p">).</span><span class="nf">add_</span><span class="p">(</span><span class="n">other</span><span class="o">=</span><span class="n">student_param</span><span class="p">.</span><span class="n">data</span><span class="p">,</span> <span class="n">alpha</span><span class="o">=</span><span class="mi">1</span> <span class="o">-</span> <span class="n">beta</span><span class="p">)</span>
</code></pre></div></div> <h4 id="i-jepa-code-implementation">I-JEPA Code Implementation</h4> <p>The two core part of the code implementation is useful to understand is the masking code and the forward passes of the encoder_context/_target (same model) and the predictor. Both are ViTs.</p> <details class="notion-toggle"> <summary><code class="language-plaintext highlighter-rouge">MASKING CODE</code></summary> <p>The mask are not bool mask but list of index. Using <em>advanced indexing</em> of torch we can extract relevant patches:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">apply_masks</span><span class="p">(</span><span class="n">x</span><span class="p">,</span> <span class="n">masks</span><span class="p">):</span> <span class="c1"># TODO: add study
</span>    <span class="sh">"""</span><span class="s">
    :param x: tensor of shape [B (batch-size), N (num-patches), D (feature-dim)]
    :param masks: *list* of tensors containing indices of patches in [N] to keep
    </span><span class="sh">"""</span>
    <span class="c1"># len(m) -&gt; number of patches sample for a mask view (context)
</span>    <span class="c1"># len(mask) -&gt; number of masked views created for a single imaged
</span>    <span class="n">all_x</span> <span class="o">=</span> <span class="p">[]</span>
    <span class="k">for</span> <span class="n">m</span> <span class="ow">in</span> <span class="n">masks</span><span class="p">:</span>
        <span class="n">mask_keep</span> <span class="o">=</span> <span class="n">x</span><span class="p">[:,</span><span class="n">m</span><span class="p">,:]</span> <span class="c1"># B, len(m), D
</span>        <span class="n">all_x</span><span class="p">.</span><span class="nf">append</span><span class="p">(</span><span class="n">mask_keep</span><span class="p">)</span> 
    <span class="k">return</span> <span class="n">torch</span><span class="p">.</span><span class="nf">cat</span><span class="p">(</span><span class="n">all_x</span><span class="p">,</span> <span class="n">dim</span><span class="o">=</span><span class="mi">0</span><span class="p">)</span> <span class="c1"># [len(masks) * B, len(m), D]
</span></code></pre></div> </div> <p>To decide what we are going to select for masking we pass to parameters: (i) Area percentage and H/W ratio (describe how much of a fat or tall matrix we pick). Then selecting the mask size is possible solving the following equations (\(H\) and \(W\) are <code class="language-plaintext highlighter-rouge">patch_h</code> and <code class="language-plaintext highlighter-rouge">patch_w</code>, \(h_m\) and \(w_m\) are the mask dimensions to compute):</p> <ul> <li> <p><strong>Area constraint:</strong> The mask should cover a fraction \(s\) of the total area: $$ h_m \cdot w_m = s \cdot H \cdot W</p> <p>$$</p> </li> <li> <p><strong>Aspect ratio constraint:</strong> The mask should have aspect ratio \(r\): \(\frac{w_m}{h_m} = r\)</p> </li> </ul> <p>Then we get the code</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">get_mask</span><span class="p">(</span><span class="n">patch_h</span><span class="p">,</span> <span class="n">patch_w</span><span class="p">,</span> <span class="n">scale</span><span class="p">,</span> <span class="n">aspect_ratio</span><span class="p">):</span>
    <span class="sh">"""</span><span class="s">
    :param patch_h: number of patches along height
    :param patch_w: number of patches along width
    :param scale: scale of the mask with respect to the image (0 to 1)
    :param aspect_ratio: aspect ratio of the mask (width / height)
    :return: height and width of the mask
    </span><span class="sh">"""</span>
    <span class="n">mask_h</span> <span class="o">=</span> <span class="nf">int</span><span class="p">(</span><span class="nf">round</span><span class="p">(</span><span class="n">math</span><span class="p">.</span><span class="nf">sqrt</span><span class="p">(</span><span class="n">scale</span> <span class="o">*</span> <span class="n">patch_h</span> <span class="o">*</span> <span class="n">patch_w</span> <span class="o">/</span> <span class="n">aspect_ratio</span><span class="p">)))</span>
    <span class="n">mask_w</span> <span class="o">=</span> <span class="nf">int</span><span class="p">(</span><span class="nf">round</span><span class="p">(</span><span class="n">math</span><span class="p">.</span><span class="nf">sqrt</span><span class="p">(</span><span class="n">scale</span> <span class="o">*</span> <span class="n">patch_h</span> <span class="o">*</span> <span class="n">patch_w</span> <span class="o">*</span> <span class="n">aspect_ratio</span><span class="p">)))</span>

    <span class="c1"># clamp to valid dimension -&gt; a mask cannot be larger than the image itself, wrong inputting can cause it
</span>    <span class="n">mask_h</span> <span class="o">=</span> <span class="nf">min</span><span class="p">(</span><span class="n">mask_h</span><span class="p">,</span> <span class="n">patch_h</span><span class="p">)</span>
    <span class="n">mask_w</span> <span class="o">=</span> <span class="nf">min</span><span class="p">(</span><span class="n">mask_w</span><span class="p">,</span> <span class="n">patch_w</span><span class="p">)</span>

    <span class="k">return</span> <span class="n">mask_h</span><span class="p">,</span> <span class="n">mask_w</span>
</code></pre></div> </div> <p>The last element that we need is how to sample an image to get this mask. The idea is easy. An image is tokenized in this way, e.g. for an image described by <code class="language-plaintext highlighter-rouge">2x2</code> grid of patches we get patch assembled <code class="language-plaintext highlighter-rouge">h1w1,h1w2,h2w1,h2w2</code> . So once we have the mask height and width (via <code class="language-plaintext highlighter-rouge">get_mask</code>) we sample a valid beginning on <code class="language-plaintext highlighter-rouge">h</code> and <code class="language-plaintext highlighter-rouge">w</code> . NB if we move on the patchified image made as a list of pixels we access the patch at h=j and width=i via: <code class="language-plaintext highlighter-rouge">j*patch_h +i*patch_width</code></p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">get_context</span><span class="p">(</span><span class="n">patch_dim</span><span class="p">,</span> <span class="n">aspect_ratio</span><span class="p">,</span> <span class="n">scale</span><span class="p">,</span> <span class="n">target_patches</span><span class="p">):</span>
    <span class="n">patch_h</span><span class="p">,</span> <span class="n">patch_w</span> <span class="o">=</span> <span class="n">patch_dim</span>
    <span class="n">block_h</span><span class="p">,</span> <span class="n">block_w</span> <span class="o">=</span> <span class="nf">get_mask</span><span class="p">(</span><span class="n">patch_h</span><span class="p">,</span> <span class="n">patch_w</span><span class="p">,</span> <span class="n">scale</span><span class="p">,</span> <span class="n">aspect_ratio</span><span class="p">)</span>

    <span class="c1">#get a random starting patch
</span>    <span class="n">start_patch_h</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">randint</span><span class="p">(</span><span class="mi">0</span><span class="p">,</span> <span class="n">patch_h</span> <span class="o">-</span> <span class="n">block_h</span><span class="o">+</span><span class="mi">1</span><span class="p">,</span> <span class="p">(</span><span class="mi">1</span><span class="p">,)).</span><span class="nf">item</span><span class="p">()</span> 
    <span class="n">start_patch_w</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">randint</span><span class="p">(</span><span class="mi">0</span><span class="p">,</span> <span class="n">patch_w</span> <span class="o">-</span> <span class="n">block_w</span><span class="o">+</span><span class="mi">1</span><span class="p">,</span> <span class="p">(</span><span class="mi">1</span><span class="p">,)).</span><span class="nf">item</span><span class="p">()</span> 
    <span class="n">start_patch</span> <span class="o">=</span> <span class="n">start_patch_h</span> <span class="o">*</span> <span class="n">patch_w</span> <span class="o">+</span> <span class="n">start_patch_w</span>

    <span class="c1">#get the patches in the context_block
</span>    <span class="n">patches</span> <span class="o">=</span> <span class="p">[]</span>
    <span class="k">for</span> <span class="n">i</span> <span class="ow">in</span> <span class="nf">range</span><span class="p">(</span><span class="n">block_h</span><span class="p">):</span>
        <span class="k">for</span> <span class="n">j</span> <span class="ow">in</span> <span class="nf">range</span><span class="p">(</span><span class="n">block_w</span><span class="p">):</span>
            <span class="k">if</span> <span class="n">start_patch</span> <span class="o">+</span> <span class="n">i</span> <span class="o">*</span> <span class="n">patch_w</span> <span class="o">+</span> <span class="n">j</span> <span class="ow">not</span> <span class="ow">in</span> <span class="n">target_patches</span><span class="p">:</span> <span class="c1">#remove the target patches to avoid overlapping
</span>                <span class="n">patches</span><span class="p">.</span><span class="nf">append</span><span class="p">(</span><span class="n">start_patch</span> <span class="o">+</span> <span class="n">i</span> <span class="o">*</span> <span class="n">patch_w</span> <span class="o">+</span> <span class="n">j</span><span class="p">)</span>
    <span class="n">context_patches</span> <span class="o">=</span> <span class="p">[</span><span class="n">torch</span><span class="p">.</span><span class="nf">tensor</span><span class="p">(</span><span class="n">patches</span><span class="p">)]</span>
    <span class="k">return</span> <span class="n">context_patches</span>
</code></pre></div> </div> </details> <details class="notion-toggle"> <summary><code class="language-plaintext highlighter-rouge">FORWARD PASS</code></summary> <p>The forward pass of the encoder is quite intuitive:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">forward</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">x</span><span class="p">,</span> <span class="n">masks</span><span class="o">=</span><span class="bp">None</span><span class="p">):</span> 

        <span class="c1"># -- patchify x
</span>        <span class="n">x</span> <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="nf">patch_embed</span><span class="p">(</span><span class="n">x</span><span class="p">)</span>
        <span class="n">B</span><span class="p">,</span> <span class="n">N</span><span class="p">,</span> <span class="n">D</span> <span class="o">=</span> <span class="n">x</span><span class="p">.</span><span class="n">shape</span>

        <span class="c1"># -- add positional embedding to x
</span>        <span class="n">x</span> <span class="o">=</span> <span class="n">x</span> <span class="o">+</span> <span class="n">self</span><span class="p">.</span><span class="n">pos_embed</span>

        <span class="c1"># -- mask x
</span>        <span class="k">if</span> <span class="n">masks</span> <span class="ow">is</span> <span class="ow">not</span> <span class="bp">None</span><span class="p">:</span>
            <span class="n">x</span> <span class="o">=</span> <span class="nf">apply_masks</span><span class="p">(</span><span class="n">x</span><span class="p">,</span> <span class="n">masks</span><span class="p">)</span>

        <span class="c1"># -- fwd prop
</span>        <span class="k">for</span> <span class="n">i</span><span class="p">,</span> <span class="n">blk</span> <span class="ow">in</span> <span class="nf">enumerate</span><span class="p">(</span><span class="n">self</span><span class="p">.</span><span class="n">blocks</span><span class="p">):</span>
            <span class="n">x</span> <span class="o">=</span> <span class="nf">blk</span><span class="p">(</span><span class="n">x</span><span class="p">)</span>

        <span class="k">if</span> <span class="n">self</span><span class="p">.</span><span class="n">norm</span> <span class="ow">is</span> <span class="ow">not</span> <span class="bp">None</span><span class="p">:</span>
            <span class="n">x</span> <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="nf">norm</span><span class="p">(</span><span class="n">x</span><span class="p">)</span>

        <span class="k">return</span> <span class="n">x</span>

</code></pre></div> </div> <p>The mess comes with the predictor. The core ideas are these:</p> <ul> <li>we need both the masks (list of index of patches) of the student and the teacher</li> <li>positional information, even if used in the context encoder, are now lost. So take the output of the context encoder and add positional embedding that can be read by the predictor</li> <li>now concat after the context_output+pos_embed put <code class="language-plaintext highlighter-rouge">positional_masked_information</code></li> <li><code class="language-plaintext highlighter-rouge">positional_masked_information</code> are a pain to understand but here is the idea: <ul> <li>we want to say to the predictor, before doing its prediction that try to match the context to a given target_patch, which was is position.</li> <li>to say to the predictor, we write this information by concat <code class="language-plaintext highlighter-rouge">positional_masked_information</code> after the output_context + pos_embed</li> <li>what are <code class="language-plaintext highlighter-rouge">positional_masked_information</code> ? learnable masked_tokens, so litterally nn.parameters (we have 1 for each possible patch) + unlernable positional emebdding (sinuisoidal)</li> </ul> </li> <li>after we pass to the predictor (context_output + pos) || positional_masked_information and we slice just the image of the positional_masked_information</li> <li>we project to the same dimension of the target_embed with the final layer of the predictor′s ViT</li> </ul> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">forward</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">x</span><span class="p">,</span> <span class="n">masks_x</span><span class="p">,</span> <span class="n">masks</span><span class="p">):</span>
        <span class="sh">"""</span><span class="s">
        x:
            Output of the context encoder.
            Shape: [B*M, Kc, encoder_dim]
            B  = true batch size
            M  = number of context masks per image
            Kc = number of visible (context) patches per mask

        masks_x:
            List of length M.
            Each element is a 1D tensor of patch indices of len Kc.
            These are the patches that WERE GIVEN to the context encoder.
            Example: masks_x[m] = [1, 5, 6, 9, ...]

        masks:
            List of length T.
            Each element is a 1D tensor of patch indices of en Kt.
            These are the patches to PREDICT.
            Example: masks[t] = [12, 13, 14]
        </span><span class="sh">"""</span>
        <span class="c1"># 1. routine code to avoid error
</span>        <span class="nf">assert </span><span class="p">(</span><span class="n">masks</span> <span class="ow">is</span> <span class="ow">not</span> <span class="bp">None</span><span class="p">)</span> <span class="ow">and</span> <span class="p">(</span><span class="n">masks_x</span> <span class="ow">is</span> <span class="ow">not</span> <span class="bp">None</span><span class="p">),</span> <span class="sh">'</span><span class="s">Cannot run predictor without mask indices</span><span class="sh">'</span>

        <span class="k">if</span> <span class="ow">not</span> <span class="nf">isinstance</span><span class="p">(</span><span class="n">masks_x</span><span class="p">,</span> <span class="nb">list</span><span class="p">):</span>
            <span class="n">masks_x</span> <span class="o">=</span> <span class="p">[</span><span class="n">masks_x</span><span class="p">]</span>

        <span class="k">if</span> <span class="ow">not</span> <span class="nf">isinstance</span><span class="p">(</span><span class="n">masks</span><span class="p">,</span> <span class="nb">list</span><span class="p">):</span>
            <span class="n">masks</span> <span class="o">=</span> <span class="p">[</span><span class="n">masks</span><span class="p">]</span>

        <span class="c1"># 2. context_encoder has shape (B*M, d) -&gt; one row for each mask for each element of the batch
</span>        <span class="n">B</span> <span class="o">=</span> <span class="nf">len</span><span class="p">(</span><span class="n">x</span><span class="p">)</span> <span class="o">//</span> <span class="nf">len</span><span class="p">(</span><span class="n">masks_x</span><span class="p">)</span> <span class="c1"># retrieve original batch-size
</span>
        <span class="c1"># 3.  map from encoder-dim to pedictor-dim
</span>        <span class="n">x</span> <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="nf">predictor_embed</span><span class="p">(</span><span class="n">x</span><span class="p">)</span> <span class="c1"># [B*M, Kc, pred_dim]
</span>
        <span class="c1"># 4. re-add positional embedding to masks_x as the predictor have no idea where they are from
</span>        <span class="n">x_pos_embed</span> <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="n">predictor_pos_embed</span><span class="p">.</span><span class="nf">repeat</span><span class="p">(</span><span class="n">B</span><span class="p">,</span> <span class="mi">1</span><span class="p">,</span> <span class="mi">1</span><span class="p">)</span> <span class="c1"># [B, N, pred_dim]
</span>        <span class="n">x</span> <span class="o">+=</span> <span class="nf">apply_masks</span><span class="p">(</span><span class="n">x_pos_embed</span><span class="p">,</span> <span class="n">masks_x</span><span class="p">)</span> <span class="c1"># [B*M, Kc, pred_dim]
</span>        <span class="n">_</span><span class="p">,</span> <span class="n">N_ctxt</span><span class="p">,</span> <span class="n">D</span> <span class="o">=</span> <span class="n">x</span><span class="p">.</span><span class="n">shape</span> <span class="c1"># Kc
</span>
        <span class="c1"># 5. Build positional embeddings for TARGET patches 
</span>        <span class="n">pos_embs</span> <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="n">predictor_pos_embed</span><span class="p">.</span><span class="nf">repeat</span><span class="p">(</span><span class="n">B</span><span class="p">,</span> <span class="mi">1</span><span class="p">,</span> <span class="mi">1</span><span class="p">)</span> <span class="c1"># [B, N, pred_dim]
</span>        <span class="n">pos_embs</span> <span class="o">=</span> <span class="nf">apply_masks</span><span class="p">(</span><span class="n">pos_embs</span><span class="p">,</span> <span class="n">masks</span><span class="p">)</span> <span class="c1"># [B*T, Kt, pred_dim]
</span>
        <span class="c1"># we must predict from each mask each target token [B * T, Kt, D] → [B * M * T, Kt, D]
</span>        <span class="n">pos_embs</span> <span class="o">=</span> <span class="nf">repeat_interleave_batch</span><span class="p">(</span><span class="n">pos_embs</span><span class="p">,</span> <span class="n">B</span><span class="p">,</span> <span class="n">repeat</span><span class="o">=</span><span class="nf">len</span><span class="p">(</span><span class="n">masks_x</span><span class="p">))</span> 
        
        <span class="c1"># 6. Create MASK TOKENS for target patches -&gt; writable placeholders where predictions will live
</span>        <span class="n">pred_tokens</span> <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="n">mask_token</span><span class="p">.</span><span class="nf">repeat</span><span class="p">(</span><span class="n">pos_embs</span><span class="p">.</span><span class="nf">size</span><span class="p">(</span><span class="mi">0</span><span class="p">),</span> <span class="n">pos_embs</span><span class="p">.</span><span class="nf">size</span><span class="p">(</span><span class="mi">1</span><span class="p">),</span> <span class="mi">1</span><span class="p">)</span> <span class="c1"># [B*M*T, Kt, pred_dim]
</span>        <span class="c1"># --
</span>        <span class="n">pred_tokens</span> <span class="o">+=</span> <span class="n">pos_embs</span> <span class="c1"># Add positional meaning to each mask token
</span>
        <span class="c1"># Repeat context tokens so each (context, target) pair is evaluated
</span>        <span class="n">x</span> <span class="o">=</span> <span class="n">x</span><span class="p">.</span><span class="nf">repeat</span><span class="p">(</span><span class="nf">len</span><span class="p">(</span><span class="n">masks</span><span class="p">),</span> <span class="mi">1</span><span class="p">,</span> <span class="mi">1</span><span class="p">)</span> <span class="c1"># [B*M*T, Kc, pred_dim]
</span>        <span class="n">x</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">cat</span><span class="p">([</span><span class="n">x</span><span class="p">,</span> <span class="n">pred_tokens</span><span class="p">],</span> <span class="n">dim</span><span class="o">=</span><span class="mi">1</span><span class="p">)</span>  <span class="c1"># [B*M*T, Kc+Kt, pred_dim]
</span>
        <span class="c1"># 7. fwd prop through transformer block
</span>        <span class="k">for</span> <span class="n">blk</span> <span class="ow">in</span> <span class="n">self</span><span class="p">.</span><span class="n">predictor_blocks</span><span class="p">:</span>
            <span class="n">x</span> <span class="o">=</span> <span class="nf">blk</span><span class="p">(</span><span class="n">x</span><span class="p">)</span> 
        <span class="n">x</span> <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="nf">predictor_norm</span><span class="p">(</span><span class="n">x</span><span class="p">)</span>

        <span class="c1"># 8. 
</span>        <span class="n">x</span> <span class="o">=</span> <span class="n">x</span><span class="p">[:,</span> <span class="n">N_ctxt</span><span class="p">:]</span> <span class="c1"># [B*M*T, Kc+Kt, pred_dim] -&gt; [B*M*T, Kt, pred_dim]
</span>        <span class="n">x</span> <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="nf">predictor_proj</span><span class="p">(</span><span class="n">x</span><span class="p">)</span> <span class="c1"># [B*M*T, Kt, target_enc_dim]
</span>
        <span class="k">return</span> <span class="n">x</span>
</code></pre></div> </div> </details> <details class="notion-toggle"> <summary><code class="language-plaintext highlighter-rouge">BONUS</code>: <em>how to use convolution to patchify the image.</em></summary> <ul> <li>the core intuition is that: <ul> <li>i patch and embedd at the same time, so each patch is a vector with an hidden dimension</li> <li>i can get the embedding dimension over the channel dimension</li> <li>i can get non overlapping patches by striding with a size equal to the kernel diameter</li> </ul> </li> </ul> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">class</span> <span class="nc">PatchEmbed</span><span class="p">(</span><span class="n">nn</span><span class="p">.</span><span class="n">Module</span><span class="p">):</span> <span class="c1"># TODO: add study
</span>    <span class="sh">"""</span><span class="s"> Image to Patch Embedding
    
     main objective: [B, C, H, W] -&gt;  [B, H/P * W/P, embed_dim]
    </span><span class="sh">"""</span>
    <span class="k">def</span> <span class="nf">__init__</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">img_size</span><span class="o">=</span><span class="mi">224</span><span class="p">,</span> <span class="n">patch_size</span><span class="o">=</span><span class="mi">16</span><span class="p">,</span> <span class="n">in_chans</span><span class="o">=</span><span class="mi">3</span><span class="p">,</span> <span class="n">embed_dim</span><span class="o">=</span><span class="mi">768</span><span class="p">):</span>
        <span class="nf">super</span><span class="p">().</span><span class="nf">__init__</span><span class="p">()</span>
        <span class="n">num_patches</span> <span class="o">=</span> <span class="p">(</span><span class="n">img_size</span> <span class="o">//</span> <span class="n">patch_size</span><span class="p">)</span> <span class="o">*</span> <span class="p">(</span><span class="n">img_size</span> <span class="o">//</span> <span class="n">patch_size</span><span class="p">)</span>
        <span class="n">self</span><span class="p">.</span><span class="n">img_size</span> <span class="o">=</span> <span class="n">img_size</span>
        <span class="n">self</span><span class="p">.</span><span class="n">patch_size</span> <span class="o">=</span> <span class="n">patch_size</span>
        <span class="n">self</span><span class="p">.</span><span class="n">num_patches</span> <span class="o">=</span> <span class="n">num_patches</span>
        <span class="n">self</span><span class="p">.</span><span class="n">patch_shape</span> <span class="o">=</span> <span class="p">(</span><span class="n">img_size</span> <span class="o">//</span> <span class="n">patch_size</span><span class="p">,</span> <span class="n">img_size</span> <span class="o">//</span> <span class="n">patch_size</span><span class="p">)</span>
        
        <span class="c1"># GOAL: projection [B, C, H, W] -&gt; [B, embed_dim, H/P, W/P]
</span>        <span class="n">self</span><span class="p">.</span><span class="n">in_chans</span> <span class="o">=</span> <span class="n">in_chans</span>
        <span class="n">self</span><span class="p">.</span><span class="n">embed_dim</span> <span class="o">=</span> <span class="n">embed_dim</span>
        <span class="c1"># CORE IDEA -&gt; convolution that patchify over H and P and embedd at the sime time along channels
</span>        <span class="n">self</span><span class="p">.</span><span class="n">proj</span> <span class="o">=</span> <span class="n">nn</span><span class="p">.</span><span class="nc">Conv2d</span><span class="p">(</span><span class="n">in_channels</span><span class="o">=</span><span class="n">self</span><span class="p">.</span><span class="n">in_chans</span><span class="p">,</span> <span class="n">out_channels</span><span class="o">=</span><span class="n">self</span><span class="p">.</span><span class="n">embed_dim</span><span class="p">,</span> <span class="n">kernel_size</span><span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="n">patch_size</span><span class="p">,</span> <span class="n">stride</span><span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="n">patch_size</span><span class="p">)</span> 

    <span class="k">def</span> <span class="nf">forward</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">x</span><span class="p">):</span>
        <span class="c1"># [B, C, H, W] -&gt; [B, embed_dim, H/P, W/P] -&gt; [B, embed_dim, H/P * W/P] -&gt; [B, H/P * W/P, embed_dim]
</span>        <span class="n">x</span> <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="nf">proj</span><span class="p">(</span><span class="n">x</span><span class="p">)</span> <span class="c1"># [B, C, H, W] -&gt; [B, embed_dim, H/P, W/P]
</span>        <span class="n">x</span> <span class="o">=</span> <span class="n">x</span><span class="p">.</span><span class="nf">view</span><span class="p">(</span><span class="n">x</span><span class="p">.</span><span class="n">shape</span><span class="p">[</span><span class="mi">0</span><span class="p">],</span><span class="n">x</span><span class="p">.</span><span class="n">shape</span><span class="p">[</span><span class="mi">1</span><span class="p">],</span><span class="o">-</span><span class="mi">1</span><span class="p">)</span> <span class="c1"># [B, embed_dim, H/P, W/P] -&gt; [B, embed_dim, H/P * W/P]
</span>        <span class="n">x</span> <span class="o">=</span> <span class="n">x</span><span class="p">.</span><span class="nf">permute</span><span class="p">(</span><span class="mi">0</span><span class="p">,</span><span class="mi">2</span><span class="p">,</span><span class="mi">1</span><span class="p">)</span> <span class="c1"># [B, embed_dim, H/P * W/P] -&gt; [B, H/P * W/P, embed_dim]
</span>        <span class="k">return</span> <span class="n">x</span>
</code></pre></div> </div> </details> <h4 id="jepa-and-dino">JEPA and DINO</h4> <div class="notion-callout notion-callout--red"> <div class="notion-callout-icon">🚨</div> <div class="notion-callout-body"> <p>I do not like this take, but let′s keep it for the exam</p> </div> </div> <p>DINO-style self-supervised methods can be interpreted as a special case of JEPA. In DINO, two encoders process different augmented views of the same image, and one encoder predicts the representation produced by the other. The “future” in this case is not a different time or spatial region, but a different augmentation of the same content. I-JEPA generalizes this idea by allowing the target to be a <strong>structured, masked subset</strong> of the input rather than a full alternative view.</p> <h4 id="v-jepa">V-JEPA</h4> <p><strong>CORE IDEA</strong> V-JEPA extends the JEPA framework from images to videos, introducing time as the primary axis of prediction. Instead of predicting masked image regions, V-JEPA predicts the <strong>representations of future video frames</strong> from representations of past frames. In doing so, it learns temporal structure and dynamics without ever generating pixels.</p> <p><strong>CORE ADVANTAGE</strong> Because there is no pixel-level reconstruction and no autoregressive rollout, V-JEPA avoids many of the computational and optimization difficulties associated with video generation. Prediction can be parallelized over time, and the model is not forced to model high-frequency visual detail. What it learns instead are <strong>regularities of motion, object persistence, and event structure</strong>.</p> <p><strong>CHANGES FROM I-JEPA</strong> Extending from I-JEPA, V-JEPA trains on videos and treats videos as 3D images. The key differences are therefore on:</p> <ul> <li>PatchEmd2D \(\rightarrow\) PatchEmd3D</li> <li>MaskSampling2D \(\rightarrow\) MaskSampling3D</li> <li>Other modifications to handle 3D input including 3D positional embeddings (not included)</li> </ul> <p>While the masking strategy becomes 3D, the authors also make changes on how the masks are sampled and how the context and target are defined:</p> <ul> <li>They leverage two types of masks: <ul> <li>Short-range masks, where they take the union of 8 randomly sampled target blocks covering 15% of each frame.</li> <li>Long-range masks, where they take the union of 2 randomly sampled target blocks covering 70% of each frame.</li> <li>In both cases, the aspect ratio for all sampled blocks is randomly chosen in the range (0.75, 1.5).</li> <li>In both cases, the same spatial mask is applied to the full temporal dimension.</li> </ul> </li> <li>The sampled mask is considered as the target, and <em><strong>the context is directly the complement of the target.</strong></em></li> </ul> <p><strong>APPLICATION 1: V-JEPA-2 with Action Conditioning (V-JEPA-2-AC)</strong> V-JEPA-2-AC extends V-JEPA-2 by introducing <strong>actions</strong> into the prediction process, turning it into a latent dynamics model suitable for robotics. <em><strong>In this variant, the predictor is conditioned not only on the current visual embedding but also on robot actions and poses.</strong></em> <strong>The target remains the embedding of a future video frame.</strong></p> <p><img src="/assets/img/posts/notes-on-world-models/notion-3bc815d2904b80aabfeacd0974df97f4.png" alt="notes-on-world-models"/></p> <p><em>Remarkably, this action-conditioned model can be trained with a very small amount of robot data, on the order of tens of hours, because the bulk of the visual and dynamical understanding has already been acquired during large-scale video pretraining. The action-conditioning phase essentially grounds the abstract dynamics learned from internet video into a specific embodied agent</em></p> <p><strong>APPLICATION 2: Zero-Shot Planning with V-JEPA-2-AC</strong> <em><strong>Planning with V-JEPA-2-AC is performed entirely in embedding space</strong></em>. A current observation is encoded into a latent state, and a goal image is encoded into the same latent space. The planner searches for a sequence of actions such that the predicted future embedding, obtained by rolling the latent dynamics forward, matches the goal embedding.</p> <hr/> <div class="notion-divider">✠</div> <h3 id="explicit-world-models">Explicit World Models</h3> <p>An <strong>explicit world model</strong> is able to <strong>reconstruct or predict future observations in the same modality as perception</strong> (e.g., pixels, point clouds, images) during imagined rollouts. Unlike implicit models, the model does not only reason in latent space, but explicitly generates what the agent would <em>see</em> if a certain action sequence were executed.</p> <blockquote> <p><em><strong>The agent can “imagine” future frames of the world, not just abstract symbols. Thinking by actually rendering the future, not just reasoning abstractly about it</strong></em></p> </blockquote> <p><img src="/assets/img/posts/notes-on-world-models/notion-3bc815d2904b801a90cee6ea5e451a42.png" alt="notes-on-world-models"/></p> <p><strong>ARCHITECTURE</strong> Formally, the environment is modeled through two components. A <strong>dynamics model</strong> predicts how the internal state of the world evolves given the current state and an action, while an <strong>observation model</strong> maps that predicted state back to an observable quantity.</p> <h4 id="dinov2-world-model">DINOV2 World Model</h4> <p><strong>CORE IDEA</strong> In this paper by Zhou et al., 2024 illustrates how explicit world modeling can be combined with strong pretrained vision encoders.</p> <ul> <li>In this approach, observations are first encoded using DINOv2 representations, which provide rich and semantically meaningful visual features.</li> <li>A learned dynamics model <strong>(DINO-WM)</strong> then predicts how these representations evolve under actions</li> <li>a decoder reconstructs future observations from the predicted representations</li> </ul> <p><img src="/assets/img/posts/notes-on-world-models/notion-3bc815d2904b80198ce6f74a8f8b5501.png" alt="notes-on-world-models"/></p> <p>At test time, the model can roll forward from an initial observation under different action sequences and generate corresponding future visual trajectories. Because the predicted observations are explicit images, the agent can compare alternative futures visually or use them as input to downstream evaluation modules.</p> <h4 id="diffusion-world-models">Diffusion World Models</h4> <p><strong>CORE IDEA</strong> Diffusion world models extend explicit world modeling by using <strong>conditional diffusion processes</strong> to generate future observations. Rather than predicting a single deterministic outcome, these models learn a generative distribution over possible future trajectories conditioned on past observations and actions.</p> <p><img src="/assets/img/posts/notes-on-world-models/notion-3bc815d2904b80a99f4bf0106a61305a.png" alt="notes-on-world-models"/></p> <p><strong>ARCHITECTURE</strong> In practice, a diffusion world model is trained to denoise future observations given a history of observations and a sequence of actions. Generation starts from noise and iteratively refines the sample until a plausible future observation, or even a full future trajectory, is produced.</p> <p><img src="/assets/img/posts/notes-on-world-models/notion-3bc815d2904b8034afb3f7a88d7ba2e0.png" alt="notes-on-world-models"/></p> <p><em>Because the model represents a distribution rather than a point estimate, multiple diverse futures can be sampled from the same initial state.</em></p> <blockquote> <p><em><strong>Instead of committing to a single imagined future, the agent reasons over a space of plausible futures.</strong></em></p> </blockquote> <hr/> <div class="notion-divider">✠</div> <h3 id="simulator-based-world-models">Simulator Based World Models</h3> <p><strong>CORE IDEA</strong> Instead of learning the environment dynamics and observation functions from data, they <strong>delegate the entire world modeling problem to an external simulator or to the real physical world itself</strong>. <em><strong>Instead of imagining future trajectories through a learned model, the agent evaluates actions by rolling them out inside the simulator.</strong></em></p> <div class="notion-columns"> <div class="notion-column" style="flex: 50 1 0%"> <p><img src="/assets/img/posts/notes-on-world-models/notion-3bc815d2904b80a5820dd7e57ddf301e.png" alt="notes-on-world-models"/></p> </div> <div class="notion-column" style="flex: 50 1 0%"> <p><img src="/assets/img/posts/notes-on-world-models/notion-3bc815d2904b805da23be5eb503020cc.png" alt="notes-on-world-models"/></p> </div> </div> <p>In this setting, the agent does not approximate the transition function \(T\) (to turn latent state into new latent state) or the observation function \(O\) (to turn latent state into pixel). Given a current state and an action, it simply queries the simulator, which returns the next state and the corresponding observation</p> <blockquote> <p><em><strong>The world model already exists; the agent only needs to query it.</strong></em></p> </blockquote> <h4 id="sapien">SAPIEN</h4> <p>SAPien (Simulated Part-based Interactive Environment) is an open-source, physics-rich simulation platform designed specifically for robotics and embodied AI research</p> <p><img src="/assets/img/posts/notes-on-world-models/notion-3bc815d2904b80828675dde3c0908b4f.png" alt="notes-on-world-models"/></p> <blockquote> <p><em><strong>Learning happens on top of physics, not instead of it.</strong></em></p> </blockquote> <p>In practice, SAPien functions as a complete world model: given a state and an action, it deterministically (or stochastically, depending on configuration) produces the next state and observation. This makes it a natural backbone for research in robot learning, sim-to-real transfer, and vision-based control.</p> <hr/> <div class="notion-divider">✠</div> <h3 id="instruction-driven">Instruction Driven</h3> <p><strong>CORE IDEA</strong> Instruction-driven/hybrid world models are models that can be updated, constrained, or corrected via instructions, manuals, or on-the-fly rule discovery.</p> <p><img src="/assets/img/posts/notes-on-world-models/notion-3bc815d2904b80309a3afd145f1a9a4e.png" alt="notes-on-world-models"/></p> <ul> <li>A partial implicit or explicit model handles latent dynamics and perception, so state transition. But before completing the state transition it calls an external component</li> <li>an external reasoning component is typically an LLM or a structured rule base that is either updated and applied or prompted → <em><strong>it proposes constraints or checks consistency</strong></em></li> <li>therefore, we improve via these suggestion the previously predicted state by latent dynamic</li> </ul> <blockquote> <p><em><strong>The world model is no longer only learned; it is instructed. Therefore is hybrid.</strong></em></p> </blockquote> <p><strong>WHEN TO APPLY</strong> This hybrid setup is particularly powerful in environments where rules are known, partially known, or can be described linguistically, such as games, simulated worlds, or structured physical environments.</p> <h4 id="genie-instruction-driven-generation-of-playable-worlds"><strong>Genie: Instruction-Driven Generation of Playable Worlds</strong></h4> <p><strong>CORE IDEA</strong> Given a text prompt, Genie can generate dynamic, navigable worlds that respond in real time to user actions. The first prompt (<em>sunny medieval village</em>) generate the observed world via the world model, the other model the interactions that will happen.</p> <p><strong>ARCHITECTURE</strong> In Genie:</p> <ul> <li>the <strong>implicit or partial model</strong> corresponds to the <strong>autoregressive world model</strong> that predicts the next frame (or latent representation of the next frame) given the full prior trajectory and the current user action.</li> <li><strong>External Reasoner</strong> During interaction, additional prompts or implicit instructions—such as player actions (move forward, turn, jump)—continuously update the conditioning context of the <em>implicit model.</em></li> </ul> <p>Genie Family</p> <p><strong>GENIE 1</strong> introduces instruction-driven world modeling by <em><strong>combining a spatiotemporal video tokenizer with a latent action model</strong></em>. <em>That is a</em> Transformer-like dynamics model predicts the next frame (or latent tokens for the next frame), conditioned on: the past frames (or their tokens), the current user action, and often a text prompt.</p> <div class="notion-callout notion-callout--red"> <div class="notion-callout-icon">🚨</div> <div class="notion-callout-body"> <p><em><strong>Multimodality is handled outside the core model, with all inputs eventually mapped into a unified video–action token stream</strong></em></p> </div> </div> <p><img src="/assets/img/posts/notes-on-world-models/notion-3bc815d2904b804c91cbca5ab0b2c12e.png" alt="notes-on-world-models"/></p> <p><strong>GENIE 2</strong> Genie 2 advances this idea by using an autoregressive <strong>latent diffusion world model</strong>. Frames are encoded into a latent space and passed through a large Transformer dynamics model with causal masking. (<em><strong>classifier guidance allow to steer via text the generation of diffusion models</strong></em>)</p> <div class="notion-callout notion-callout--red"> <div class="notion-callout-icon">🚨</div> <div class="notion-callout-body"> <p><em><strong>same input(action) + state → GENIE → next state</strong></em> but now the state is latent, in GENIE 1 was pixel. Thereafter, a decoder maps it to observation in pixel space</p> </div> </div> <p><img src="/assets/img/posts/notes-on-world-models/notion-3bc815d2904b80968a87fc4edf50b124.png" alt="notes-on-world-models"/></p> <p><strong>GENIE 3</strong> Genie 3 further shifts toward real-time interaction. It is a <em><strong>frame-by-frame</strong></em> autoregressive world model that a_<strong>ttends to the full prior trajectory and maintains visual memory</strong>_. Genie 3 accepts direct text prompts and promptable world events, enabling richer instruction-driven control. While architectural details are not fully disclosed, it is explicitly optimized for interactive frame rates and persistent world state.</p> <p><img src="/assets/img/posts/notes-on-world-models/notion-3bc815d2904b80d4b347df5b7de1bbf1.png" alt="notes-on-world-models"/></p> <p><strong>MULTIMODALITY COMPARISON</strong> In genie 1 everything is handled before the autoregressive world model. All text, image, and actions mapped to the same space on which world model is trained. In last one, Genie 3, instead everyhing is blended together natevily, there is not an embedder, which often times can be an information bottleneck</p> <p><img src="/assets/img/posts/notes-on-world-models/notion-3bc815d2904b80bd85a3fff9c58acd50.png" alt="notes-on-world-models"/></p> <h4 id="cosmos-models-for-physical-ai">Cosmos Models for Physical AI</h4> <p>NVIDIA’s Cosmos World Foundation Model Platform provides an <em><strong>open-source, open-weight</strong></em> ecosystem for building high-fidelity digital twins of the physical world:</p> <ul> <li>very useful to address problem like data-scarcity in training robotics (physical AI) models</li> </ul> <p>Within the Cosmos framework, different model families target different aspects of physical AI:</p> <ul> <li><em><strong>Cosmos Predict</strong></em> focuses on <code class="language-plaintext highlighter-rouge">future state prediction</code>, enabling forecasting and scenario planning by predicting how dynamic environments may evolve.</li> <li><em><strong>Cosmos Transfer</strong></em> provides multimodal simulation-to-simulation or simulation-to-real transfer, allowing policies and representations to generalize across environments.</li> <li><em><strong>Cosmos Reason</strong></em> integrates vision-language reasoning to support higher-level understanding, commonsense reasoning, and decision-making in physical worlds.</li> </ul> <p><img src="/assets/img/posts/notes-on-world-models/notion-3bc815d2904b807e9219e37bf0377a68.png" alt="notes-on-world-models"/></p> <p>These models illustrate how instruction-driven world models can scale from research prototypes to production-grade systems, combining perception, dynamics, reasoning, and instruction-following into unified foundations for physical intelligence.</p> <hr/> <div class="notion-divider">✠</div>]]></content><author><name></name></author><category term="blog"/><category term="tutorial"/><category term="ml"/><summary type="html"><![CDATA[Some notes I took at EPFL on world models from C. Bunne’s course. It is still WIP, some references (e.g., for images) are missing.]]></summary></entry></feed>